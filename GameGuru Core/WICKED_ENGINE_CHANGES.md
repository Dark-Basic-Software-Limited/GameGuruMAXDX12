# Wicked Engine Side Changes — Brief for Upstream Author

This document tracks every change we have made to the `WickedEngineDX12`
clone at `D:\max\WickedEngineDX12` while porting GameGuru MAX to Wicked,
from the point this log began. Engine changes made during the original
port, before this log existed, are summarised in section 1.0 below.

Two categories:

1. **Genuine bug fixes** — should be considered for upstream merge.
2. **Temporary debug overrides** — active during analysis; must be reverted
   before any test is treated as final and before any upstream brief.

The WickedEngineDX12 repo is a separate git repo from GameGuruMAXDX12.
Tracking the changes here (inside GameGuru Core) so they survive in source
control even if the Wicked clone is reset or re-cloned.

---

## 1.0 Pre-existing port-era engine deltas (before this log began)

The clone already carried GG-specific engine modifications from the
original DX12 port before entry 1.1 was written. Pointers only, not
full diffs:

- **RenderPath3D `customDraw` function-pointer hooks** — Wicked commit `9118befe`
- **`customDraw_ShadowMap` / `customDraw_EnvProbe` hooks** — Wicked commits `0a8ea4fb`, `b84ff4fe`
- **SRV count 16 → 64 + sampler expansion**
- **`GetDX12Device` / `GetDX12GraphicsCommandList` accessors**
- **/MTd runtime change**

Full details in MIGRATION_PLAN.md and DX11_to_DX12_Shader_Porting_Plan.md
§13.9. WITHOUT these, a re-cloned WickedEngineDX12 will not render at
all — this doc alone is not sufficient to restore the clone.

## 1. Bug fixes (candidates for upstream)

### 1.28 Terrain: soften SVT mip bias −2.0 → 0.0 (fix mid/long pixel-swim) — supersedes 1.27

**Files:**
- `WickedEngine/shaders/ShaderInterop_Renderer.h` (`SVT_MIP_BIAS` −2.0 → 0.0)
- `WickedEngine/wiTerrain.cpp` (revert 1.27's no-op sampler `max_anisotropy` 8 → 4)

**Why:** the terrain is Wicked's virtual-textured (SVT) terrain, which computes its own LOD in software — `virtual_lod = get_lod(dim, ddx(uv), ddy(uv), GetAnisotropy()) + SVT_MIP_BIAS`, then fetches the physical atlas via `SampleLevel(sampler, atlas_uv, 0)` (explicit LOD). Stock Wicked sets `SVT_MIP_BIAS = -2.0` — biasing terrain TWO mip levels sharper than the Nyquist-correct LOD for crispness. That over-sharpening under-samples the ground at mid/long range → it "swims"/shimmers as the camera creeps. **Because the atlas fetch is `SampleLevel` (explicit LOD), the material sampler's anisotropy AND mip_lod_bias are IGNORED — so 1.27's ×4→×8 sampler bump was a confirmed visual no-op (reverted here).** The real terrain LOD lever is `SVT_MIP_BIAS`, which biases the streaming FEEDBACK and the SAMPLE together (in sync). Set to 0.0 = Nyquist-correct: mip-based swim can't occur, near field stays crisp.

**Behaviour:** affects ALL SVT sampling (terrain is GG's only SVT user). 0.0 vs stock −2.0 = terrain ~2 mips softer at a given distance but stable; near field near-correct (crisp). User-tuned on TESTPRO1 (A/B'd −1.0/0.0/+1.0 and a +4.0 extreme that confirmed the lever); picked 0.0. Shared header — a GAME rebuild (or engine relaunch) recompiles the ~375 engine shaders that include it (`refresh_shaders` handles staleness). Tune toward +1.0 for more distance stability (foreground softens too), toward −2.0 for more crispness (swim returns).

### 1.27 Terrain: raise Wicked SVT terrain anisotropy ×4 → ×8 (fix grazing pixel-swim) — REVERTED (no-op, see 1.28)

**Files:**
- `WickedEngine/wiTerrain.cpp` (the terrain SVT sampler in the material-update loop: `samplerDesc.max_anisotropy = 4` → `8`, assigned to every terrain chunk material's `sampler_descriptor`)

**Why:** GameGuru's shipping terrain renders through Wicked's NATIVE SVT terrain path — the GGTerrain custom-draw path is gated OFF: `ggterrain_use_wicked_terrain = 1` (default) forces `ggterrain_draw_enabled = 0` in MasterRenderer::Update (master_part1.cpp), so `GGTerrain_Draw` early-returns and never textures anything (confirmed live via harness `TERRAIN_DRAW_EN 0` / `TERRAIN_DRAW_COUNT 0`). **So the entire GGTerrain page-cache / GGTERRAIN_TEXTURE_FILTERING machinery is dead code on the shipping image — the real terrain sampler is this one in wiTerrain.cpp.** Stock Wicked creates it as anisotropic ×4, but every OTHER surface in the scene samples at ×16 (SAMPLER_OBJECTSHADER). Terrain is the surface most often viewed near edge-on, and ×4 only resolves a 4:1 minified footprint — at grazing angles the true per-pixel footprint far exceeds that, so the minor axis is under-sampled and the ground "swims" as the camera creeps. ×8 doubles the resolved footprint (8:1), covering the bulk of near-ground horizon views, and also sharpens the normal map (same sampler) reducing specular shimmer.

**Behaviour:** sampler-state only, fully reversible, affects only terrain-chunk materials (and decals that reuse the base material's sampler — a harmless quality bump). No VRAM change, no shader recompile (bindless sampler read at runtime). ×8 stays within the 4-texel SVT tile border (`SVT_TILE_BORDER`) so aniso taps don't spill across page edges — no tile seams (verified on the TESTPRO1 grazing view). ×16 (matching the rest of the scene) is the escalation if grazing is still soft, but at ×16 the kernel can reach past the 4px border at extreme grazing (faint tile-seam risk) unless the border is also widened; ×8 is the safe default. Fixes the user-reported "a lot of pixel swim on the terrain when moving the camera slowly." NOTE for future work: do NOT chase the GGTERRAIN_TEXTURE_FILTERING macro or GGTerrain_part0.cpp samplerAnisotropicWrap for terrain filtering — that path does not render.

### 1.26 Ocean: env-map reflection fallback when planar reflections are OFF (fix garbage-water corruption)

**Files:**
- `WickedEngine/wiRenderPath3D.cpp` (`RenderPath3D::PreRender()` — guard the main camera's `texture_reflection_index` / `texture_reflection_depth_index` writes with the SAME condition that gates the planar reflection render below, `getReflectionsEnabled() && visibility_main.IsRequestedPlanarReflections()`; force both to -1 in the else)

**Why:** the DX12 ocean PS (`oceanSurfacePS.hlsl:63`) branches on `texture_reflection_index >= 0` — planar reflection when set, else the stable `EnvironmentReflection_Global` sky/global-probe fallback. But `PreRender` wrote that index UNCONDITIONALLY every frame from `rtReflection_resolved`, with no reflections-enabled guard (unlike the adjacent VXGI-specular field, which IS guarded). When the user unticks "Reflections", `setReflectionsEnabled(false)` frees `rtReflection`/`depthBuffer_Reflection` but NOT `rtReflection_resolved`/`depthBuffer_Reflection_resolved`, so `GetDescriptorIndex` kept returning a valid index >= 0 while the planar render that refills that texture was gated off — the ocean took the planar branch and sampled a stale/uninitialised texture = bright blocky garbage on the water. DX11 never hit this: its ocean sampled a fixed slot and the renderer bound `wiTextureHelper::getTransparent()` when reflections were off — a safety net the DX12 bindless-index rewrite dropped. The guard restores it: the index becomes the -1 sentinel the shader keys on, so the ocean uses its env-map fallback (reflect the sky/probe) — stable and ~free (skips the whole planar scene re-render).

**Behaviour:** reflections ON is byte-identical — the guard condition is the exact same as the planar-render gate, so whenever the mirror is actually rendered the same index is written as before. Reflections OFF now shows env-map water (sky/probe reflection, matching the DX11 look) instead of garbage, AND is cheaper: measured on the TESTPRO1 island editor, OFF 57.9 FPS vs ON 51.3 FPS (skips the planar scene re-render). Also closes a latent case where reflections are enabled but no planar reflection is requested that frame (the index would otherwise point at the stale resolved texture). C++-only — no shader recompile. Fixes the user-reported "unticking reflection corrupts the water". Harness A/B lever: `SET_REFLECTIONS 0|1` (game `AutomationHarness.cpp`).

### 1.25 Hair particles: skip billboard-write + physics for non-drawn (culled) strands

**Files:**
- `WickedEngine/shaders/hairparticle_simulateCS.hlsl` (early-out after the wave-atomic index append: `if (!visible && !regenerate_frame) return;`)

**Why:** the hair/grass simulate compute shader ran the full per-strand billboard vertex writes + physics integration for EVERY strand, even those culled from the draw (distance/frustum) — their output is never referenced by the culled index buffer, so it's pure wasted GPU work. The early-out bails for non-visible strands. The wave-atomic index append above it still runs for every lane (wave-safe), and `regenerate_frame` strands never bail (they must seed sim state + build the static primitive buffer). **Perf/upstream-worthy, not a GG behaviour change.**

**Behaviour:** drawn (visible) strands are byte-identical, so the rendered grass is unchanged; a culled strand's sim state freezes until it re-enters view (imperceptible under low wind, none for a static camera). Measured on the TESTPRO1 editor: grass sim 52.0 → 48.3 ms (the dominant cost is the ~905K simulated strand count itself, which is density-bound). Saves proportional to the off-screen strand fraction — more when zoomed into a small edit area.

### 1.24 Ocean: "Water Base Color" tints transparent water (depth-based surface tint)

**Files:**
- `WickedEngine/shaders/ShaderInterop_Ocean.h` (`OceanCB::xOceanWaterColorDepth` + 3 pad floats)
- `WickedEngine/wiOcean.h` (`OceanParameters::water_color_depth` default 0 = stock)
- `WickedEngine/wiOcean.cpp` (`GetOceanCBAtDim` fills `xOceanWaterColorDepth` from the param)
- `WickedEngine/shaders/oceanSurfacePS.hlsl` (lerps `surface.refraction.rgb` toward `xOceanWaterColor.rgb` by `saturate(abs(water_depth) * xOceanWaterColorDepth)`, right after the extinction/transmittance step)

**Why:** on the ocean SURFACE the base colour (`xOceanWaterColor.rgb`) only reaches the eye through opacity — `surface.albedo` blended by `refraction.a = exp(-water_depth * color.a)` and a depth-tinted reflection, all gated by `color.a` (WaterAlpha). GameGuru's water runs at **WaterAlpha 0** (fully transparent, so you can see the seabed), which means the base colour contributes essentially nothing — setting "Water Base Color" to red left the sea unchanged from above. This is the ocean CB used by `oceanSurfacePS` (NOT the `ShaderOcean` in ShaderInterop_Weather.h; that one feeds underwaterCS/lighting). `water_color_depth` tints the see-through refraction toward the base colour with depth, alpha-independent — clear shallows, coloured depths, like real water absorbing light — so the colour is visible on transparent water. Tint scales with the colour's own saturation (a subtle blue tints gently, a bold colour reads strongly).

**Behaviour:** no stock behaviour change — `water_color_depth` defaults to 0, which skips the lerp (`saturate(0) == 0`). GG sets 0.005 (M-GridEditB_part3.cpp) for all water-enabled levels, so existing GG water now shows its base colour with depth (an improvement — most water_color values are sensible blues; a level that relied on the colour being invisible will now show it). Paired with the same commit's picker fix (the Water Base Color picker preserves WaterAlpha instead of forcing 255), so changing the colour no longer forces the surface opaque OR leaves it colourless — it stays transparent AND takes the colour.

### 1.23 Ocean: decouple the underwater view (fog colour + density) from the water surface

**Files:**
- `WickedEngine/shaders/ShaderInterop_Weather.h` (`ShaderOcean::underwater_fog_density` reuses the `caustic_pad0` slot; new `ShaderOcean::underwater_color` float4)
- `WickedEngine/wiOcean.h` (`OceanParameters::underwater_fog_density` default 0 = stock; `underwater_color` default deep blue-green)
- `WickedEngine/wiScene.cpp` (marshals both into `ShaderOcean`, next to `caustic_scale`)
- `WickedEngine/shaders/underwaterCS.hlsl` (computes `uw_density`/`uw_color` with a stock fallback; uses them for `waterfog`, `transmittance` and the waterline intersection band instead of `water_color.a`/`water_color.rgb`)

**Why:** the `underwaterCS` post-process (the submerged fog/tint/extinction) took its colour from `water_color.rgb` and its density from `water_color.a` — the SAME values that drive the water SURFACE (`oceanSurfacePS`). So the underwater look could not be tuned independently, and — worse — GameGuru's "Water Base Color" picker, on any edit, wrote `WaterAlpha = 255` (it read the picker alpha as a hard-coded 1.0), which via `water_color.a` turned the surface fully opaque/solid ("lost all transparency"). Decoupling lets the surface keep transparent water while the submerged view fogs to its own colour/distance.

**Behaviour:** no stock behaviour change — `underwater_fog_density` defaults to 0, and the shader then falls back to the original `water_color.a` / `water_color.rgb` path exactly. GG sets density > 0 (from the new **"Underwater Fog"** slider) and **"Underwater Color"** picker, and also fixes the surface picker to preserve alpha so changing the water colour no longer forces it opaque. Density scale tuned on the TESTPRO1 deep-water vista (slider 0..100 -> density 0..0.015; default 20 -> 0.003 shows seabed + caustics with a natural blue depth fade).

### 1.22 Ocean: decouple seabed caustic size from the wave patch size

**Files:**
- `WickedEngine/shaders/ShaderInterop_Weather.h` (`ShaderOcean::caustic_scale` + 3 pad floats)
- `WickedEngine/wiOcean.h` (`OceanParameters::caustic_scale` default 1.0 = stock)
- `WickedEngine/wiScene.cpp` (marshals `ocean.caustic_scale` into `ShaderOcean`, next to `patch_size_rcp`)
- `WickedEngine/shaders/lightingHF.hlsli` (the `texture_caustics` seabed lookup samples `ocean_uv * ocean.caustic_scale`; the displacement / water-height lookup deliberately keeps the UNscaled `ocean_uv`)

**Why:** the underwater "caustic" light ripples on the sea floor are the `texture_caustics` term inside `light_directional`, sampled at `surface.P.xz * ocean.patch_size_rcp`. Because that UV is tied to `patch_length`, the only in-editor control over caustic size was **Water Tiling Patch Size** — which ALSO drives the wave FFT, so the ripples could not be enlarged without blowing the waves up to absurd size. `caustic_scale` multiplies ONLY the caustic lookup, so ripple size is now independent of wave size. GG drives it from a new Water-panel **"Caustic Size"** slider (`visuals.fWaterCausticSize`, scale = 1/size). Measured, patch length pinned: 1.0 = fine stock speckle, 0.3 = larger cells, 0.1 = broad soft blobs.

**Behaviour:** no stock behaviour change — `caustic_scale` defaults to 1.0, which samples identically to before.

**Diagnostic note (this cost hours):** the terrain's LIVE seabed caustics are in THIS engine `lightingHF.hlsli`, NOT the game copy `GGTerrain/Shaders/PBR/lightingHF.hlsli`. `GGTerrainVirtualPBR_PS.hlsl` comments out `ForwardLighting` and calls `GGTiledLightingWithAmbient` (GGLighting.hlsli), whose `light_directional` resolves here — so the game's `lightingHF.hlsli` is DEAD CODE for terrain. Earlier caustic edits made to the game copy (commits `14b90e0a` / `565f704b`) never ran; a follow-up should strip that dead plumbing (GGCustomFrameCB `causticScale`, GGFrameCompat macro, the game `lightingHF` block).

### 1.21 VT: expanded working set — island-wide full-res zone + removal margin

**Files:**
- `WickedEngine/wiTerrain.h` (`Terrain::gg_near_ring_dist` default 2 = stock; `Terrain::gg_removal_margin` default 0 = stock)
- `WickedEngine/wiTerrain.cpp` (`required_resolution = dist < gg_near_ring_dist ? max : min`; `removal_threshold = generation + 2 + gg_removal_margin`)

**Why:** the user's "bigger buffer" insight, translated. The residual violent-zoom squares came from the full-resolution zone being only ±2 chunks (~10k inch-units) — a fast camera crosses it in milliseconds, and every crossing is a VT cache re-reference (re-init, residency reset, re-stream) no heuristic can fully hide. The atlas itself cannot grow (16384² is the D3D12 texture-dimension limit) but it never needed to: tile residency is feedback-driven (≈ what's on screen), so widening the zone costs almost no physical tiles — only per-chunk residency structures. GG sets the zone to ±6 chunks: the whole island lives INSIDE it, so camera travel over the island never crosses a resolution boundary and near/correct tiles are never re-referenced — only genuinely new far detail streams in. The removal margin (+12) keeps chunks alive across zoom travel, eliminating destroy/recreate cache flushes.

**Behaviour:** no stock behaviour change — both fields default to stock values.

### 1.20 VT: tile allocator freeze while the camera is in motion

**Files:**
- `WickedEngine/wiTerrain.cpp` (`UpdateVirtualTexturesCPU`: while `gg_center_stable_frames < 10` — tile aging (free_frames++) suspended; the reusable-tile list only admits tiles unused for 60+ frames (provably off-screen) instead of 1+; the free list entering the chunk loop is filtered the same way so chunk-init tail allocations can't steal either)

**Why:** the final piece of the violent-zoom "random squares". The GPU feedback that keeps a visible tile alive arrives on a 2-3 frame delayed readback; during fast camera moves the keep-alive gaps let STILL-VISIBLE, correct tiles age into the free list, and the flood of misses from newly-visible terrain then steals them — another chunk renders its pixels into a square that was correct and on screen. Now, while the camera is crossing chunk boundaries, nothing recently used can be recycled: correct terrain keeps every tile unconditionally, and newly approached terrain streams from the genuinely stale pool (or stays coarse if it drains — "under construction", exactly the acceptable behavior). Full streaming resumes the moment the camera settles; dropped requests cost nothing since the GPU re-requests missing pages every frame.

**Behaviour:** gated by `gg_vt_upgrade_hysteresis` (default false = stock; the threshold falls back to stock `>= 1` when the camera is stable or the flag is off).

**HOTFIX (same day):** the first version ALSO froze tile aging (`free_frames++`) while the camera moved — fatal under sustained motion: nothing ever crossed the recycle threshold, the pool starved, chunk inits failed to allocate their tail tiles and (stock has no retry path) the whole terrain degraded to flat untextured squares within ~2 minutes of continuous zooming. Aging now always runs — the 60-frame threshold alone is the protection (visible tiles oscillate at 0-3 frames via keep-alives, unreachable; off-screen tiles keep flowing into the pool). Added `gg_tail_invalid` self-heal: a chunk whose tail-tile allocation failed re-inits automatically once the pool has tiles again (fixes stock's silent no-retry fragility on pool exhaustion too).

### 1.19 VT: stale tile-identity release + full freeze-while-moving

**Files:**
- `WickedEngine/wiTerrain.h` (`VirtualTexture::free`: releases `physical_tiles[].last_used` ownership by identity before clearing the tiles vector; 1.18 flag comment updated)
- `WickedEngine/wiTerrain.cpp` (`UpdateVirtualTexturesCPU`: the 1.18 defer logic now freezes BOTH directions — downgrades as well as upgrades — while the camera is crossing chunk boundaries; when stable, downgrades run immediately and upgrades stay budgeted)

**Why (part 1, the real bug — stock, upstream-worthy):** `PhysicalTile::last_used` stores a raw pointer into the owning `VirtualTexture::tiles` vector and `check_tile_resident` compares by POINTER IDENTITY. `free()` cleared the vector without releasing those back-pointers, and a later `tiles` allocation (fast zoom in/out = min→max→min re-inits with identical vector sizes) frequently reuses the same heap block — the stale `last_used` then matches a brand-new tile by address. Consequences: the page table keeps mappings to physical tiles that were freed and recycled to OTHER chunks, and `request_residency` skips the re-render because the tile "is already resident" — random squares of another chunk's pixels, healing gradually as GPU feedback re-requests. Fixed by nulling matching `last_used` entries in `free()` before `tiles.clear()`.

**Why (part 2):** 1.18 froze only upgrades; downgrades still re-initialized mid-motion, churning the pool and (via part 1) seeding stale identities. Now no VT reference changes at all while the camera is moving — chunks render their existing correct tiles; the ring re-balances only after the camera settles.

**Behaviour:** part 1 is an unconditional correctness fix (stale-identity sampling was never intended); part 2 stays behind `gg_vt_upgrade_hysteresis` (default false = stock).

### 1.18 Terrain: VT residency upgrade hysteresis

**Files:**
- `WickedEngine/wiTerrain.h` (`Terrain::gg_vt_upgrade_hysteresis` + `gg_prev_center_chunk`/`gg_center_stable_frames`)
- `WickedEngine/wiTerrain.cpp` (`Generation_Update`: center-chunk stability counter; `UpdateVirtualTexturesCPU`: residency UPGRADES (min→max when a chunk enters the dist<2 near ring) deferred until the camera holds one chunk for 10 frames, then budgeted at 4 per frame)

**Why:** the near ring is only ±2 chunks; a fast camera zoom sweeps that boundary across the island and every chunk crossing far→near called `vt.init()` mid-motion — residency reset, tail-mip rendering, detail tiles landing over several GPU-feedback frames = square tiles of mixed sharpness (and the odd recycled tile showing a neighbour's old pixels for a frame) flickering while the camera moved, calming on stop. With hysteresis, crossing chunks keep rendering their existing correct low-res tile (soft, stable); once the camera settles, the ring sharpens over a few frames. Downgrades (frees tile-pool memory), fresh chunks (resolution 0) and unbound-material chunks (in-place regen rebinds) are never deferred.

**Behaviour:** no stock behaviour change — flag defaults false.

### 1.17 Terrain: gg_generate_blendmap — chunks born with game-correct blendmaps

**Files:**
- `WickedEngine/wiTerrain.h` (`Terrain::gg_generate_blendmap` callback + `ChunkData::gg_blendmap_generated`)
- `WickedEngine/wiTerrain.cpp` (`Generation_Update` regen branch: after the chunk's vertex data completes and before `CreateChunkRegionTexture`, non-preserved chunks call the callback; its success is recorded in `gg_blendmap_generated`)

**Why:** Wicked streams chunks — beyond `removal_threshold` the whole ChunkData is erased, and a fast camera zoom re-creates those chunks from scratch. They were born with the ENGINE-default region weights (grass-heavy `region_base`) and rendered that for at least one frame — several during bursts — until GG's main-thread blend passes rewrote them: chunk-shaped green default-blend squares flickering on sand during quick zooms, calming when the camera stopped. With the callback, the generator thread fills GG's DX11-style auto weights + painted overrides BEFORE the region texture is built, so the first pixel a streamed chunk ever renders is already correct. The game latches `gg_blendmap_generated` chunks in both passes (keys only, no rewrite, no VT churn) and clears the flag from the edit bridge so real edits reprocess. Callback returning false (e.g. GG materials not set up during initial level load) falls back to engine-default weights + the bulk passes — load behavior unchanged.

**Behaviour:** no stock behaviour change — the callback is null by default.

### 1.16 VT repaint flag: main-thread latch (lost-update race fix)

**Files:**
- `WickedEngine/wiTerrain.h` (`VirtualTexture::gg_repaint_blendmap_latched`)
- `WickedEngine/wiTerrain.cpp` (`UpdateVirtualTexturesCPU`: the main-thread pickup consumes `pending_repaint_blendmap` — clears it, sets the latch, re-binds `vt.blendmap`; the async job's repaint loop consumes the LATCH instead of the live flag)

**Why:** found by adversarial multi-agent review of 1.15 (three independent reviewers converged on it; 8/9 verifiers confirmed against the code). The 1.13 flag had two unsynchronized consumers: the main-thread re-bind (runs inside Generation_Update BEFORE the async `virtual_texture_ctx` job launches) and the job's repaint loop, which blind-cleared the live flag. GG's blend passes run AFTER Generation_Update in the same frame and set the flag while that frame's Low-priority job may still be queued or running (the Low pool is THREAD_PRIORITY_LOWEST and starves under load). If the job consumed a freshly-set flag, it re-rendered tiles against the OLD `vt.blendmap` binding and dropped the request — the edit never landed, and the chunk stayed visually pre-edit until the next edit touched it. Mid-drag the bridge's key-erase re-fires the pass so it self-healed; the FINAL settle of a drag/paint burst had no retry — and 1.15 routes exactly that one-shot settle through this path. Ownership after the fix: game code only SETS the live flag; the main-thread block (previous job already joined at function top) transfers it to the latch; only the job clears the latch. Delivery timing unchanged (next frame).

**Behaviour:** no stock behaviour change — both flags are passive unless a consumer sets them.

### 1.15 Terrain: preserve blendmap + VT residency across in-place chunk regen

**Files:**
- `WickedEngine/wiTerrain.h` (`Terrain::gg_preserve_blendmap_on_regen`, default false = stock)
- `WickedEngine/wiTerrain.cpp` (`Generation_Update`: regen branch skips the blendmap_layers resize/overwrite and the GPU `blendmap = {}` reset when the flag is up and the old chunk has usable layers; merge epilogue skips `vt->invalidate()` for removable/regenerated chunks; `UpdateVirtualTexturesCPU`: `gg_material_rebind` detects the freshly-merged bindingless MaterialComponent and re-runs the atlas bind block against the EXISTING vt without `vt.init()`)

**Why:** sculpting in GG regenerates the brush's chunks in place EVERY drag frame. Stock regen (a) overwrites the chunk's blendmap layers with engine-default base/slope/altitude region weights — clobbering GG's DX11-style multi-layer blendmap and flashing wrong textures — and (b) `vt->invalidate()` in the merge epilogue resets the sparse VT's residency, so all tiles re-stream through multi-frame GPU feedback round-trips = chunk-shaped blur/checker for the whole duration of a sculpt drag, healing only after mouse release. With the flag up, only the mesh regenerates; blendmap layers, the GPU blendmap texture and VT residency all survive. The one wrinkle: the merge still replaces the chunk's MaterialComponent with a fresh (bindingless) one — `gg_material_rebind` re-binds it to the surviving vt in the same frame, no reset. GG's blend passes rewrite the weights right after regen anyway (the bridge erases the processed keys), delivered via the 1.13 resident-tile repaint.

**Behaviour:** no stock behaviour change — flag defaults false.

### 1.14 Terrain: optional generation restart on dirty materials

**Files:**
- `WickedEngine/wiTerrain.h` (`Terrain::generation_restart_on_dirty_materials`, default true = stock)
- `WickedEngine/wiTerrain.cpp` (`Generation_Update` material-dirty check guarded by the flag)

**Why:** stock Wicked restarts generation (full chunk teardown + rebuild) whenever any terrain material component is dirty — an editor convenience for material parameter tweaks. GG registers painted-material blendmap slots at RUNTIME (incremental, no restart), and the freshly-created material is dirty for one frame until the scene update launders it. Whether the restart fired depended on frame ordering: real editor paint strokes (GGTerrain_Update runs before GGTerrainWicked_Update in the same frame) hit it; harness strokes did not — a 4-5s full-terrain flicker on the first stroke with each new texture that only reproduced by hand. GG sets the flag false at terrain init: GG owns its blendmaps, material dirt must never rebuild the island.

### 1.13 VirtualTexture::pending_repaint_blendmap — instant blendmap-edit refresh

**Files:**
- `WickedEngine/wiTerrain.h` (`VirtualTexture::pending_repaint_blendmap` flag)
- `WickedEngine/wiTerrain.cpp` (`UpdateVirtualTexturesCPU`: main-thread blendmap rebind + update-job emits UpdateRequests for every currently RESIDENT tile, then clears the flag)

**Why:** after a terrain paint/blend edit, GG rebuilds the chunk's blendmap texture and previously called `vt->invalidate()` — which resets the sparse VT's residency and re-streams the whole chunk through multi-frame GPU-feedback round-trips. On a 65536-res near chunk that took **4-5 seconds** for the paint stroke to become visible. The new flag keeps residency intact: the blendmap is rebound and all resident tiles are re-rendered in place, so the edit lands on screen the next frame. Far single-tile chunks (no residency) keep using `invalidate()` (cheap for them).

**Behaviour:** no stock behaviour change — the flag is passive unless a consumer sets it.

### 1.12 Terrain ChunkData::merge_pending — stale-mesh window flag

**Files:**
- `WickedEngine/wiTerrain.h` (`ChunkData::merge_pending` field)
- `WickedEngine/wiTerrain.cpp` (set alongside the `invalidated = false` flip when the generator finishes a chunk; cleared for all chunks right after `MergeFastInternal` in `Generation_Update`)

**Why:** when a chunk is invalidated (spline edit upstream; GG sculpt/paint bridge in the fork), the generator regenerates it in place reusing the entity, but the regenerated mesh only replaces the main-scene mesh at the NEXT `Generation_Update`'s merge. In that window `invalidated` is already false while the main-scene `MeshComponent` is still the pre-regeneration version. Consumers that bake data from the chunk mesh (GG's DX11-style + painted blendmap passes) would read the stale mesh and latch wrong results permanently (entity reuse means entity-churn detection never fires). `merge_pending` brackets the window exactly: set on the generator thread when the chunk's regen completes, cleared on the main thread once the merge lands. Consumers skip chunks with either flag up.

**Behaviour:** no functional change to stock Wicked rendering — the flag is passive unless something reads it.

### 1.11 Delayed shadow cascades: staggered per-cascade refresh

**Files:**
- `WickedEngine/wiRenderer.h` (`SetDelayedShadowCascadesEnabled` / `GetDelayedShadowCascadesEnabled` / `InvalidateDelayedShadowCascades`)
- `WickedEngine/wiRenderer.cpp` (cadence + frozen matrices in `UpdatePerFrameData`; `DrawShadowmaps` LoadOp::LOAD + per-rect clear draws + per-cascade skips; `PSO_shadowClear_GG`)
- `WickedEngine/shaders/shadowClearPS.hlsl` (new — in-renderpass rect clear, paired with screenVS + DSSTYPE_WRITEONLY)

**Date:** 2026-07-18 (Wicked commit `38a9e82a`)

**MODIFIED 2026-07-23 (Wicked commit `8c89731b`) — cascade-blend SYNC (fixes terrain-shadow flicker):**
the original `%2 %3 %4 %9` cadence + load-leveling deliberately DESYNCED cascades 1/2/3. But the lighting
shader deterministically blends adjacent cascades in the edge-fade band, so a boundary pixel lerps between
two cascades holding different-frame snapshots — whose silhouettes differ by per-cascade terrain LOD floor
(GGTerrain_part0.cpp) and by the terrain draw's per-frame `IsGenerating()/IsVisible()` chunk set during VT
re-stream — producing a "two terrain shapes" flicker even at a static camera. New cadence: cascade 0 every
frame, cascades 1..N refreshed TOGETHER every other frame, so every far-cascade boundary is always
self-consistent (still ~halves the staggered cascades' cost). USER-CONFIRMED fix. See
[[project-shadow-flicker]]. (Also: the DX12 "Delayed Shadows" UI checkbox is now wired to
`SetDelayedShadowCascadesEnabled` — game `a756c627` — and the shadow LOD override to it too — `d770ed00`.)

#### Use case in GameGuru MAX

Port of production DX11's "delayed shadows" (`g_bDelayedShadows`, old
WickedRepo wiRenderer.cpp GGREDUCED blocks): sun cascades refresh at
60/30/20/15/6.7 fps (c0 every frame, then %2 %3 %4 %9) with the DX11
load leveler and 64-inch camera-translation override. Skipped cascades
keep their atlas contents (the atlas is LOADED, and each rect that
renders clears itself with a scissored draw) and sample with frozen
matrices. Forced full refresh on: atlas grow/repack, directional rect
move, sun rotation and cascade-split changes (change-latched GG hooks in
`WickedCall_SetSunDirection` / `WickedCall_SetShadowRange`), and
`InvalidateDelayedShadowCascades()`. GG enables it at sun creation
(master_part1.cpp); the `DELAYED_SHADOWS 0|1` harness command A/Bs it
live. Measured on TESTPRO1 (static camera): Shadowmap Rendering CPU
2.32 -> 1.04 ms, GPU 0.62 -> 0.17 ms, visuals unchanged. Default OFF =
stock behaviour bit-for-bit.

### 1.10 Ocean: world-unit scale + intensity knob for shore/wave foam

**Files:**
- `WickedEngine/wiOcean.h` (`float foam_unit_scale = 1` + `float foam_amount = 1` on OceanParameters)
- `WickedEngine/wiOcean.cpp` (`GetOceanCBAtDim` fills the two new CB fields)
- `WickedEngine/shaders/ShaderInterop_Ocean.h` (CB padding slots become `xOceanFoamUnitScale` / `xOceanFoamAmount`)
- `WickedEngine/shaders/oceanSurfacePS.hlsl` (FOAM block: depth differences, shallow-water gate and noise positions multiplied by the unit scale; final foam multiplied by the amount)

**Date:** 2026-07-18 (Wicked commit `da60bfad`)

#### Use case in GameGuru MAX

The stock foam math is tuned for meters: shore band `exp(-depth_diff * 2)`,
wave foam gated on ~10m shallows, foam noise sampled per-meter. GG's world
is inch-scaled, so the shore foam band was ~40x too thin (a hairline at the
water's edge) and the noise repeated every inch. GG passes
`foam_unit_scale = 0.08` and `foam_amount = 1.3` (globals
`g_fWaterFoamUnitScale` / `g_fWaterFoamAmount` in M-GridEditB_part3.cpp,
live-tunable via the `SET_OCEAN` harness command). Note 0.08 is deliberately
NOT the pure inch conversion (0.0254): GG beaches are so shallow that a
true 1.5m-deep foam band covers a huge horizontal area — 0.08 was picked
visually on TESTPRO1 (bold shore line + foam collar around protruding
rocks, no milky blanket). Defaults 1/1 = zero change for stock scenes.

### 1.9 Animation/transform hardening: unit-quaternion guards + decompose validation

**Files:**
- `WickedEngine/wiScene_Components.cpp` (`TransformComponent::ApplyTransform` validates the `XMMatrixDecompose` result; on garbage keeps previous rotation/scale, takes translation from the world matrix; tripwire logs a symbolized call stack to `applytransform_garbage.txt`)
- `WickedEngine/wiScene.cpp` (both rotation write-back sites in `RunAnimationUpdateSystem` enforce a unit quaternion — renormalize, identity for zero/NaN/inf; tripwire logs inputs to `anim_garbage.txt`)

**Date:** 2026-07-18 (Wicked commit `a4539a76`)

#### Use case in GameGuru MAX

Fixes the intermittent "exploded skinned model" corruption (the Island
Showdown parrots): during level-load bursts, garbage rotations (a valid
axis scaled by thousands — an unnormalized accumulation) could be baked
into a few bones' `rotation_local` and persist for the entire session
whenever that bone's animation never re-evaluated (the stopped/culled
case), rendering the model as giant coloured sheets across the sky.
`XMQuaternionSlerp` propagates garbage instead of healing it, and
`XMMatrixDecompose` on a sheared/torn matrix emits huge quaternion
components — these guards make the corrupt pose impossible to store or
propagate. Paired with the GG-side `WickedCall_SanitizeSkeletons()`
(end of level load: repairs non-unit bone rotations + forces one
evaluation of every animation under the reveal cover). The tripwire
files only ever get written when a guard actually fires — if they appear
next to the exe, the original writer can finally be identified from them.

### 1.8 Terrain: optional high-priority generation jobs

**Files:**
- `WickedEngine/wiTerrain.h` (`bool generation_high_priority = false` on Terrain)
- `WickedEngine/wiTerrain.cpp` (generator workload + per-chunk vertex dispatch contexts pick High vs Low pool from the flag)

**Date:** 2026-07-18

#### Use case in GameGuru MAX

The generation workload AND its per-chunk parallel dispatches run on the
Low job pool, whose threads are `THREAD_PRIORITY_LOWEST` — the OS starves
them whenever the CPU is busy, which is precisely the level-load window
where terrain generation matters most. With the flag on, generation runs
on the High pool. GG enables it only while the camera-facing cone is
still building (< 40% of the ring total): combined with deltas 1.7 +
the GG-side budget turbo, the visible terrain on TESTPRO1 is COMPLETE
by the time the loading screen dismisses (generation races ahead behind
the load). Dropped back to Low afterwards — leaving it High during the
off-camera fill cost the editor 40-55 → 19-43 FPS. Default false =
stock behaviour.

### 1.7 Terrain: camera view-cone priority for chunk generation

**Files:**
- `WickedEngine/wiTerrain.h` (`bool generation_view_cone_priority = false` on Terrain)
- `WickedEngine/wiTerrain.cpp` (Generation_Update captures the camera's horizontal look direction; the generation job runs a cone-filtered spiral pre-pass before the normal outward spiral)

**Date:** 2026-07-18

#### Use case in GameGuru MAX

The stock generation spiral is omnidirectional, so after a level load the
chunks BEHIND the camera build at the same priority as the mountain the
user is staring at. With the flag enabled, a pre-pass sweeps the same
spiral restricted to chunks within ~70° of the camera's look direction
(plus the two rings immediately around the camera), then the normal
spiral fills the rest. `request_chunk` fast-skips existing chunks, and
the per-launch time budget makes each job launch resume where the last
ran out — the job also re-captures the camera each launch, so rotating
the camera re-aims the priority cone within ~one budget period.
Combined with a raised `generation_time_budget_milliseconds` during the
initial build (GG-side), the visible terrain on TESTPRO1 completes ~2-3
seconds after the editor appears (down from 30+ at stock settings).
Default false = zero behaviour change for stock scenes.

### 1.6 ObjectComponent: per-object opt-out from GPU occlusion queries

**Files:**
- `WickedEngine/wiScene_Components.h` (`OCCLUSION_QUERY_DISABLED = 1 << 11` flag + `SetOcclusionQueryDisabled` / `IsOcclusionQueryDisabled`)
- `WickedEngine/wiRenderer.cpp` (frustum-visibility job: flagged objects get `occlusionHistory |= 1` each frame instead of a query allocation)

**Date:** 2026-07-18

#### Use case in GameGuru MAX

The 20K-slot tree pool made per-object occlusion queries a net loss:
~2.5ms CPU + ~1.3ms GPU per frame of query bookkeeping + proxy-box
rendering on TESTPRO1, while foliage occludes almost nothing. Globally
disabling occlusion culling would take the feature away from regular
entities (and the user-facing `t.visuals.bOcclusionCulling` setting),
so instead pool objects carry the new flag and are simply always
treated as visible. Measured on TESTPRO1: "Occlusion Culling" +
"Occlusion Culling Render" GPU ranges 1.96 + 1.94ms → 0.08 + 0.08ms;
part of the 24 → 60 FPS editor perf push.

The `occlusionHistory |= 1` write is required: history shifts left every
frame, so a flagged object that never allocates a query would otherwise
decay to "occluded" after 32 frames and vanish whenever occlusion
culling is enabled.

### 1.5 HairParticleSystem: per-strand slope + altitude filters for grass entities

**Files:**
- `WickedEngine/shaders/hairparticle_simulateCS.hlsl` (two extra branches inside the existing `xHairGrassType != 0u` block)
- `WickedEngine/shaders/ShaderInterop_HairParticle.h` (5 new floats + 3 padding on the CB for the altitude cutoffs)
- `WickedEngine/wiHairParticle.h` (5 mirror fields on `HairParticleSystem`)
- `WickedEngine/wiHairParticle.cpp` (copy mirror fields into the CB alongside the Option B fields)

**Date:** 2026-07-10

#### Use case in GameGuru MAX

DX11 filtered grass instances by terrain normal — `GGGrass_UpdateInstances`
skipped instances whose sampled `ny < 0.7` (≈45°), so no grass grew on
cliff faces or steep mountain flanks. In Wicked mode `UpdateInstances`
is gated off (perf), and reproducing the same filter on the CPU turns
out to be structurally hopeless:

- The DX11 normal map is stored at per-chunk texel resolution (hundreds
  of world units per texel), so a per-cell scan of the grass map (~4.8
  units per cell on a typical island level) reads bilinearly-averaged
  false-flat values along cliff edges.
- Computing a local slope from three `GGTerrain_GetHeight` samples per
  cell fixes the resolution mismatch but doesn't fix the placement
  mismatch: a strand belonging to a *flat* paint cell can still land on
  the adjacent *cliff* triangle via the hair system's random-barycentric
  distribution, so the "grass right on the cliff edge" case survives.

#### Mechanism

The correct filter is per-strand, using the exact face normal of the
triangle the strand sits on. Fix 1.2 already computes that face normal
in-shader (from three vertex positions, replacing the stored per-vertex
normal that was invalid at `chunk_scale > 1`). The value is available
in `target` at the point where the paint mask check runs, so the slope
filter is a single extra branch inside the existing `xHairGrassType != 0u`
block:

```hlsl
if (target.y < (half)0.7)
{
    strand_length = 0;
}
```

Cliffs go strand-by-strand grass-free at exact triangle granularity.
Gentle slopes keep their grass. No paint cells are mutated, so a later
sculpt that flattens a cliff face doesn't require repainting.

#### Companion: per-strand altitude filter (added same day)

Same design, same gate — an altitude band drives the visibility of each
strand based on its world Y (`base.y`, which for terrain-mounted grass IS
the terrain height at that XZ). Above-water and underwater use disjoint
`[min, max]` pairs, selected by a comparison against a water plane. The
CB row layout:

```hlsl
float xHairGrassWaterHeight;
float xHairGrassMinHeight;
float xHairGrassMaxHeight;
float xHairGrassMinHeightUnderwater;
float xHairGrassMaxHeightUnderwater;
```

Defaults span the full range on the C++ side (`grass_min_height = -1e30f`,
`grass_max_height = 1e30f`, likewise for the underwater pair) so callers
who don't set them get "no filter" — zero behavioral change out of the
box. The shader branch sits inside the existing `xHairGrassType != 0u`
gate so upstream Wicked hair (`grass_type == 0`) still bypasses it.

#### Why this is genuinely useful upstream

The check runs only when `xHairGrassType != 0u` — so upstream callers
that don't opt into the grass-type feature see zero behavior change.
Callers that DO use the grass-type hook (Option B / entry 1.4) get an
extra "filter strands by triangle slope" tool for free. Handy for any
system that wants to place vegetation on terrain-shaped meshes without
manual cliff masking. The threshold is currently hardcoded at 0.7 to
match DX11; a runtime constant would be a small extension if configurability
is needed later.

### 1.4 HairParticleSystem: per-strand visibility from an external paint mask (Stage 3 Option B)

**Files:**
- `WickedEngine/shaders/ShaderInterop_HairParticle.h` (CB field additions)
- `WickedEngine/shaders/hairparticle_simulateCS.hlsl` (SRV declaration + sample)
- `WickedEngine/wiHairParticle.h` (public field additions)
- `WickedEngine/wiHairParticle.cpp` (placeholder texture + CB write + SRV bind)

**Date:** 2026-06-19

#### Use case in GameGuru MAX

Grass is placed per-chunk over a `wi::HairParticleSystem` whose strand
positions are derived from random barycentric coordinates on the chunk
mesh's triangulation. With paint-mask gating only at the vertex level
(via `vertex_lengths`), every triangle that *contains any painted
vertex* gets strands distributed across its FULL surface — producing
a roughly 4×-brush-wide footprint when the brush is small (~30 in) but
the vertex grid is coarse (~80 in spacing at `chunk_scale = 80`).

To match the DX11 grass behaviour where a brush blob renders blades
ONLY inside the painted footprint, each strand needs to check the
paint mask at *its own* world XZ, not just at the triangle vertices.

#### Mechanism

The upstream `HairParticleCB` is extended with one 16-byte row carrying
the world-space-to-UV transform for an external paint mask plus a
"which type does this hair entity represent" tag:

```hlsl
uint  xHairGrassType;            // 1..N = active; 0 = disabled (upstream behavior)
float xHairGrassMapInvWorldSize; // 1.0 / world extent (matches GG_GetGrassMap CPU formula)
float xHairGrassMapOriginX;      // world XZ of map center (0 for centered maps)
float xHairGrassMapOriginZ;
```

A new SRV slot (`Texture2D<float> texHairGrassMap : register(t4)`) is
declared in the simulate CS. `HairParticleSystem` gains mirroring
public fields (`grass_type`, `grass_map_inv_world_size`,
`grass_map_origin_x/z`, `grass_visibility_texture`). When the caller
sets `grass_type` to a non-zero value, the simulate CS samples the
mask at the strand's world XZ and zeros `strand_length` if the cell
encodes a different type. A 1×1 zero-init placeholder bound by
default keeps DX12 validation happy for non-GG hair entities, which
leave `grass_type` at 0 and never reach the sample branch.

#### Why this is genuinely useful upstream

Any caller that wants per-strand visibility against an external mask
gets it for free with a CB write + one SRV bind. The default of
`grass_type = 0` makes the feature a pure no-op for existing usage —
no shader branch is taken, no perf cost. The R8_UNORM byte decode in
the shader is GG-specific (`flattened | type-with-+2-offset`), but
the *mechanism* — passing a typed paint mask plus a world-XZ-to-UV
transform into the simulate CS — generalises directly to other use
cases (e.g. lawn mowing, footprint trails, multi-zone variation).

### 1.3 HairParticleSystem has no targeted vertex_lengths update — `CreateRenderData` is destructive

**Files:** `WickedEngine/wiHairParticle.h`, `WickedEngine/wiHairParticle.cpp`
**Date diagnosed:** 2026-06-19

#### Symptom in GameGuru MAX

When the player paints grass, each affected chunk's hair entity needs
the per-vertex paint mask refreshed on the GPU. Wicked's only existing
upload path is `CreateRenderData()`, which calls `DeleteRenderData()`
first — destroying `generalBuffer` (the suballocated GPU memory that
hosts `simulation_view`, `vb_pos[0/1]`, `vb_nor`, …) and setting
`regenerate_frame = true`. The next simulate-CS dispatch sees garbage
`prevTail` / `currentTail` plus the regenerate flag and snaps every
strand's animated tip to the rest position for one frame. Result: a
visible "settling pop" on the wind animation every time we update
paint mask, even when nothing about strand count, mesh, or index list
has changed.

#### Fix (this entry)

Add a public method `HairParticleSystem::UpdateVertexLengthsBuffer()`
that recreates **only** the `vertexBuffer_length` GPU buffer from the
current `vertex_lengths` data. It leaves every other resource alone:
`generalBuffer`, `simulation_view`, `vb_pos[0/1]`, `vb_nor`, `vb_uvs`,
`wetmap`, `ib_culled`, `prim_view`, `indirect_view`,
`vb_pos_raytracing`, `indexBuffer`, `BLAS`, `regenerate_frame`,
`_flags`. The simulate CS rebinds `vertexBuffer_length` per dispatch
(`wiHairParticle.cpp` ~line 560), so the next frame's dispatch picks
up the new buffer automatically and per-strand simulation state stays
alive across the update.

Falls back to a full `CreateRenderData()` call if `generalBuffer`
isn't valid yet (caller skipped initial setup), so misuse is safe.

This is genuinely useful upstream — any caller that needs to mutate
the paint mask on a live hair system gets free animation continuity.

### 1.2 HairParticleSystem reads stored vertex normals that don't match the mesh triangulation

**File:** `WickedEngine/shaders/hairparticle_simulateCS.hlsl`
**Lines:** ~81-82 (the `target = ...` block)
**Date diagnosed:** 2026-06-18

#### Symptom in GameGuru MAX

With Terrain `chunk_scale = 80`, grass blades placed on the chunk
appeared at chaotic orientations — many leaning at ~45-90° on what
should be mild slopes, with neighbouring blades pointing in different
directions despite the underlying terrain being smooth.

#### Root cause

The simulate compute shader reads the three stored vertex normals
(`nor0`, `nor1`, `nor2`) from the chunk mesh, barycentrically
interpolates them, applies the emitter transform adjoint and uses the
result as the blade's up-axis (`target`). The problem is that
`wiTerrain.cpp` writes each vertex's stored normal as the face normal
of a **fixed reference triangle** `(V, V+x, V+z)` — NOT as an average
of the face normals of the actual mesh triangles touching that vertex.

For the terrain mesh's first triangle in each quad
(`topLeft, lowerLeft, lowerRight`):
- `nor1` (lowerLeft) happens to match — Wicked's reference triangle at
  lowerLeft is `(lowerLeft, lowerRight, topLeft)`, which is the same
  three vertices.
- `nor0` (topLeft) is computed from the triangle
  `(topLeft, topRight, topLeft+1z)` — vertices entirely in adjacent
  quads, **not in the triangle being sampled**.
- `nor2` (lowerRight) is similar — reference triangle is in the
  right-hand neighbour quad.

For the second triangle (`topLeft, lowerRight, topRight`) all three
stored normals are for unrelated phantom triangles.

At `chunk_scale = 1` (Wicked default) the height delta over one V→V+1
hop is tiny, so every reference triangle in a small area has nearly
the same normal — the structural mismatch is invisible. At
`chunk_scale = 80` the same hop spans 80 world units, so the height
deltas (and resulting normals) of two different reference triangles
near the same point can differ by tens of degrees.

#### Fix (this entry)

In the simulate CS, replace `target` with the **actual face normal of
the triangle the blade sits on**, computed in-shader from the three
vertex positions (`pos0`, `pos1`, `pos2`) after the same
`xHairBaseMeshUnormRemap.GetMatrix()` step that `position` goes
through:

```hlsl
float3 P0 = mul(xHairBaseMeshUnormRemap.GetMatrix(), float4(pos0, 1)).xyz;
float3 P1 = mul(xHairBaseMeshUnormRemap.GetMatrix(), float4(pos1, 1)).xyz;
float3 P2 = mul(xHairBaseMeshUnormRemap.GetMatrix(), float4(pos2, 1)).xyz;
target = (half3)normalize(cross(P2 - P0, P1 - P0));
```

The operand order matters — the terrain index winding makes
`cross(P1 - P0, P2 - P0)` point DOWN, so the cross product operands
are swapped to give +Y on flat ground.

This is a **GameGuru-side workaround**. Only the grass simulate path
is affected. The structural Wicked issue (stored vertex normals not
matching the mesh triangulation) still affects terrain shading and
slope-based material weighting — those would benefit from a proper
upstream fix that averages face normals across all triangles touching
each vertex.

### 1.1 Terrain chunk per-vertex normals ignore `chunk_scale`

**File:** `WickedEngine/wiTerrain.cpp`
**Lines:** ~1100-1107 (inside the second `wi::jobsystem::Dispatch` lambda
that fills `mesh.vertex_normals`)
**Date diagnosed:** 2026-06-18

#### Symptom in GameGuru MAX

When `Terrain::chunk_scale` is set above 1 (we use `chunk_scale = 80.0f`
in `GGTerrainWicked.cpp` to reduce chunk popping), the per-vertex normals
written into the terrain chunk meshes become almost horizontal. Anything
that reads those normals sees a near-vertical-cliff slope wherever the
ground is mildly sloped:

- `Terrain::HairParticleSystem`-based grass renders blades laid on their
  side rather than upright (this is how we first noticed).
- `Terrain` slope-based auto-material weighting (`slope_amount = 1.0 -
  saturate(normal.y)` at the same callsite) flips into "slope" material
  for almost the entire chunk.
- Object/terrain lighting uses these normals, so terrain shading also
  reads as if every triangle were a vertical wall.

The visible terrain *geometry* is correct because
`mesh.vertex_positions` (set further down at line ~1133) uses the
`chunk_scale`-aware local x,z. Only the normals are wrong.

#### Root cause

```cpp
const float x = (float(coord.x) - chunk_half_width) * chunk_scale;
const float z = (float(coord.y) - chunk_half_width) * chunk_scale;
const float height = heights_padded[coord.x][coord.y];
const XMVECTOR corners[3] = {
    XMVectorSet(chunk_data.position.x + x,     height,                                       chunk_data.position.z + z,     0),
    XMVectorSet(chunk_data.position.x + x + 1, heights_padded[coord.x + 1][coord.y],        chunk_data.position.z + z,     0),
    XMVectorSet(chunk_data.position.x + x,     heights_padded[coord.x][coord.y + 1],        chunk_data.position.z + z + 1, 0),
};
const XMVECTOR T = XMVectorSubtract(corners[1], corners[2]);
const XMVECTOR B = XMVectorSubtract(corners[0], corners[1]);
const XMVECTOR N = XMVector3Normalize(XMVector3Cross(T, B));
```

The horizontal step between `corners[0]` and `corners[1]` (and between
`corners[0]` and `corners[2]`) is a literal `+ 1`, but the height
sampled is the **real world height** at the next vertex, which sits
`chunk_scale` world units away. The cross product therefore sees the
height delta over a run of `1` instead of `chunk_scale` — slopes get
amplified `chunk_scale`-fold.

With `chunk_scale = 80`, a real 5° slope (height rise ~7 units over an
80-unit run) is interpreted as `atan(7/1) ≈ 82°`. Hence near-horizontal
normals everywhere.

When `chunk_scale = 1` (engine default in `wiTerrain.h:327`) the formula
happens to be correct, which is why this bug has not been noticed before.

#### Fix

Replace the two literal `+ 1` offsets with `+ chunk_scale` so that the
horizontal steps match the actual vertex spacing:

```cpp
XMVectorSet(chunk_data.position.x + x + chunk_scale, heights_padded[coord.x + 1][coord.y], chunk_data.position.z + z,              0),
XMVectorSet(chunk_data.position.x + x,               heights_padded[coord.x][coord.y + 1], chunk_data.position.z + z + chunk_scale, 0),
```

`chunk_scale` is already captured in the dispatch lambda via `[&]` and
is used immediately above at the same indentation (lines 1097, 1098).

This change keeps the orientation of the cross product unchanged (only
the y-component grows from `1` to `chunk_scale^2` after the cross, and
normalization restores the magnitude). The fix has zero effect when
`chunk_scale = 1`, so it does not regress the default Wicked terrain.

---

### 2.18 (2026-08-10) — ECS entity->index lookup: BUCKET_HASH -> SPARSE  ★ −27% of Scene::Update

**File:** `WickedEngine/wiECS.h:445` (one #define; the implementation at `:486-593` is stock
upstream code that was already present and unused).

**What and why.** This is a **DX11-parity restoration, not a new optimisation.** The DX11 fork
replaced stock Wicked's hash lookup with a flat sparse array years ago
(`WickedRepo/WickedEngine/wiECS.h:413`, commit `b40f00a`, "way faster for larger games ... no
more lookup.find(entity)") and the DX12 port silently reverted to upstream stock. Every
`Contains()` / `GetComponent()` / `GetIndex()` in every `Scene::Update` system pays the
difference, on every entity, every frame.

Chose `LOOKUP_SPARSE` over `LOOKUP_STRAIGHT` (the literal DX11 equivalent) because STRAIGHT
allocates one `size_t` per entity id ever seen in EVERY component manager, and entity ids come
from a monotonic global counter — a long editing session with create/destroy churn would grow
every table forever. SPARSE keeps the direct mapping but pages it in 64-entry blocks that are
freed when they empty.

**Measured** (Switch Escape, editor, profiler on in both arms, `Scene-S1..S5` ms sum):
`Scene::Update` **2.412 -> 1.750 ms (−0.662, −27.4%)**; CPU Frame 4.49 -> 3.93.
POLYS bit-identical (109358). FPS unchanged (~142) because that frame is GPU-fence-bound —
judge this by the ms column, not FPS. Full method + tables: `SWITCHESCAPE_PERF.md` §11.

**Audited before enabling** (this path shipped upstream but was never exercised here):
`BlockData::Item` defaults to `INVALID_INDEX` and `wi::allocator::BlockAllocator::allocate()`
placement-news `T()` (value-initialises), so `get()` on an un-inserted slot in a partially
filled block returns INVALID_INDEX, not garbage; `erase()` dereferences `block_data` without a
null test but its only callers (`Remove` :303, `Remove_KeepSorted` :326) first check
`GetIndex() != INVALID_INDEX`, so the block provably exists.

⚠ **BUILD-SIDE COST (game repo, not engine).** The extra per-ComponentManager `BlockAllocator`
instantiations pushed `DarkLUA.cpp` — a 9-part unity TU that also includes the Wicked headers —
past the COFF section limit (`C1128`), and enlarged Camera's browse-info database past BSCMAKE's
`BK1520` limit. Fixed game-side with `/bigobj` on DarkLUA and Release-only browse info off on
Camera; both are build-flag changes with no codegen effect. **If another TU hits C1128 later,
add `/bigobj` — do not revert this.**

**Revert:** re-comment `LOOKUP_SPARSE`, uncomment `LOOKUP_BUCKET_HASH`, rebuild the engine. Or
`git checkout baseline-sceneupdate-20260810`.

### 2.18b — opt-in parallel instance-array blank pass (`SET_INSTINIT`, DEFAULT OFF)

**File:** `WickedEngine/wiScene.cpp` (flag `gg_instinit_parallel`, use site in `Scene::Update`).
Stock Wicked blanks every instance slot from a single `jobsystem::Execute` — 256 B x
instanceArraySize of write-combined stores on ONE worker, ~1.87 MB/frame on Switch Escape. The
opt-in path Dispatches the identical writes across the pool.
**MEASURED: a wash (+0.015 ms, +0.9%).** It runs alongside the parallel Animation/Physics/
Transform dispatches and is not the critical path. Kept default-OFF as a documented knob rather
than deleted, since it should matter where S1's dispatches are cheap relative to
instanceArraySize. ⚠ Do not re-raise it on Switch Escape without re-measuring.

---

## 2. Temporary debug overrides (MUST be reverted before any upstream brief)

These are diagnostic edits made during the grass-rendering analysis on
2026-06-18. They live in the same WickedEngineDX12 clone but should
never reach upstream. Backups of the original compiled shaders sit
alongside the live ones in
`D:\DEV\BUILD\GameGuru Wicked MAX Build Area\Max\shaders\` with a
`.bak` suffix.

### 2.1 `WickedEngine/shaders/hairparticlePS.hlsl`

Force grass pixels to opaque white at the end of `main()`. Used to
confirm the shader-edit → DXC-auto-recompile loop is intact.

Revert by restoring the original `return color;` and deleting the
debug comment block.

All debug overrides have been reverted as of 2026-06-18 18:28 (UK
local). See `git status` of `D:\max\WickedEngineDX12` — only
`wiTerrain.cpp` (#1.1) and `hairparticle_simulateCS.hlsl` (#1.2) are
modified, both genuine bug fixes.

---

## 3. Status

| Item | Status | Action |
|---|---|---|
| 1.1 Terrain chunk normal fix | applied 2026-06-18 in Wicked commit `6068a1bb`, lib rebuilt | candidate for upstream PR |
| 1.2 HairParticleSystem face-normal override | applied 2026-06-18 in Wicked commit `5329fc8b`, shader auto-recompiles | candidate for upstream PR; long-term Wicked fix is to average vertex normals properly |
| 1.3 HairParticleSystem UpdateVertexLengthsBuffer | applied 2026-06-19, lib rebuilt | candidate for upstream PR — pure addition, no behavioural change for existing callers |
| 1.4 HairParticleSystem external paint mask (Option B) | applied 2026-06-19, lib + shaders rebuilt | candidate for upstream PR — `grass_type == 0` default means zero behavior change for existing callers; adds general-purpose per-strand visibility hook |
| 1.5 HairParticleSystem per-strand slope + altitude filters | applied 2026-07-10, lib + shaders rebuilt | candidate for upstream PR — both gated on `xHairGrassType != 0u`, zero effect on non-GG hair; slope reuses the face-normal fix 1.2 already computes, altitude adds 5 CB floats with permissive defaults |
| 1.6 ObjectComponent occlusion-query opt-out flag | applied 2026-07-18, lib rebuilt | candidate for upstream PR — flag defaults off, zero behaviour change for existing scenes; new flag consumers must set `occlusionHistory |= 1` semantics (already handled in the visibility job) |
| 1.7 Terrain view-cone chunk generation priority | applied 2026-07-18, lib rebuilt | candidate for upstream PR — flag defaults off; pure generation-ORDER change, the generated content is identical |
| 1.8 Terrain high-priority generation jobs | applied 2026-07-18, lib rebuilt | candidate for upstream PR — flag defaults off; burst-scenario knob, GG enables it only while the view cone is incomplete |
| 1.9 Animation/transform hardening (unit-quaternion guards + decompose validation) | applied 2026-07-18 in Wicked commit `a4539a76`, lib rebuilt | candidate for upstream PR — guards only fire on already-corrupt data (zero behaviour change for healthy scenes); tripwire files `anim_garbage.txt`/`applytransform_garbage.txt` appear next to the exe only if a guard fires |
| 1.10 Ocean foam world-unit scale + intensity knob | applied 2026-07-18 in Wicked commit `da60bfad`, lib rebuilt, shader auto-recompiles | candidate for upstream PR — both params default 1 = stock behaviour; fills existing CB padding so no layout change |
| 1.11 Delayed shadow cascades (staggered refresh) | applied 2026-07-18 in Wicked commit `38a9e82a`, lib rebuilt, new shadowClearPS shader auto-compiles | candidate for upstream PR — default OFF preserves stock behaviour bit-for-bit; GG enables at sun creation; single-directional-light assumption documented in code |
| 1.12 Terrain ChunkData::merge_pending stale-mesh flag | applied 2026-07-18, lib rebuilt; **hotfix `df3c10e0` same day: flag only on chunks actually (re)generated** — the spiral visits every chunk every job run and the original unconditional set made GG's blend passes skip the whole map forever (Island Showdown grey terrain) | candidate for upstream PR — passive field, closes the regen-vs-merge window for mesh-baking consumers (GG blendmap passes) |
| 1.13 VirtualTexture::pending_repaint_blendmap instant refresh | applied 2026-07-18, lib rebuilt | candidate for upstream PR — passive flag; paint strokes visible next frame instead of after seconds of feedback re-streaming |
| 1.14 Optional generation restart on dirty materials | applied 2026-07-19 (`cbce724d`), lib rebuilt | candidate for upstream PR — default true = stock; GG disables (runtime material registration must not rebuild the island) |
| 1.15 Preserve blendmap + VT residency on in-place regen | applied 2026-07-19, lib rebuilt | GG-specific (default false = stock) — sculpt-drag chunks keep GG blendmaps and resident tiles; fresh merged material re-bound without vt.init() |
| 1.16 VT repaint flag main-thread latch | applied 2026-07-19, lib rebuilt | race fix for 1.13's flag (found by multi-agent review of 1.15) — async job consumes a main-thread-latched copy, never the live flag |
| 1.17 gg_generate_blendmap born-correct chunks | applied 2026-07-19, lib rebuilt | GG-specific (null by default) — generator thread fills GG auto+painted weights before the region texture is built; kills the green default-blend flicker on fast camera zooms |
| 1.18 VT residency upgrade hysteresis | applied 2026-07-19, lib rebuilt | GG-specific (default false = stock) — residency upgrades wait for the camera to settle (10 stable frames), then 4/frame; kills the square sharpness-flicker while zooming |
| 1.19 VT stale tile-identity release + full freeze-while-moving | applied 2026-07-19, lib rebuilt | part 1 = stock bug, CANDIDATE FOR UPSTREAM PR (dangling last_used pointer → foreign-content squares); part 2 extends 1.18's freeze to downgrades |
| 1.20 VT tile allocator freeze while camera in motion | applied 2026-07-19 + same-day hotfix (aging always runs, tail-invalid self-heal) | 60-frame recycle threshold mid-motion — visible tiles can never be stolen by the miss flood |
| 1.21 VT expanded working set (island-wide full-res zone) | applied 2026-07-19, lib rebuilt | GG-specific (defaults = stock) — gg_near_ring_dist 6 + gg_removal_margin 12: camera travel over the island never crosses a VT resolution boundary; chunks survive zoom travel |
| 1.22 Ocean caustic size decoupled from patch size | applied 2026-07-20, lib rebuilt | GG-specific (default 1.0 = stock) — new ShaderOcean.caustic_scale scales ONLY the seabed caustic lookup; Water-panel "Caustic Size" slider tunes ripple size without touching the waves |
| 1.23 Ocean underwater fog colour+density decoupled from surface | applied 2026-07-21, lib rebuilt | GG-specific (default density 0 = stock fallback) — new ShaderOcean.underwater_color + underwater_fog_density; Water-panel "Underwater Color" + "Underwater Fog" control the submerged view independently, so the surface can stay transparent when the water colour changes |
| 1.24 Ocean water base colour tints transparent water (depth) | applied 2026-07-21, lib rebuilt | GG-specific (default 0 = stock) — new OceanCB.xOceanWaterColorDepth; oceanSurfacePS lerps refraction toward the base colour by depth so "Water Base Color" is visible on WaterAlpha-0 water (GG sets 0.005). Fixes "set red, sea didn't change from above" |
| 1.25 Hair particles skip billboard-write + physics for culled strands | applied 2026-07-22 in Wicked commit `3273b651`, lib rebuilt, shader auto-recompiles | candidate for upstream PR — drawn (visible) strands byte-identical; a culled strand's sim freezes until it re-enters view; saves proportional to off-screen strand fraction |
| 1.28 Terrain SVT mip bias −2.0 → 0.0 (mid/long swim) | applied 2026-07-24, lib rebuilt | GG value-change on stock Wicked `SVT_MIP_BIAS` (ShaderInterop_Renderer.h) — stock −2.0 biases SVT terrain 2 mips SHARPER than correct → mid/long pixel-swim; 0.0 = Nyquist-correct, swim gone, near field stays crisp. This is the REAL terrain-LOD lever (SVT fetches via SampleLevel, so the sampler's aniso/mip-bias are ignored). Also reverts 1.27. Shared header → recompiles ~375 engine shaders |
| 1.29 Animation: wire the 30fps throttle to the "Lower Animation & LUA Speed" checkbox + distance gate | applied 2026-07-24, lib rebuilt (wiScene.cpp only) | The every-other-frame animation-skip in `Scene::RunAnimationUpdateSystem` (wiScene.cpp) was gated on a hardcoded lambda-`static bEnable30FpsAnimations=0` — unwired ever since the game-side GGAnimBridge was removed as too slow (89873913). Added namespace globals `gg_anim30fps_enabled/frame/dist2`, driven per-frame by the game (`MasterRenderer::Update`) from the editor checkbox BEFORE `__super::Update`. (b) Fixed the skip parity to a real per-frame counter (the old `iAnimFrames` was a racy per-job lambda-static → non-deterministic skip). (c) Added a per-object distance gate (`gg_anim30fps_dist2`, cmp `aabb_objects[oi].getCenter()` vs `GetCamera().Eye`) so near-camera armatures stay full-rate; default farDist 2000 (game `g_animThrottleFarDist`, clamped ≤60000 for the uint32 dist² guard). Timer still advances on skipped frames (true 30fps, not slow-motion); keeps the `!updateonce` guard + unit-quaternion guards → parrot-safe (no tripwire). Measured: throttle-all halves anim sampling 0.61→0.20ms; distance gate modulates by distance (crowd all <~4000 units on TESTPRO1). Harness `SET_ANIM30FPS <0|1> [farDist]`. Absolute distance is scene-scale-dependent → wants a UI slider / apparent-size gate follow-up |
| 1.27 Terrain SVT anisotropy ×4 → ×8 (grazing swim) | **REVERTED 2026-07-24 (no-op), superseded by 1.28** — was Wicked commit `3a6fc38c` | The SVT terrain samples via `SampleLevel` (explicit LOD), which ignores the sampler's anisotropy — so the ×4→×8 bump had NO visible effect. Reverted to stock ×4. The real swim fix is the mip bias (1.28). Kept in history as a lesson: confirm the sampling path (Sample vs SampleLevel) before touching sampler state on SVT |
| 1.26 Ocean env-map reflection fallback when planar reflections off | applied 2026-07-24 in Wicked commit `bf09e448`, lib rebuilt | CANDIDATE FOR UPSTREAM PR — fixes garbage-water when reflections are toggled off (unguarded `texture_reflection_index` left dangling at a stale `rtReflection_resolved`); guard matches the planar-render gate so reflections-ON is byte-identical, OFF uses the existing `EnvironmentReflection_Global` path and skips the planar pass |
| 1.30 Apparent-size object cull in UpdateVisibility | applied 2026-07-24 in Wicked commit `8ad10e54`, lib rebuilt | GG-specific (default `gg_apparent_cull_bits`=0 = disabled = stock) — new atomic threshold in the main-view object cull loop (`wiRenderer::UpdateVisibility`): an object that passes the frustum but whose world AABB `getRadius()/distance` < tangent is dropped from `visibleObjects` (renders as a distant speck). World AABB radius bakes in original size × scale, /distance = on-screen apparent size → scale-invariant. Squared-tangent precomputed per view (no per-object sqrt); `distSq > radiusSq` guard keeps near/inside objects. Affects the 3 UpdateVisibility callers (main + planar-reflection + render-to-texture cam) — each culls specks by its own camera; shadow cascades cull separately so a culled speck can still cast its sub-pixel shadow. Driven per-frame by game `MasterRenderer::Update` from the editor "Apparent Size" slider (`maxApparentSize` → tangent via `g_apparentCullK`=59; default slider = tangent 0 = no cull; slider 1.0 → tangent ~0.0054). Harness `SET_APPARENTSIZE <tangent>` / `SET_APPARENTSIZE slider <fASize>`. CANDIDATE FOR UPSTREAM PR — a generic projected-size cull, flag-gated off by default |
| 1.31 Configurable delayed-shadow cascade interval ("Laptop" mode) | applied 2026-07-25 in Wicked commit `de50638b`, lib rebuilt | GG-specific (default 2 = stock delta-1.11 behaviour). The far directional cascades (1..N) refreshed every other frame (hardcoded `frame%2`); made the period a runtime knob `delayedShadowCascadeInterval` (setter `SetDelayedShadowCascadeInterval`, clamped >=2) so the game can request an every-4th-frame "Laptop" cadence. Cascades 1..N still refresh TOGETHER on the same frame → the cascade-blend SYNC that cured the "two terrain shapes" flicker (see [[project-shadow-flicker]]) holds at any interval; cascade 0 stays every-frame and the >64" camera-move force-refresh is unchanged. Driven per-frame by game `MasterRenderer::Update` from `g_bDelayedShadowsLaptop` (2 or 4). Verified: interval-4 shadows are statistically identical to interval-2 at a static camera (cross-interval diff below the scene's animation noise floor). Harness `SET_LAPTOP_SHADOWS <0|1>` |
| 1.32 Frame-cost CPU instrumentation (Scene stages, RP3D recording jobs, VT job) | applied 2026-07-25, lib rebuilt (wiScene.cpp, wiRenderPath3D.cpp, wiTerrain.cpp) | Instrumentation only — `wi::profiler` CPU ranges, free when the profiler is off. Scene::Update partitioned at its jobsystem::Wait barriers (`Scene-S1 Anim+Transform` … `Scene-S5 Tail+ShaderScene`); RenderPath3D PreRender (`RP3D-VisMain/VisRefl/PerFrameData`) + Render serial/wait spans + a range inside every command-list recording job (`RP3D-rec *`); terrain VT job internals (`VT-job Total/FreeSort/PageBuf`, `VT-WaitInRecord`, `VT-record`). THIS FOUND THE FRAME POLE: the months-old "fixed ~9.5ms Render CPU" was `RP3D-rec PrepareAsync` blocking on the terrain VT background job — **16.4ms EVERY frame on one worker thread** (14.2ms uncached page-table memcpy loop + 2.1ms free-list rebuild+sort) |
| 1.33 Incremental terrain-VT bookkeeping (16.4ms/frame → 0.4ms) | applied 2026-07-25, lib rebuilt (wiTerrain.h/.cpp) | GG-specific, master switch `wi::terrain::gg_vt_incremental` (false = stock). A chunk-VT's page table only changes when a tile is allocated INTO it or stolen FROM it: `PhysicalTile::gg_owner` tracks the owning VT; `allocate_tile` reports the steal VICTIM so both winner and victim are marked `gg_page_dirty`. Clean VTs skip (a) the huge uncached upload-buffer write loop, (b) the GPU `pageBuffer` CopyResource, (c) the residency-map recompute dispatch — each pending flag has exactly one consumer on its own command list. A rotating heartbeat force-refreshes ONE resident VT per frame as insurance. Free-list rebuild+sort (2.1ms) is now lazy: only on frames with allocation requests / after consumption (`gg_free_dirty`) / rate-limited low-water refill. VT::free() clears `gg_owner` (no dangling deref). MEASURED: VT-job 16.39→0.41ms, CPU "Render" 9.72→1.71ms, TESTPRO1 gate **52 → 62-70 FPS**; live A/B via harness `SET_VTINC 0|1` (stock 50-52 / incremental 59-70). Stress-verified: 3× violent teleport cycles → no foreign squares, terrain streams sharp; sculpt+paint+undo clean. The frozen-mode main-thread filter and tile AGING cadence are untouched (VT do-not rules respected) |
| 1.34 Remove dead entity-set inserts in ScanAnimationDependencies | applied 2026-07-25, lib rebuilt (wiScene.cpp) | The animation-dependency check (sole READER of `AnimationQueue::entities`) was already commented out by the earlier "assume no dependencies" GG delta — but the writer survived: `queue.entities.clear()` + per-channel hash-set inserts for every playing animation ≈ 10-25k write-only ops/frame at 122 anims (~0.8ms of the "Animation Dependencies" range). Deleted the writes; restore together with the dependency block if it ever returns |
| 1.35 Frustum-visibility animation pause | applied 2026-07-25, lib rebuilt (wiScene_Components.h, wiRenderer.cpp, wiScene.cpp) | GG-specific, off when `gg_anim_vis_pause_frames`=0. `ObjectComponent::gg_last_visible_frame` stamped in the main-view (and reflection) `UpdateVisibility` cull; `RunAnimationUpdateSystem` pauses EVALUATION of animations whose target object hasn't passed the cull for >N frames AND is beyond the near guard (`gg_anim_vis_pause_neardist2`, protects just-off-frame characters with on-screen shadows). Timers keep advancing (exact same semantics as the long-shipped `IsRenderable` cull just above it) → characters resume in perfect sync on re-entry. Driven per-frame by `MasterRenderer::Update` from game globals `g_animVisPauseFrames`=3 / `g_animVisPauseNearDist`=2000. Known accepted trade: a fully off-screen character's cast shadow freezes while unseen. Harness `SET_ANIMVIS <frames> [neardist]` |
| 1.36 Subtree-parallel hierarchy update (O(N×depth) → O(N)) | applied 2026-07-25, lib rebuilt (wiScene.h/.cpp) | GG-specific, master switch `wi::scene::gg_hierarchy_levelorder` (false = stock chain walk). Stock `RunHierarchyUpdateSystem` rebuilds the ENTIRE ancestor chain per hierarchy entry (`GetLocalMatrix` + multiply per level ≈ millions of ops at 18k bone entries × depth 8-15). Now the topdown workload also collects the subtree ROOTS (entries whose parent is INVALID or not itself an entry); the update dispatches ONE JOB PER ROOT and each job walks its own subtree depth-first via `topdown_hierarchy`, CARRYING the accumulated world matrix + ancestor layer mask down the chain — one local-matrix build + one multiply per entity, zero cross-job dependencies, zero barriers (a first level-order design with a jobsystem barrier per depth level was discarded: 30-60 bone-depth levels × Dispatch+Wait ate the win). Float note: different association order ⇒ last-ulp differences vs stock, visually identical. Layer propagation carried exactly per stock semantics incl. layer-less/transform-less mid-chain ancestors and no-hierarchy root parents; 100k-node cycle guard. Harness `SET_HIERLO 0|1` |
| 1.37(+b/c/d) Hair/grass simulation static-skip + wind cadence | **DISABLED BY DEFAULT 2026-07-25 (`gg_hair_sim_static_skip=false`) — a full manual knob bisect (6 knobs, fresh-launch rounds A-E) convicted this delta ALONE of the intermittent load/paint grass flicker. Fix attempts b (ping-pong parity — real bug, kept as dormant hardening), c (full-rate hysteresis), d (frozen-velocity vb_pre patch for TAA) reduced but did not eliminate it; the residual mechanism needs a PIX/RenderDoc frame capture. Re-enable via SET_HAIRSKIP for future investigation; costs ~1.4ms GPU parked (~78→~73 FPS on TESTPRO1).** Original delta: applied 2026-07-25 (+b `6d4ab66c`, +c `d7cea7f5`, +d), lib rebuilt (wiHairParticle.h/.cpp, wiRenderer.cpp, wiScene.cpp) | GG-specific, master switch `wi::renderer::gg_hair_sim_static_skip` (false = stock every-frame sim). The 2M-strand grass simulate dispatch (~1.9ms GPU on TESTPRO1) recomputes a near-identical result when the camera is parked (the sim's only view inputs are per-strand culling + camera-bend). Camera parked ≥4 frames AND every visible system settled (`gg_sim_runs ≥ 4` since creation / CreateRenderData regeneration / transform move): **no wind → skip the batch entirely; wind active (GG levels default to dir=(1,0,1) speed=1) → simulate every `gg_hair_sim_wind_interval`-th frame (default 4)** — sway continues in gentle slow-motion (the sim integrates per-dispatch dt). Any new/regenerated/moved system or camera change resumes full-rate instantly. **1.37b (the load/paint grass-flicker fix):** the vb_pos ping-pong swap is gated on `gg_sim_ran_last_frame` — swapping on frames the sim skipped made write/read parity depend on the GAP LENGTH between sims; irregular gaps (grass streaming, paint creating systems → intermittent forced full-rate) flipped parity per sim → position pops = flicker (camera movement 'cured' it by forcing every-frame sim). The ping-pong now only advances on sim frames, so every sim reads exactly its predecessor. **1.37c (`d7cea7f5`, residual transition flicker): forcing events (camera motion, any unsettled system) arm a 60-frame FULL-RATE cooldown — the cadence may only engage after everything has been calm that long, so load-streaming/painting windows run bit-for-bit stock instead of bouncing between full-rate and skipping (irregular sway hops + erratic motion vectors = the ~8s intermittent flicker).** Verified: every post-materialization load pair ≤1.3 mean (1.37b had 6.7-6.8 spikes; stock reference 1.5-3.1); cadence still engages (settled FPS ~68+ vs stock ~50). Harness `SET_HAIRSKIP <0|1> [windInterval]` |
| 1.38 RunObjectUpdateSystem: row-lengths instead of XMMatrixDecompose | applied 2026-07-25, lib rebuilt (wiScene.cpp) | Per-object per-frame `XMMatrixDecompose` whose output was ONLY used for the scale magnitude (`size = max(S.xyz)`); basis row lengths give the identical value without the quaternion extraction, and unlike the decompose they also work for negative-determinant matrices. 8722 decomposes/frame removed |
| 1.39 Skip underwater postprocess above the waterline | applied 2026-07-25, lib rebuilt (wiRenderPath3D.cpp) | The underwater full-screen pass (dispatch + ClearUAV + barriers, ~0.15ms GPU) ran whenever the OCEAN existed, even with the camera hundreds of meters above water — it only affects a SUBMERGED camera. Skip when `camera.Eye.y > waterHeight + 200` (margin covers wave displacement); chain-safe (postprocess chain treats a skipped stage as never existing). `wi::gg_skip_underwater_above_water` (false = stock) |
| 1.40 Command-list merges (submit-fragmentation cut) | applied 2026-07-25, lib rebuilt (wiRenderPath3D.cpp) | GG-specific, master switch `wi::gg_render_merge_lists` (false = stock structure). Each `BeginCommandList` is a real allocator+list and every cross-list wait splits the queue submission — ~17 lists / ~15 submits per frame leave GPU idle bubbles between submits (measured frame 12.6ms vs CPU 9.8 / GPU 9.8 busy). Merges: (a) the occlusion-culling pass records at the TAIL of the main prepass list (same depth target, same-list sequencing; the ocean's wait retargets to the prepass list); (b) transparents + postprocess chain record into ONE list, with `RenderCameraComponents` hoisted before it (camera render-to-texture output is consumed by materials next frame either way). Harness `SET_MERGELISTS 0|1` |
| 1.41 ShaderMaterial recompose cache | applied 2026-07-25, lib rebuilt (wiScene_Components.h, wiScene.cpp, wiResourceManager.h/.cpp) | GG-specific, master switch `wi::scene::gg_material_cache` (false = stock). `RunMaterialUpdateSystem` ran the FULL `WriteShaderMaterial` recompose (half-packs, sin/cos, ~16 per-slot `GetDescriptorIndex` lookups) for every material every frame (2225 on TESTPRO1). The composed struct only changes when the material is dirty or a texture-streaming event moves GPU descriptors. New `wi::resourcemanager::gg_streaming_descriptor_epoch` bumps on BOTH streaming mutation sites (texture-object replacement and min-lod subresource recreation) — the cache is valid only within an epoch, so streamed descriptor churn can never serve a stale index. Recompose triggers: dirty flag, epoch move, per-material staggered 64-frame heartbeat (insurance for direct field writes that skip SetDirty), or video/camera slot attachments; otherwise a 256-byte memcpy of the cached composition into the (per-frame cycled) mapped array — the mapped write itself always happens. Harness `SET_MATCACHE 0|1` |
| 1.42 SetDirty on terrain VT rebind | applied 2026-07-25 in Wicked commit `683fa37c`, lib rebuilt (wiTerrain.cpp) | The terrain virtual-texture (re)bind block writes descriptor indices / `texMulAdd` / `lod_clamp` straight onto the chunk `MaterialComponent` WITHOUT `SetDirty`. Harmless before 1.41 (every material recomposed every frame); with the 1.41 cache the STALE composition could be served for up to 63 frames after any rebind (load/reload storms, resolution-ring crossings at `gg_near_ring_dist`, `gg_tail_invalid` retries), pointing a chunk at a pooled residency/feedback map possibly already re-issued to ANOTHER chunk = grey / foreign chunk textures. One `material->SetDirty()` at the end of the bind block closes it. Found + adversarially verified by the terrain-corruption audit workflow |
| 1.43 `GraphicsDevice::FlushDeferredDestroys` | applied 2026-07-26 in Wicked commit `3af8655c` (wiGraphicsDevice.h, wiGraphicsDevice_DX12.h/.cpp) — **PRESENT BUT DISABLED AT THE CALL SITE, do NOT re-enable without fixing the frame-boundary problem** | Virtual (default no-op), DX12 impl = full-queue `WaitForGPU` + `allocationhandler->Update(FRAMECOUNT+1, 0)` = release the ENTIRE deferred-destroy backlog immediately (returns the drained count). Intended to give in-place level reloads a cold-load-clean device. **Result: it did NOT fix any corruption (~16.5k items drained per reload, artifacts unchanged) and is a SUSPECTED DEVICE-REMOVAL CAUSE** — the game calls it from `gridedit_load_map`, which runs mid-frame while the editor still pumps/renders, so freeing everything can destroy resources that recorded-but-unsubmitted command lists reference (user hit `DXGI_ERROR_DEVICE_HUNG` / `Device Lost on Present 0x887a0006`; harness repro then crashed 2/2 during load). Game-side gate `gg_enable_deferred_flush=false` (game `2ee384f9`). Kept in the engine as a correct-if-called-at-a-frame-boundary primitive |
| 1.44 Texture-streaming reload guard | applied 2026-07-26 in Wicked commit `3af8655c`, lib rebuilt (wiResourceManager.h/.cpp) | GG-specific. `wi::resourcemanager::GGReloadGuardBegin()` = pause streaming (`gg_streaming_paused`) + `Wait(streaming_ctx)` (join the in-flight streaming job) + DROP `streaming_texture_replacements` (they were computed against the DYING session's resources) + clear `streaming_texture_jobs`; `GGReloadGuardEnd()` resumes. Called by the game around an in-place level reload. **Why: the streaming system was proven to write ANOTHER FILE'S BYTES into live textures across reloads** — deterministic 'blue palms' (their own normal-map bytes showing as basecolor) at reload>=2, healed IN PLACE by `SetOutdated`+re-`Load` (harness `REUPLOAD_TEXTURE`), and eliminated entirely with streaming paused (harness `SET_STREAMING 0`). `gg_streaming_paused` doubles as that A/B knob. Full forensic chain in the game repo's memory notes |
| 1.45 Join the async VT job before terrain teardown | applied 2026-07-26 in Wicked commit `12a64a39`, lib rebuilt (wiTerrain.cpp) | **Fixes a real use-after-free that predates the P.5 perf push.** The terrain freed `VirtualTexture` objects while the async VT job could still be running — that job holds raw VT pointers (last frame's `virtual_textures_in_use`) and mutates the shared tile atlas, so `free()`+`erase` underneath it corrupts GPU sparse-resource bookkeeping. Now `wi::jobsystem::Wait(virtual_texture_ctx)` runs first at all three teardown sites: the per-frame chunk-removal branch (dominant during sustained camera travel — one-shot `gg_vt_teardown_joined` flag so only frames that actually remove pay, others keep full job overlap), `Generation_Restart` (level switch / material re-setup), and the `terrainEntity==INVALID` path. Matches the user repro: fly the editor camera at full speed far outside the playable area, leave without saving, load a different level -> missing chunk underfoot + far-field geometry corruption (+ one AMD `DEVICE_HUNG`); a clean relaunch of the same level always rendered fine. **Explains the long-standing 'occasional far-distance corruption'. NEEDS EXTENDED USER SOAK to confirm** |
| 1.46 GPU suballocator guards + op-log tripwire | applied 2026-07-26 (wiAllocator.h), lib rebuilt | **The mesh/hair GPU suballocator (one shared global buffer, every mesh a suballocated alias — wiRenderer `SuballocateGPUBuffer`) is the PRIME SUSPECT for the travel-churn geometry corruption**: one instrumented island load caught **596 overlapping grants + 172 out-of-bounds grants** (out-of-bounds = the `CreateAliasingResource` E_INVALIDARG → `DXGI_ERROR_INVALID_CALL` device-removal observed same day; overlap = two live meshes sharing bytes = the stretched-blade/missing-chunk stomps with all CPU records correct). INTERMITTENT: subsequent loads + 3 churn-reload cycles (27K ops) replayed perfectly clean, storm correlated with the shader-recompile-burdened first load (timing-sensitive race). Guards: (a) out-of-range grants rejected gracefully (caller falls back to standalone buffer instead of device removal); (b) live-range overlap tripwire + full A/D/F/R op log to `alloc_tripwire.txt` (replayer: scratchpad `replay_alloc.py`); (c) freed ranges held `gg_deferred_extra_hold=8` extra frames (mid-frame load pumps make the bare buffercount window unsafe); (d) `is_empty()` storageReport now under the allocator lock (was a racy read). Knobs: `wi::allocator::gg_alloc_tripwire`, `gg_deferred_extra_hold`. |
| 1.46b Allocator node/thread logging + Reset steal-guard | applied 2026-07-26 in Wicked commit `1b7709ae` (wiAllocator.h), lib rebuilt | **ORGANIC CAPTURE CONFIRMED THE ALLOCATOR (user session, plain island load): 1369 overlap + 411 OOB grants, op history shows the free-list bin SELF-LOOPING — offset 129 granted 20+× consecutively with no frees between = aftermath of a double-free double-inserting a node.** VERIFY_MESH on that session: 294/1012 terrain chunk buffers held FOREIGN bytes (ib containing float vertex data), healed in place, FPS 7.3→64.7. This delta: (a) op log lines gain `m=<nodeindex> t=<threadid>` so the next capture shows the node-level double-free + colliding threads directly (`replay_alloc.py` in GameGuru Core models nodes too); (b) `Allocation::Reset()` atomically steals `internal_state` (candidate mechanism: racing double-Reset on the same object double-decrements the refcount → frees someone else's node). |
| 1.47 **OffsetAllocator port bug — THE reload/travel-churn corruption ROOT CAUSE** | applied 2026-07-26 (Utility/offsetAllocator.cpp), lib rebuilt | **UPSTREAM-WICKED-WORTHY (port bug, like 1.19).** `insertNodeIntoBin` recycles a node from the freelist; sebbbi's original resets the whole node via aggregate assignment (`m_nodes[i] = {.dataOffset=..., .dataSize=..., .binListNext=...}` → used/binListPrev/neighbor* implicitly cleared); Wicked's MSVC-compat port rewrote it as THREE FIELD WRITES, leaving `used`, `binListPrev`, `neighborPrev/Next` STALE on every recycled node. Consequences: refused neighbor merges, unlinks through stale binListPrev, self-looped bin lists (same node granted dozens of times consecutively), overlapping live suballocations → one mesh's upload stomps another's GPU bytes with all CPU records correct = the slabs/wedges/missing-chunks/blue-palms-family corruption + the CreateAliasingResource E_INVALIDARG device removals. **Proven end-to-end**: lossless 1.46c op capture from the user's triple-level repro → eventual first-divergence at a legal free (op #3592) via a standalone testbench replaying the REAL vendored allocator with per-op node-graph integrity checks → one-line fix (`m_nodes[nodeIndex] = Node();` before the field writes) → same 7629-op workload replays with ZERO integrity failures. Fix = full node reset before reuse; testbench archived in game repo `GameGuru Core/alloc_testbench/`. |
| 1.48a Submit-tail phase attribution | applied 2026-07-26 in Wicked commit `8209a359` (wiGraphicsDevice_DX12.cpp), lib rebuilt | Instrumentation only. `SubmitCommandLists` phase timers (close+execute / frame fences / Present / cross-queue sync / end stall) + per-frame counts (lists closed, `ExecuteCommandLists` batches, dependency-carrying lists) published as `wi::graphics::gg_submit_*`, printed by the harness `SUBMIT_PHASES_MS` line. **FINDING (kills the P.5 'submit batch cost' theory): the ~2-4ms tail is ~95% the END STALL — the next-buffer frame fence, i.e. the GPU is still busy. The TESTPRO1 editor frame is GPU-WALL-bound (~13.6ms wall vs ~11.7 GPU busy w/ profiler), which retroactively explains BUFFERCOUNT 2→3 flat, resolutionScale flat, and hairskip's clean ±1.4ms FPS coupling. CPU-side cuts yield ~zero FPS here until GPU wall < CPU wall** |
| 1.48b Single-queue A/B knob | applied 2026-07-26 in Wicked commit `8209a359` (wiGraphicsDevice_DX12.cpp), lib rebuilt — **default OFF, measured NEGATIVE, keep as probe** | `wi::graphics::gg_single_queue`: `BeginCommandList(COMPUTE/COPY)` → GRAPHICS + same-queue `WaitCommandList` fences dropped (correct because waits only point at LOWER list ids and submission is in id order). Batches 15→1, deps 12→0 — and FPS 71.5→66.8: **the async-queue overlap genuinely wins ~1ms of GPU wall on the RX 9060 XT; cross-queue fences are NOT bubbles here.** Harness `SET_SINGLEQUEUE 0|1` for low-end-GPU probes |
| 1.48c Lean-async queue routing | applied 2026-07-26 (wiGraphicsDevice_DX12.cpp, wiRenderPath3D.cpp), lib rebuilt — **default OFF, measured NEGATIVE, keep as probe** | `wi::graphics::gg_lean_async`: the four tiny helper lists (terrain VT copy-pages, ocean sim + readback copy, VT tile-request + writeback) run on GRAPHICS while the two big compute lists (prepare-async, compute-effects) stay async; same-queue fences dropped (in-order-submission guarantee). Deps 12→~5 — and clean steady-state ABAB says **71.6 → 67.5 FPS (−0.9ms)**: even the tiny lists' async/DMA overlap is worth more than their fence cost on the RX 9060 XT. Together with 1.48b: Wicked's cross-queue split structure is FULLY EARNING ITS KEEP on this hardware — submission overhead is a dead end for perf here. Harness `SET_LEANASYNC 0|1` |
| 1.49(+b) Grass strand LOD (far-distance decimation + width compensation) | 1.49 applied 2026-07-26 in Wicked commit `8209a359`; **1.49b rework applied 2026-07-27** (ShaderInterop_HairParticle.h padding fields, hairparticle_simulateCS.hlsl, wiHairParticle.cpp), lib rebuilt — **DEFAULT ON since 2026-07-27 (user-confirmed in a game-mode walk with the game-side tier coupling — no square pops, no flicker — then explicitly requested as default; `SET_GRASSLOD 0` = kill switch, also pulls the AUTO tiers back to stock)** | `wi::gg_grass_lod` + step2/step4 viewDistance fractions + width boost. GG grass systems only (`grass_type != 0`), character hair untouched; dropped strands take the existing 1.25 early-out (sim work saved too); draw savings flow to EVERY pass reading the culled buffer (prepass/opaque/shadow). MEASURED (v1) TESTPRO1 gate view, clean steady-state ABAB ×2: **72.1 → 77.2 FPS (+0.9ms)** at 0.35/0.60. **v1 FIELD FAILURE (user-reported, same day): two hard rings meant camera motion swept a synchronized band of strands flipping normal↔wide = two-shade shimmer in the mid field, decaying with the editor camera's glide after stopping. 1.49b fix (verified by a 3-lens adversarial workflow BEFORE deploy): (a) per-strand hash-jittered drop radii (±15% band) — transitions become sparse single-blade events, no ring; (b) EXACT hyperbolic coverage ramp 1/(1−0.5t) aligned to the drop window (the review's coverage lens proved v1's linear ramp left a static +16%/−26% lush/sparse ring pattern and boost 1.8 a permanent −10/−19% deficit); (c) boost default → 2.0 = coverage-neutral in expectation, <2.0 = documented deliberate thinning; (d) step4<=0 hardening (was: drops 25% of strands at ALL distances if a future caller zeroed it) + host-side step4>=step2 clamp. Residual accepted risks (review-graded minor/theoretical): un-dropping strand shows one stale-vb_pre frame (same class as the stock cull edge), threshold blades can re-blink under to-and-fro camera dither (single blades, width-compensated).** Harness `SET_GRASSLOD <0|1> [f2 f4 boost]`; until the next game relink the EXE's default boost is still 1.8 — use `SET_GRASSLOD 1 0.35 0.60 2.0` for the exact-neutral config |
| 1.50 GG grass wetmap opt-out (dark-on-reveal fix) | applied 2026-07-27 (ShaderInterop_Renderer.h `WetmapPush.padding` → `gg_force_dry`, wetmap_updateCS.hlsl early-out, wiRenderer.cpp knob + RefreshWetmaps hair-loop gate), lib rebuilt — **default ON (grass force-dried); USER-CONFIRMED 2026-07-27 evening (revealed grass now full brightness immediately, no dark fade)** | **ROOT CAUSE of the "grass renders flat-DARK on reveal, then VERY slowly brightens over 15-30s" editor/game artifact (and the shade component of the original two-shade flicker reports)**: the GG perf early-out in `hairparticle_simulateCS` skips ALL vertex writes for strands that are distance/frustum/1.49-LOD culled, so a never-yet-drawn strand's entries in the alternating ping-pong position buffer stay at the cleared raw ZERO = world (0,0,0) (GG grass = FP32 positions, identity instance transform) = deep below the island waterline. `RefreshWetmaps` (every frame while an ocean exists — MAX enables the Wicked ocean whenever editor water is on) dispatches over ALL vertices of every visible hair system, reads those zeros as submerged, and ratchets them to wet≈0.8 with drying disabled (`max()` + `drying_enabled=false`); `hairparticlePS` renders wet strands with `albedo = lerp(albedo, 0, wet)` = flat ×0.2 darkening (no shadow shape), drying only ticks while the strand is DRAWN at 0.02–0.08/s = the 15-30s fade. Fast-motion flicker = strands crossing the jittered LOD radii swapping between the wet(dark, undrawn) and dried(normal, drawn) populations. Fix: GG grass (`grass_type != 0`) wetmap dispatches carry `gg_force_dry=1` → CS writes wet=0 and exits — grass permanently dry = DX11 parity (DX11 grass never had wetmaps; kelp/seaweed lose wet-darkening too, acceptable). Character hair (`grass_type==0`) keeps stock wetting. `wi::renderer::gg_grass_wetmap=true` restores stock wetting for the bug demo; harness `SET_GRASSWET <0|1>` (0 = default force-dry; recovery on toggle-back is instant for visible systems). Diagnosed by a 4-reader brightness-pipeline mapping workflow, all links verified in code |
| 1.51 DRED post-mortem capture in Release (dred.txt file flag) | applied 2026-07-27 (wiGraphicsDevice_DX12.cpp), lib rebuilt — **armed on the dev rig for the canyon TDR hunt** | Stock Wicked only enables DRED (D3D12 auto-breadcrumbs + page-fault tracking) together with the debug layer (`validationMode != Disabled`), so a Release device removal dumps nothing — exactly what happened with the 2026-07-27 23:07 `DXGI_ERROR_DEVICE_HUNG` TDR during the Canyon Adventure terrain build (unreproduced ×2 afterwards; allocator/streaming/quiesce tripwires all clean; evidence archived in scratchpad crash_2307). Now: an empty **`dred.txt` next to the EXE** arms DRED settings at device creation WITHOUT the debug layer (`gg_dred_armed`, launch log line confirms; delete the file to disarm — breadcrumbs cost a few % GPU while armed), and OnDeviceRemoved APPENDS the full report (removal reason, last in-flight op per command list, page-fault VA + owning live/freed resources) to exe-dir **`dred_report.txt`** — persists across the process Exit(), backlog flush not required. Complements AMD Radeon GPU Detective (driver-level capture) for the same hunt |
| 1.52 **Pooled weak_ptr underflow — THE entity wrong-texture ROOT CAUSE** (upstream port be8c766e) + 1.52b hijack tripwire | applied 2026-07-28, **USER-CONFIRMED same day (multi-level repro repeated, no wrong textures; hijack tripwire stays as the long-term sentinel)** (wiAllocator.h cherry-pick of upstream `be8c766e` "fixed critical issue with weak_ptr #1366 #1367" — 2025-12-14, NOT an ancestor of our vendored HEAD; heap-allocator portion skipped, nothing here instantiates it; + wiResourceManager.cpp Load() tripwire), lib rebuilt | **SECOND allocator-vintage root cause after 1.47, same file family.** `weak_ptr::lock()` on a dead-but-still-mapped resource entry used inc-then-undo; the undo (`dec_refcount(ptr,false)`) hits old==1 and UNCONDITIONALLY calls `dec_refcount_weak` — stealing the name map's weak claim → the 256-byte ResourceInternal pool block is reclaimed to the LIFO free list WHILE `resources[name]` still references it → next `make_shared<ResourceInternal>` (level-load burst = usually the adjacent texture, often the `_normal.dds`) reuses the block → the stale map entry aliases a FOREIGN live resource → next `Load(name)` returns the normal map under the color map's name (timestamp gate ≈ coin flip = the intermittency) → template material + every name-shared clone compose the impostor's (real, valid) sRGB descriptor = **"normal map as color map" palms/ferns after multi-level sessions, seen for weeks**. Explains all live forensics (2026-07-28 island-as-4th-level triage: composed-vs-default descriptor scatter on COLOR slots only, epoch=0, SET_MATCACHE 0 no-heal — recompose faithfully re-reads the hijacked handle, REUPLOAD_TEXTURE instant heal — fresh internal). Diagnosed by a 4-reader workflow; bug + fix verified by eye against upstream. Fix = CAS `try_inc_refcount` (never resurrects from 0), undo path deleted, `dec_refcount` loses `destruct_on_zero`, `use_count()` IsValid-guarded. **1.52b**: `Load()` cache hits are filename-checked — a mismatch (hijacked binding, any residual sibling bug) self-heals as a cache miss and appends evidence to exe-dir `resource_hijack.txt`; post-1.52 this should NEVER fire = permanent field tripwire. Also candidate root for the racy variant behind the 2026-07-27 canyon TDR (DRED/RGD net stays armed to confirm). Related side-leaks found same hunt (separate tickets): hot-swap watcher DeleteImage+in-situ reload under shared names; MasterObject=0 image pins never freed by entity_deletebank (cross-level VRAM growth); ghost-human +3 materials/reload |
| 1.53(+b/c) Terrain VT tiling repeat CAP + ladder HOLD (near-camera scale-swim fix) | 1.53 applied 2026-07-28, **1.53b redesign same night after the user's field test**, 1.53c hold dial same night (ShaderInterop_Renderer.h `TerrainVirtualTexturePush.padding0` → `gg_tile_share`, terrainVirtualTextureUpdateCS.hlsl policy helper across all 4 map variants, wiTerrain.cpp knob + push fill; game: `GGTerrainWicked_SetTileShare` + harness `SET_TERRAINTILE`), lib rebuilt — **FINAL DEFAULTS cap=32 hold=4, USER-CONFIRMED 2026-07-28 (live bracketed walk: cap 8→16→32 "human scaled underfoot", hold 8→6→5→4 "that will do nicely")** | **User-reported: painted terrain cross-fades several UV scales of the same texture right at the camera ("swimming mess underfoot").** Mechanism: the VT compose bakes each mip of a chunk region with the texture repeat count halved per mip (near chunks ladder 64,32,16,8,... repeats), and `SampleVirtual`'s custom trilinear cross-fades two different TILINGS per mip boundary — in MAX's inch-scale world the visible rungs sit within walking distance (user's meter-vs-inch prediction: correct). **v1 FIELD FAILURE (caught by the user in minutes): v1 anchored a share window to the TOP of each chunk's ladder — but the top rungs are sub-magnification (never rendered), so K only multiplied the VISIBLE rungs' density 2^K; same transition distances, finer/busier pattern = "the transition moved CLOSER".** v2 = repeat CAP: every rung finer than `cap` repeats-per-chunk bakes the SAME cap-scale layout (pure downsample chain = invisible transitions from the camera through the mid field); rungs at/below the cap keep stock halving (far anti-tiling preserved). World-anchored → immune to the binary 65536/256 chunk resolution rings, border seams, and promotion scale-pops; the dial doubles as the terrain texture's world feature size (chunk ≈ 5120 in: cap 8 ≈ 16m repeat, 16 ≈ 8m, 32 ≈ 4m). 0 = stock bit-for-bit. Live: `SET_TERRAINTILE <cap>` queues the safe 1.13 fast repaint (knob-time burst only; bake-time policy, ZERO per-frame cost). A/B verified from the snowy test scene: stock = mixed-scale patchwork at the marker; cap 16/8 = one consistent world scale from feet outward. **1.53c HOLD** (bits 16..23): the cap welded feature size to handoff distance — hold delays the halving ladder N rungs (applied PRE-floor so rung positions past repeats==1 survive), moving the first cross-fade ~1.4× further per +1 while feature size stays fixed; far anti-tiling engages beyond the held range. `SET_TERRAINTILE <cap> [hold]` |
| 1.54 Tonemap true display gamma + bloom strength — **PARKED (reverted to stock same evening)** | attempted + reverted 2026-07-28 evening; NOT in the tree (tonemapCS.hlsl / ShaderInterop_Postprocess.h / wiRenderer.cpp all back to stock) | Design: `PushConstantsTonemap` + packed half2 (gamma exp = 2.2/userGamma, bloom strength), pow applied last in display space. FIELD FAILURE: with neutral 1.0/1.0 pushed, the live editor viewport washed to ~3.4x luminance (screenshot-metered 60->205 uniformly near+far = tonemap-stage); bisect proved the two shader lines were the trigger, yet a corner-probe visualizer of the pushed pair never appeared in the viewport — i.e. the pushed values read wrong in the LIVE path and the visible pass didn't show the probe where expected. Do NOT re-attempt blind: needs a PIX capture of the editor frame to see which tonemap dispatch feeds the viewport and what the root-constant block actually contains. Gamma slider meanwhile uses the brightness-offset approximation (game-side, neutral at 2.2); Bloom Strength re-hidden |
| 1.55 Global env-probe brightness at sample time (restores DX11 slider) | applied 2026-07-28 daytime (ShaderInterop_Renderer.h `ShaderScene.padding6` → `gg_envprobe_brightness`, lightingHF.hlsli EnvironmentReflection_Global multiply inside the non-ENVMAPRENDERING branch, wiScene.cpp `wi::scene::gg_envprobe_brightness` + shaderscene fill), lib rebuilt | `EnvironmentProbeComponent::SetBrightness` was removed upstream — the "Global Probe Brightness" slider only re-rendered probes at unchanged brightness (looked alive, did nothing; UI-audit find). Now the global probe cubemap sample is scaled at lighting time: live while dragging, no probe re-render, 1 = bit-stock. ENVMAPRENDERING (probe capture) branch deliberately untouched so probes don't self-multiply |
| 1.56 Light shafts sun-mask taming: unit-peak sun color in dark mode + dark mode for the gradient sky path (whole-screen wash fix) | applied 2026-07-29, screenshot-verified same day on TESTPRO1 island (crop luma: off 114 → on@parity 123, was 164 pre-fix at the same strength / 199 at upstream defaults); **USER-CONFIRMED same day ("Lovely") incl. the tree test — distinct crepuscular rays through foliage gaps, depth-tested disc occlusion working** (skyHF.hlsli only — BOTH darkMode sites; shader-only, sunPS.cso recompile via refresh/delete, no lib dependency though engine was rebuilt anyway) | **User-reported: enabling Light Shafts washed the entire viewport with white haze instead of drawing rays.** TWO stacked causes in the sun mask that sunPS.hlsl renders via `GetDynamicSkyColor(dir, true, true)` (dark_enabled=true, expects a sun-disc-only image): (1) **this engine's `GetSunColor()` arrives with intensity PRE-applied** (globals.hlsli:928 "sun color with intensity applied") while the DX11-era fork fed the RAW light color into the darkMode disc — MAX drives sun energy ~9, so the disc was ~9× hotter on BOTH sky paths; fixed by normalizing to unit peak (`sunColor / max(maxcomp, 1)`) in the darkMode branch — hue preserved, dusk/night sun still dims (divisor clamps at 1). (2) the non-realistic horizon/zenith gradient branch IGNORED dark_enabled — the FULL sky gradient entered the quarter-res mask (DX11's fork had the disc formula here, skyHF.hlsli:148 in D:\max\WickedRepo); restored as `pow(dot(sunDir,V),64) * unitPeakSunColor`. Pairs with the game-side DX11 exposure parity `setLightShaftsStrength(0.18f)` (upstream default 0.5 — raised from DX11-era 0.2 in 787d0afa — and 64 taps vs DX11's 32: DX11 gain 0.2×4.80≈0.96 vs 0.18×5.42≈0.98; game never called the setter before). Harness `SET_LIGHTSHAFTS <0|1> [strength]` |
| 1.57(+b) Shadow flicker toolkit: receiver-bias knob (default 0) + slope-bias CLAMP | applied 2026-07-30 early AM; **1.57b USER-CONFIRMED 2026-07-30 ("MUCH better — the shadow moves rather than flickers as the animation sways") — the unbounded D16 slope-bias explosion WAS the flicker root cause; 1Hz instruments simply couldn't see the fix** (1.57: ShaderFrame.padding0 → `gg_shadow_receiver_bias`, lightingHF.hlsli `shadow_pos.z +=` at BOTH cascade sample sites, wiRenderer.cpp fill + `wi::renderer::gg_shadow_receiver_bias` global, harness `SET_SHADOWBIAS <0-64 ulps>`; **1.57b: RSTYPE_SHADOW `depth_bias_clamp = -16/65536`** — was 0/unbounded), lib rebuilt | **User-reported: sun self-shadow on an animated character's back/neck flickers in sync with the idle animation (TESTPRO1 close-up, camera EYE 3305.6,187.2,-8483.0 ang 16.9,-93.4 — SET_CAMERA restores the repro).** Investigation status: NOT yet convicted. ELIMINATED at the repro (identical 1Hz burst signatures): receiver bias 0/2/6/12/64 ULPs, delayed-cascade stagger ON/OFF, per-cascade shadow LOD override ON/OFF, far-cascade cull ON/OFF, slope-bias clamp on/off. KEY CONSTRAINT: harness screenshots sample ~1Hz — a 60fps temporal artifact cannot be measured; every burst metric was dominated by legitimate pose-driven shadow movement (grazing-incidence amplification ~×10 on shoulder/neck surfaces). 1.57b kept on principle: with clamp 0 the -4× slope term is UNBOUNDED and in D16 each bias unit = 1/65536 of cascade Z range (~128× coarser than the DX11 fork's D32 maps at the same -1/-4 numbers) — grazing casters got pushed tens of inches lightward, an unphysical port artifact regardless of this bug. TOP REMAINING SUSPECT (untested): the DX11 fork's receiver-side depth FEATHER (`shadows *= saturate((depthSample-dist)*65536/(cascade+1))` + 3×3 bilinear Gather PCF, WickedRepo lightingHF.hlsli:48-60) which the new hard-SampleCmp Vogel-disk path lost — a 1.58 candidate needing the user's 60fps eyes for verification. Full elimination data in memory `project_shadow_system.md` |
| 1.58 DX11-fidelity shadows: D32_FLOAT atlas + feathered gather PCF for sun cascades (+hair bias latent-bug fix) | applied 2026-07-30 (wiRenderer.h:33 format flip D16→D32; wiRenderer.cpp float-branch bias → DX11 parity -1/-4/clamp0, upstream -10/-3.4 noted; wiHairParticle.cpp latent bug — non-unorm branch wrote rs.depth_bias not rs_shadow → fixed; shadowHF.hlsli new `shadow_2D_feathered` — point-clamp GatherRed through the FLOAT bindless view (half4 view would quantize D32 below the feather), ALU compare with graded feather `saturate((stored-receiver)*65536/(cascade+1))`, manual bilinear, distance-based 8..1 taps; lightingHF.hlsli light_directional uses it at both cascade sites with camera distance), lib rebuilt | User asked the cost of the DX11 shadow "luxuries" after the 1.57b flicker fix: DX11 ran R32 depth + Gather/feather soft compare; upstream DX12 shipped D16 + hard 16-tap SampleCmp. This delta restores both, verbatim-ported from the DX11 fork (WickedRepo lightingHF.hlsli:48-184; extraction + full D16→D32 safety audit in session notes). Spot/point lights keep the stock path. VRAM cost ≈ atlas ×2 (sun-only island ~+10 MiB, worst cap +128 MiB). KNOWN GAP for later: the atlas packer caps at 8192 so five "2048" sun cascades actually pack at 1024 each — DX11's texture array ran true 2048; raising the cap is a separate decision. 1.57 SET_SHADOWBIAS ULP convention now D32-miscalibrated (default 0, harmless). Measured + user-eyeball verdict pending. **1.58b (2026-07-30, user-directed): atlas packer cap 8192→16384 (D3D12 max) — the five "2048" sun cascades were silently packing at 1024 each (10240 > 8192 → iterative halving); now TRUE 2048/cascade. Character-scale math: cascade 0 spans ~700-850in → was 0.7-0.9 in/texel (~80-100 texels per 72in character, visibly blocky ground shadows), now 0.35-0.45 in/texel (~180-200 texels). Cost: ~1 FPS (noise), VRAM +~190-375MB (both atlases ×4 area total vs pre-1.58).** |
| (game bridge) Character dedicated shadows — engine slot mechanism lit up from GG entities | game-side only, 2026-07-30 (`4aff35da`: wickedcalls_part3.cpp `WickedCall_UpdateCharacterShadows` + call in master_part0.cpp Master::Update after `__super::Update(dt)`; harness `SET_CHARSHADOW <0-3>`, default 3) | New Wicked ships per-character dedicated shadow slots (Scene::character_dedicated_shadows → one extra full-res cascade slice PREPENDED per sphere; sampling incl. the 1.58 feathered PCF covers it automatically) but drives them from CharacterComponents, which GG must NOT create (upstream controller stomps transforms even when inactive, wiScene.cpp:6499). Bridge fills the sphere list directly each frame AFTER Scene::Update (which clears it): live `ischaracter` elements, editor-safe filters (active/health only in game mode), nearest-3 to camera, frustum-culled, sphere = feet+(0,h/2,0) r=h/2 (h = 72 × profile scale%, clamp 30-200). Visual: ~28 texels/inch on the character vs cascade 0's ~2.5 — collar/neck/armband shadows crisp (side-by-side verified 2026-07-30). KNOWN COST: any live slot makes the engine bail out of the delayed-cascade stagger (wiRenderer.cpp:4985) and far-cascade cull (:7310) — measured ~70→55 FPS at the fighter view; FOLLOW-UP engine delta planned: offset those features' cascade indices by dedicated_count instead of bailing |
| 1.59(+b,c) Delayed-cascade stagger + far-cascade cull dedicated-aware; **1.59c blend-band containment guard (the REAL rectangles fix; 1.59b's Z extensions REVERTED)** | applied 2026-07-30 (`b4fc81ed` stagger/cull offsets; `ef719b45` 1.59b Z-extension SUPERSEDED — reverted in `ac0b282d`; **1.59c `ac0b282d`: lightingHF.hlsli blend fallback now requires `is_saturated(shadow_uv)` on the NEXT cascade before lerping**) | **TRUE root cause of the rectangular ground artifacts near characters (user repro): the cascade Z-EDGE BLEND BAND.** Pixels in the outer 10% of a slot's depth range blend toward `cascade+1` — for dedicated slot i that is ANOTHER character's slot; the fallback never checked containment, so the border clamp smeared the foreign slot's edge texels across the band (thin rectangles at stock Z; giant bars + DISPLACED shadows under 1.59b/c's stretched ranges — the moving bands were the tell). 1.59b's clamp theory was WRONG (ortho-clamped casters are geometrically correct blockers); its Z changes only relocated the bands. Concentric sun cascades always satisfy the new containment check → their edge fade is unchanged. Verified: ON/OFF diff at the 3-character repro = only edge-crispness outlines, zero structured artifacts, wiRenderer.cpp: DelayedShadowCascadeState.dedicatedCount field; stagger group offset past dedicated slots — dedicated + sun cascade 0 every frame, sun 1..N stagger; dedicated-count CHANGE forces full refresh so frozen per-slot matrices never cross shifted indices; far-cull thresholds keyed on sun_cascade = raw slot − dedicated_count, dedicated slots exempt), lib rebuilt | Both features previously BAILED entirely whenever character dedicated shadows were live (slots shift cascade indices) — measured ~70→55 FPS with one hero slot at the grass-heavy fighter view. After 1.59: dedicated ON 69.3-69.6 vs OFF 69.7 FPS — **the dedicated character shadow now costs ~0.3 FPS**, with DELAYED_SHADOWS confirmed staggering while the slot refreshes every frame. Crispness verified intact post-fix |
| 1.60 Skinned normal/tangent streamout 8-bit→16-bit float (skirt normal-map flicker) | applied 2026-07-30 early AM (`a7daf3a7`, wiScene_Components.cpp CreateStreamoutRenderData: so_nor/so_tan views R8G8B8A8_SNORM → R16G16B16A16_FLOAT + allocation ×2; shaders untouched — typed views; +8 B/vertex on SKINNED meshes only), lib rebuilt — **user 60fps verdict PENDING** | **User repro: CC warrior skirt's woven pattern/shading JUMPS between near-identical idle poses whenever material Normal Strength > 0 (they run 4.0 = 4× amplifier); rock-stable at 0.** Root-cause theory (trace-verified mechanics): skinningCS accumulates in half and wrote through 8-bit SNORM streamout views — the animated tangent frame re-quantized to ~0.45° hops per frame, per-vertex asynchronous → patchy shading twinkle; strength-0 collapses `mul((0,0,1×s),TBN)` to the vertex normal = tangent unused = stable. DX11 skinned full-float in the VS (no re-quantization). ELIMINATED by live probes: texture streaming (chars never STREAMING-flagged + SET_STREAMING null), matcache (SET_MATCACHE null). REMAINING suspects if user still sees it: CC anim-proxy gates (all anims pinned to arbitrary mesh, wickedcalls_part0.cpp:1000 — probe SET_ANIMVIS 0 / SET_ANIM30FPS 0), implicit normal-map mip hop under animated UV footprint, tan.w=0 handedness (DBO never stores sign; shader forces +1 — STATIC wrongness on mirrored islands, separate issue), strength>1 lerp extrapolation. 1s-apart screenshots CANNOT adjudicate (pose confound) — user eyes only |
| 1.61 Anim-throttle unresolvable-proxy fallback → full rate (+game: CC pin = largest mesh; **THE roaming character 'texture flicker' ROOT CAUSE**) | applied 2026-07-30 morning (engine `6ba30ab4` wiScene.cpp throttle gate else-branch; game `eea5d582` wickedcalls_part0.cpp largest-AABB pin + harness `BURST_FRAMES <2-120>` consecutive-frame instrument), lib rebuilt, **BURST-VERIFIED same morning** | **User repro: one character at a time shows per-frame 'texture/light flicker' at Normal Strength > 0; clicking (re-selecting) a character heals it permanently and the affliction moves to another — long-standing.** TRUE ROOT CAUSE: 'Lower Animation & LUA Speed' arms the 1.29 30fps throttle; its distance gate resolves characters via animation.objectIndex = the CC pin (ARBITRARY unordered_map begin() mesh, wickedcalls_part0.cpp:1000); when the lookup fails the old fallback was 'leave eligible' → the character animates at HALF FRAME RATE at any distance; pose-stepping under normal mapping reads as pattern flicker (invisible at strength 0). Click = delete+recreate = fresh pin = heal; ecs churn moves the stale-pin affliction = the rotation. CONVICTION: BURST_FRAMES showed victim deltas big/0.0/big/0.0 alternation; throttle-off and post-fix runs smooth (0 frozen steps), throttle feature intact for far characters. ELIMINATED en route (all null): dedicated slots, cascade stagger, receiver bias, streaming, matcache, 8-bit tangents (1.60 — kept as hygiene). NOTE 1.60's twinkle theory was NOT the cause of this bug |
| (game bridge) DBO tangent handedness w=-1 — **normal-map "UV swimming" flicker ROOT CAUSE** (the 1.60 skirt saga's true fix) | game-side only, 2026-07-30 afternoon (wickedcalls_part0.cpp LoadNode tangent push: `XMFLOAT4 tan(0,0,0,-1)` was `(0,0,0,0)`; + harness `DUMP_SKINGEO` forensics command) | DBO files carry NO tangent-handedness sign (converter stores assimp TANGENTS in the BINORMAL-labelled slot dwTU[2] — the slot read here is the RIGHT vector — and bitangents in dwTU[1]; neither has w). DX11's old engine ran `normalize(cross(T,N)*w)` with the packed w≈-0.004 → **effective w=-1 on every DBO mesh, stable**. New Wicked instead snaps w=0→+1 (`T.w = T.w<0 ? -1:1`, surfaceHF/objectHF) → bitangent INVERTED vs DX11 on every DBO-loaded mesh → normal-map green channel lights backwards → embossed detail responds to skinned animation in the wrong direction, perceived as texture swapping/swimming, amplitude ∝ Normal Strength, zero at 0 (user-observed law). Meshes without DBO tangent decls (MikkTSpace path) always had correct per-vertex w — hence the per-part/per-character lottery. Eliminated by live A/B before conviction: dedicated shadows, delta-1.41 material cache (recompose-every-frame changed nothing), texture streaming pause. Fix = DX11-parity constant w=-1. Possible refinement: derive true per-vertex w from the stored bitangent (dwTU[1]) — would fix mirrored UV islands DX11 also got wrong; parked pending user verdict. **User verdict PENDING.** Separate CONFIRMED artifact discovered same session: dedicated-character-shadow sub-texel swimming (no texel snapping on the per-character ortho fit) — the macro shadow-blob flicker on faces/collars; also: recreated characters drop off the dedicated-slot list until reload. Both parked as follow-ups. |
| 1.62 Tangent-frame visualization debug modes (`gg_debugvis`, harness `SET_TANGENTVIS <0-5>`) | applied 2026-07-30 afternoon (engine `789ecfbc`: ShaderInterop FrameCB padding→gg_debugvis, wiRenderer global+CB fill, objectHF.hlsli two early-out switch blocks `#ifndef PREPASS`) | Forensic instrument born from the user's "render just the tangent data" idea. Modes: 1=world tangent, 2=vertex normal, 3=bumped normal, 4=handedness (red -1/green +1; doubles as a DBO-vs-MikkTSpace part map), 5=strength-scaled normal-map sample. **VERDICTS on the character-flicker repro (TESTPRO1 3 heads, strength-4 saved level): tangents STABLE (mode-1 churn 0.19 ≈ zero — tangent theories DEAD, incl. the w=-1 bridge fix which the mode-4 map shows only ever applied to minor DBO parts like the necklace, NOT the MikkTSpace-tangent skin meshes); the churn lives in the normal-map SAMPLE (mode-5 3.14 → 1.89 with `SET_STREAMING 0`, quiet-pose pairs 3× calmer) — texture-streaming min-LOD animation flips the sampled mip = literal "texture swapping" percept, × Normal Strength (shader multiplies rg by strength = the user's amplitude law). FPE library scan = major streaming-churn aggravator in editor (their repro shots ran at FPS 15-39 mid-scan).** Next lever: damp/quantize the streaming min-lod fade or pin magnified on-screen textures; residual = strength-4 sampling shimmer + dedicated-shadow swimming (chip). |
| 1.62b full contributor battery + 1.63 DX11-parity normal strength — **"all 3 flickering like crazy" FINAL VERDICT: Normal Strength 4.0 saved in the test level, NO engine corruption** | applied 2026-07-30 evening (engine `424fac7f`: objectHF/surfaceHF; game harness `SET_TANGENTVIS <0-22\|CYCLE>` + `SET_NORMSTR <0-8>`) | The user-requested render-every-contributor sweep (22 modes, per-frame burst churn on the live repro): tangents 0.19, vertex normals, UVs (raw + ×64 grid), world positions 0.09, vertex color, emissive, F0, RAW normal-map texels incl. forced-mip0 — ALL frame-stable; streaming pause changed nothing (earlier -40% was sway-phase luck — streaming EXONERATED); no content rewriting (1.47/1.52-class ruled out, tripwires silent). The shaded churn tracked ONE variable: `SET_NORMSTR 1` → the "crazy" Elder drops to the calm warrior's level (cheek churn/sway 3.07→0.78); back to 4 → violence returns. Mechanism: rg×4 (or DX11's lerp-extrapolate — same first-order math ≈ (4x,4y,1)) pushes the bumped normal near-parallel to the surface where unavoidable sub-texel sampling drift under skinned animation swings it wildly = boiling "texture swapping" percept. Earlier ghosts explained: click-heal = recreate resets entity strength to profile default; new characters spawn at default = immune; per-load affliction = which entities carried cranked strength. 1.63 keeps DX11's exact blend semantics (unscaled sample, N-lerp, bumpColor×strength after) — modest taming + true parity; honest caveat: DX11 at 4.0 would also boil, the user likely never ran 4 on DX11. GUIDANCE: character Normal Strength belongs at ≤1.5; consider a UI cap or FPE audit. Saved TESTPRO1 still carries 4 on the characters — user should reset+save. |
| (follow-up closure 2026-07-31) **Dedicated-shadow "texel snapping" + "lost slots" chip: BOTH MEASURED PHANTOMS on the 1.64 build** | measurement-only + harness message fix (SET_CHARSHADOW now labels its count "slots last frame … one frame stale") | Post-1.64 triplicate A/B at Normal Strength 1 (TESTPRO1 3 heads): dedicated slots ON vs OFF statistically identical (churn/sway 1.11 vs 1.19; blob metric 0.39 vs 0.46 — OFF trivially higher = noise). The pre-1.64 "shadow blob swimming" diffs were the tangent-w coin-flip wearing a shadow costume. Engine-side: CreateDirLightShadowCams ALREADY floor-snaps every cascade incl. dedicated slots to the texel grid at stable texel size, and an idle character's slot sphere is entity-position-static — no swim mechanism exists. "Lost slots": the game bridge discovery is stateless per frame (no cached wicked handles; ee.obj re-resolved), double-sample reads 3/3; every "0" reading traced to the one-frame-stale toggle artifact now labeled in the message. No engine or bridge changes needed. |
| 1.62c/d `SET_BINDPOSE_TAN` lever + `DUMP_SOTAN` byte probe — **coin-flip CAUGHT AT BYTE LEVEL: foreign GPU writer stomps the streamout tangent w lanes** (user matched mode-4 cadence to the texture flicker exactly; byte verdict: SOURCE vb_tan 100% canonical+static, so_tan xyz properly skinned, w = block-uniform per-frame junk (-4.42/-1205/-0.089...), 13-23 runs, per-mesh scalars, NO ±1 anywhere; bind-pose lever = instant stability 25.79%→0.02%; dedicated shadows exonerated 24.36% with slots off; SRV+UAV collisions clean on afflicted loads too ⇒ non-character float4-UAV writer; next: RLE block fingerprint + so_nor footprint + PIX) | applied 2026-07-30 night (engine `bfb76c94`: gg_force_bindpose_tangents gate at the wiScene so_tan geometry patch; game bridge + UAV columns in DUMP_SKINGEO) | User cycled the 1.62b debug layers on a live "MASSIVE FLICKER" load and fingered **mode 4 (handedness) as the instigator** — confirmed by burst forensics: interior pixels flip red↔green at **46.5%/frame** (coin flip), timelines RANDOM but SHARED by coherent pixel groups = GPU race signature; xyz stable (mode-1 churn 2.6). w is pose-independent → bones/animation ruled out BY MATH. Tested DEAD: cross-mesh SRV collisions (278 parts unique), **UAV collisions (new DUMP_SKINGEO columns — all unique)**, within-buffer overlaps (offsets clean), 1.46c suballocator tripwire (0 OVERLAP/OOB in 1.1M ops), double dispatch (one per mesh, wiRenderer 5626 loop), streaming pause, material cache, VS/interp chain (w preserved end-to-end, verified by eye). **LOTTERY: 9 harness loads (in-place, fast, full-relaunch) ALL CALM (0-2.5%); user manual relaunch+load afflicted 3/3 same day.** NEXT on an afflicted instance: SET_TANGENTVIS 4 → SET_BINDPOSE_TAN 1 → verdict content-vs-consumer; then GPU readback (DUMP_SOTAN candidate) or PIX. Suspect space: untraced streamout writer / barrier hole visible only under specific load-time GPU scheduling. |
| **1.64 skinningCS tangent-w float carrier — THE INTERMITTENT CHARACTER "TEXTURE FLICKER" ROOT-CAUSE FIX** (+1.62e `SET_WETMAPS` kill-switch, wetmap exonerated) | applied 2026-07-30 late night (engine `92b7654d`: skinningCS.hlsl reads the tangent via bindless_buffers_float4 and carries w in a dedicated float from load to store — explicit `float4((float3)tan.xyz, gg_tan_w)` store; game `DUMP_SOTAN` RLE/footprint upgrades + SET_WETMAPS bridge) | **Byte-proven root cause: the CS's streamed-out tan.w was NEVER the source handedness — each 64-thread wave stored ONE wave-uniform garbage value** (RLE fingerprint: every corrupt run = a multiple of 64 verts on 64-vert boundaries; source vb_tan 100% canonical+static; so_nor/so_pos w-lanes intact ⇒ corruption exactly wave-granular in the tan store; packed-half register lifecycle around the partial update `tan.xyz = normalize(t)` is the best-supported mechanism — attribution inferred, facts airtight). Frame-UNSTABLE garbage = the per-load-lottery "massive flicker" (user-matched mode-4 cadence to the texture flicker); frame-STABLE garbage = the ever-present wrong handedness patchwork (no load ever had canonical w!). Exonerated by A/B on afflicted loads: wetmaps (1.62e), dedicated shadows, streaming, matcache, SRV/UAV collisions, suballocator. **VERIFIED: 10 loads (7 reload + 3 full relaunch) at 0.00-0.07% mode-4 interior flips (was 16-46% on ~1 in 3 loads); post-fix so_tan w histogram [-1]=3645 [+1]=157 wild=0 — canonical for the first time in the investigation. USER-CONFIRMED 2026-07-30 night: "OK, first time no flicker". UPSTREAM WICKED REPORT CANDIDATE** (upstream uses the same half4 tan + partial update + typed store). User 60fps verdict pending; note the saved TESTPRO1 level still carries Normal Strength 4.0 (separate deterministic boil, 1.63 row). |
| **1.65 DX11-parity fog color model + Fog Opacity plumb — Sky Box/None stale-green-fog fix** (defect 2 of the 2026-07-31 sky session) | applied 2026-07-31 early AM (engine: fogHF.hlsli `GetFog` rewrite; skyPS_static.hlsl + skyPS_dynamic.hlsl opacity tint; ShaderInterop_Weather.h `padding_stars`→`gg_fog_opacity`; wiScene_Components.h WeatherComponent `gg_fog_opacity=0`; wiScene.cpp fill. Game: Wicked_ApplyFogModel plumbs FogA_f with the DX11 legacy >2→0 clamp) | DX11's fog color per mode (GGCommonFunctions.hlsli ApplyFogCustom, byte-read from the READ-ONLY reference): **realistic sky** → per-view-direction horizon SKY SAMPLE (skyview LUT, stationary, y=0.1 flattened dir — distant terrain hazes into the sky it stands against, silhouettes stay readable), then lerp toward user FogRGB by FogOpacity; **skybox/gradient** → the surface's OWN color lerped toward FogRGB by FogOpacity = a pure opt-in recolor, NO-OP at FogA 0 (encoded as `alpha *= opacity` since lerp(C,lerp(C,F,o),a)==lerp(C,F,a*o)). Stock Wicked instead fogged toward the FLAT skyluminance-LUT average under realistic sky (mountains melt uniformly) and toward `weather.horizon` at FULL strength otherwise — which painted the None-mode green Horizon/Fog color over terrain in Sky Box mode (user screenshot repro). Stock sun HgPhase inscatter add dropped both branches (DX11 has none; the skyview sample carries azimuthal mie). Both sky PS get the DX11 `lerp(sky, FogRGB, FogOpacity)` tint for seam coherence. Verified on TESTPRO1 (FogA=0, FogRGB pure green): skybox mode green fog GONE = DX11 shot parity; None mode mountains natural. |
| **(game) Volumetric cloud parameter unit correction — Simulated Sky "sky eats the mountains" ROOT-CAUSE FIX** (defect 1 of the 2026-07-31 sky session) | game-side only, 2026-07-31 early AM (M-GridEditB_part3.cpp both Wicked_Update_Visuals + Wicked_Update_Cloud simulated branches; M-TerrainNew_part4.cpp height/thickness/coverage slider handlers; M-TerrainNew_part5.cpp snapshot override; harness `SET_SKYMODE <0-2>` + `SET_VOLCLOUDS <0\|1>` + `SET_FOGDENS <d\|-1>`) | The new engine's cloud shader works in WORLD units (planetRadius = bottomRadius·1000 treated as world; extinction·distance; march distances) — MAX world units are INCHES. The port fed `GGTerrain_UnitsToMeters(SkyCloudHeight)` = the cloud deck at **1524 units = 39 real meters**: a ground fog bank legitimately rendered IN FRONT of every distant mountain (harness A/B convicted: fog OFF → mountains still gone; clouds OFF → mountains back). Fix set, all A/B-verified on TESTPRO1: (a) cloudStartHeight/cloudThickness fed RAW units (deck back at 1524 m real); (b) engine meter-tuned defaults rescaled to units — maxMarchingDistance/renderDistance/LODDistance ×39.37, `horizonBlendAmount` ÷39.37 (unscaled it faded ALL clouds beyond 2 real km = empty sky), `extinctionCoefficient` ÷39.37 (unscaled = 39× optically thick = near-black deck), noise/weather scales ÷39.37, skew/curl modifiers ×39.37, shadowStepLength ×39.37, wind speeds ×39.37 (UI value read as m/s); (c) `inverseDistanceStepCount` = 3× shell thickness (stock ratio) — raw meter conversion left ~2 march samples per vertical ray = invisible clouds; (d) **coverageMinimum = saturate(SkyCloudCoverage − 1)** — the DX11-era shader did `saturate(CoverageMinimum - 1.0)` (WickedRepo volumetricCloud_renderCS.hlsl:76), the level's 1-based 1.38 fed raw = wall-to-wall overcast. Result: scattered puffy cumulus behind fully-visible hazy mountains = DX11 reference shot parity; mode switching stable both directions. Residual known gap: slight quarter-res upsample blockiness at cloud/ridge silhouette boundaries (stock Wicked artifact). |
| **1.66 cloud upsample bilateral inch-unit fix — the "mountain silhouette line" through the cloud deck** | applied 2026-07-31 afternoon (engine `fb742ed4`: volumetricCloud_upsamplePS.hlsl only) | User saved a LOW cloud deck (height 19 m — clouds draping over the mountains, an effect they like) and the bright pixel-stepped line tracing the ridge silhouette became glaring. Root cause: the depth-aware upsample rejected EVERY tap at terrain/sky depth discontinuities and emitted hard zeros = un-clouded holes along the silhouette (the sky/AP behind showed through as the bright line). Three compounding faults: (a) cloud depth read into `half` — +inf beyond 65504 units = **1.66 real km** in inch units → all range weights zero at distance; (b) `GAUSSIAN_SIGMA_RANGE 100.0` assumed meters = 2.5 real m of tolerance → valid same-surface taps rejected → now 3937 (stock 100 m intent); (c) `Gaussian()` computed in half — `2σ²` overflows half with the corrected sigma (NaN weights). Plus robustness: `weightSum == 0` now falls back to the bilinear sample instead of zero. Verified before/after crops on the user's 19 m deck scene: line GONE, deck blends smoothly over the ridge. This closes the "residual quarter-res blockiness at ridge silhouettes" caveat from the sky-session rows above — it was never a stock-inherent artifact, it was the same meters-vs-inches class of bug. |
| 1.67 Profiler QoL: stable GetTextData row list + main-camera poly counter | applied 2026-07-31 afternoon (engine: wiProfiler.cpp `GetTextData` persistent sorted caches + `gg_ClearTextDataCaches` on profiler toggle; wiRenderer.cpp/.h `GG_GetMainCameraPolyCount` — atomic accumulate in RenderMeshes gated `RENDERPASS_MAIN && DRAWSCENE_MAINCAMERA`, latched per frame at UpdatePerFrameData. Game: panel header "FPS: x  POLYS: xK/M (DirectX 12)" in M-GridEditB_part2.cpp, `GGPerf_GetPolyCount` wrapper in master_part1.cpp, `POLYS:` line in harness GET_PERF_DATA) | **User-reported: the test-game Performance panel "jumps" — rows appear/disappear (TerrainW - *, RP3D-rec UpdateTex...) changing the list length every few frames.** Cause: GetTextData only printed ranges begun THAT frame; conditional ranges dropped out on idle frames. Fix: once a range name is seen it stays in the printout forever (0.00 ms on idle frames) and output is sorted by name — row set can only grow (one-time) when a genuinely new range first fires. Caches clear on profiler toggle so sessions start fresh. Poly counter counts triangles submitted to the main camera color pass only (once per subset; shadow/prepass/reflection/envmap excluded; backside re-draw not double-counted; impostors/hair/ocean not included). Verified: 3× GET_PERF_DATA diffs identical in editor (101 rows) and test game (87 rows); island editor 4.21M / test game 5.33M polys; screenshot header "FPS: 63.8 POLYS: 5.3M". |
| **1.68 Scene::Intersects front-to-back early-out + hidden-object skip — THE test-game FPS-plummet fix (13.5→62.6 FPS at the user's spawn)** | applied 2026-07-31 evening (engine: wiScene.cpp Intersects(Ray) object loop restructured — phase 1 collects AABB-passing candidates with conservative world-unit ray-entry distances (slab test, NaN/inf→0 so an underestimate can only cost speed never a missed hit), sorted ascending, phase 2 breaks when `result.distance < next.t`; plus `!IsRenderable() -> skip` (DX11 fork parity "Do not Pick from hidden objects", nav-mesh filter exempt); plus RAYS2 diag counters (calls/objIter/aabbPass/tris/skinTris). Game same commit: SentRay4+ThreadSafe normalize direction + set TMin/TMax=fDistanceOfRay (rays were INFINITE — a "120-unit" LOS test traversed the whole island); WickedCall_BuildStaticMeshBVHs at level-load end (all static meshes get mesh.bvh — trees/huts/props; skinned + terrain-chunk skipped, engine maintains chunk BVHs itself); harness RUN_LUA/RAY_COST/LIST_SKINNED + RAYS/RAYS2 GET_PERF_DATA lines) | **User repro: start marker placed facing the village → test game 13.5 FPS, Logic - common_loop 93ms.** Root cause chain, all byte-measured: global.lua GetPlrLookingAt (button/pickup LOS) fires ~3 IntersectAll/frame → each = wiScene::Pick at ~26ms. Why 26ms: Scene::Intersects brute-forces EVERY triangle of every AABB-overlapping object along an UNCAPPED ray — and the scene has 50 villagers × 6 skinned parts ≈ 2.47M CPU-skinned triangles (SkinVertex ×3 per tri), plus a HIDDEN weapon mesh (W_MK19T) whose degenerate world-sized AABB (±100K all axes) taxed every ray from any direction, plus palm LOD0 canopies. Measured per up-ray pre-fix: 85 AABB passes, 98K tris (76K skinned) = 38-50ms; post-fix the same probe = 0.3ms, in-game LOS rays ~1ms avg, FPS 62.6 = the GPU wall. Verified correct: rays through actual villager bodies return their object numbers (the earlier "characters unhittable" scare was stale g_Entity coords in the probe, not the engine). Crosshair-hit + button-hover detection: mechanism proven working post-fix (hidden interceptors gone + cached results now refresh at full rate); user gameplay verdict pending. Residual known cost: a ray aimed into the villager crowd with no nearby blocker still pays 1-2 characters of CPU-skinned brute force (~15ms worst case) — skinned meshes have no BVH by design (bind-pose staleness); candidate follow-up: lowest-LOD raycast for skinned or capsule pre-gate. |
| **1.69 Texture-streaming observability counters + GAME-SIDE ENROLLMENT (streaming now actually ON for MAX)** | applied 2026-08-01 (engine: wiResourceManager.cpp `gg_dbg_stream_enrolled/replaced/resident_bytes/full_bytes/mem_permille` atomics in `wi::resourcemanager` — enrolled stored at job-gather, replaced counted at main-thread apply, bytes accumulated per streaming pass before desc mutation. Game same date: WickedCall_LoadImage passes `Flags::STREAMING` + container=real-disk-path for plain-DDS non-standalone loads (magic sniff BEFORE decrypt), new `bAllowStreaming` param default true, 4 non-material call sites opt OUT (sky map ×2, lens flares, HUD blood splats — their shaders never write mip feedback → engine would decay them to the 64KB floor = permanent blur); `g_bTextureStreamingEnabled` kill-switch + harness `SET_TEXSTREAM <0\|1>` + GET_PERF_DATA `STREAM:` line) | **2026-08-01 audit finding: Wicked's mip streaming ran every frame but streamed NOTHING — the enrolled set was EMPTY.** Enrollment is per-Load opt-in (`Flags::STREAMING`); only MaterialComponent::CreateRenderData adds it, which MAX uses solely for terrain/tree materials that explicitly `SetTextureStreamingDisabled(true)` (sync-load requirement). ALL entity textures route through WickedCall_LoadImage which passed only IMPORT_NORMALMAP → full mip chains uploaded at load, no VRAM adaptation, upload cost paid in full at spawn time. Now entity material DDS textures (12.5K files, most with full mip chains) stream: reduced initial upload, background thread streams mips in/out from GPU feedback, min-LOD fade hides pop. Gates kept honest: DDS-magic sniff before decrypt (streaming thread re-reads the file at mip offsets LONG after load — unsafe if the file is/becomes encrypted), standalone (gameisexe) excluded for now (re-encrypts files after load; follow-up option = IMPORT_RETAIN_FILEDATA to stream from RAM), engine auto-rejects single-mip/array/3D/<64KB. Resource-manager flag pinning means SET_TEXSTREAM changes need a level reload to take effect. The 1.41 descriptor-epoch + 1.44 reload-guard + 1.52 hijack-tripwire deltas were built for exactly this system and are now live-fire. **VERIFIED WORKING 2026-08-01 (island A/B, same spawn): VRam 10.11→8.81 GB (−1.30 GB), sys Mem 18.59→17.22, FPS 60.6 vs 59.7 (noise), visuals pixel-comparable — village cage_wood streamed to FULL 1024/11 minlod=0, door variant converged at 512 (its screen demand), off-screen ruins/cellar parked at 64KB floor; entering test game: replaced 77→146, resident 16→103→79 MB (demand-adaptive). Enrolled full set = 1336 MB → ~80-100 MB resident. FALSE ALARM AUTOPSY (do not re-chase): static-camera "frozen counters" = correct 2s demand equilibrium, NOT a stall — the hunt through copies/fb/req chain probes, job liveness (STREAM2), decision census (STREAM3) and DUMP_STREAM/DUMP_STREAM2 is all kept as permanent instrumentation. Content note: 39 entitybank DDS are single-mip (Aztec Witch 2048s etc.) — never stream AND stay full-size; authoring mip chains would enroll them.** |
| **1.70 VRAM census instrument + the two reductions it found (Island Showdown −1.75 GB, −20%)** | applied 2026-08-01 (engine: wiGraphicsDevice_DX12.cpp per-resource allocation registry — `GGVramRec` + `gg_vram_register/setname/unregister` hooked into CreateTexture / CreateBuffer2 / CreateRaytracingAccelerationStructure / `~Resource_DX12` / SetName, `GG_DumpVRAMCensus` + `GG_GetVRAMTotals`, swapchain + PSO-count counters; wiRenderer.cpp/.h `GG_GetSuballocatorStats`; **wiHairParticle.cpp `gg_hair_raytracing = false`** shrinks the per-system RT position copy to a 4-element scratch + CreateRaytracingRenderData refuses a scratch-sized BLAS. Game same date: **GGTerrain_part0.cpp `GGTerrain_EnsurePageAtlas()`** — the 9520² page cache AND the 32-slice source arrays move out of GGTerrain_Init into a lazy allocator called only from GGTerrain_Update's legacy tail, GGTerrain_DrawPages hard-guards on validity, GGTerrain_LoadTextureDDSIntoSlice validates-without-uploading when the destination array is absent; harness `DUMP_VRAM [tag]` + `VRAM:`/`SUBALLOC:` lines. Full findings + backlog in repo `VRAM_CENSUS.md`) | **Census validated: its total equals D3D12MA's own AllocationBytes byte-for-byte, so nothing is missed or double counted.** Findings on Island Showdown (6655 MB census / 8853 MB driver): grass strand buffers 2082 MB (31%), **GGTerrain custom-VT page cache 887 MB — DEAD** (shipping terrain is Wicked's native SVT; GGTerrain_Update returns before the only writer and every customDraw_* callback early-returns on `ggterrain_use_wicked_terrain`, which is 1 and is only ever flipped by the Y-key debug toggle), mesh suballocator 768 MB, SVT tile pool 768 MB (fixed 16384²×4, sparse), source texture arrays 650 MB (**427 MB of it the same dead path — the terrain DDS files were resident TWICE**), content textures 511 MB. **Zombie Cellar, an indoor level, paid the same ~2.5 GB of terrain machinery.** The grass finding: `vb_pos_raytracing` is a full second copy of every strand position (20% of each 59.5 MB system) consumed only by the hair BLAS, which is built only when the scene has a TLAS — MAX enables no RT feature, so it was written every frame and never read (~560 MB/frame of pure write bandwidth on the island). Gated off with NO shader change: the simulate CS still writes it, but out-of-bounds **typed**-UAV writes are discarded by hardware. **Measured Island Showdown in-game: census 6655→4930 MB, driver 8853→7064 MB (−1.79 GB, −20.2%), POLYS bit-identical (3,578,670), screenshot delta BELOW the animation noise floor (mean 2.52/765 vs 3.62 between two frames of one session), FPS 71.5→88.9 and editor 62.2→76.3 (bandwidth relief, treat as indicative). Terrain paint/sculpt/undo re-verified after the source arrays were removed.** |
| **1.71 Terrain SVT atlas height knob** | applied 2026-08-01 (engine: wiTerrain.cpp/.h `wi::terrain::gg_svt_atlas_height`, default stock 16384, consumed as `physical_height` at atlas creation; game bridge `GGSetSVTAtlasHeight` in master_part1.cpp driven by setup.ini `svtatlasheight`) | The SVT tile pool is a fixed 768 MB on EVERY level (measured invariant across all 17 sweepable demos), but only ~1000 of its 3844 tiles are ever resident. `svtatlasheight=8192` halves it: tiles 3844→1922, **−368 to −400 MB per level**, FPS unchanged, 44-46% tile headroom still free. **NOT default** — a static spawn cannot exercise eviction, so it needs a fast-travel soak on a big level before shipping. It is a setup.ini key rather than a harness command because the atlas is created on the first terrain update, before any harness command can land. |
| **1.73 THE texture-streaming load-crash fix: block-alignment in the mip reduction (+ a dead sentinel check, + crash-stack forensics)** | applied 2026-08-01 (engine: wiResourceManager.cpp — `GetFormatBlockSize` guard on BOTH halving loops, the load-time reduction in LoadResourceDirectly and the stream-out decay in UpdateStreamingResources, plus a no-progress `continue` in the latter; bounds guard + `stream_guard.txt` tripwire on the streaming job's re-read; `gg_stream_load_trace` per-load breadcrumb. wiGraphicsDevice_DX12.cpp — working sentinel check before the upload memcpy + `gg_upload_trace` footprint dump. Game same date: CrashLogger.cpp symbolized StackWalk64 + AV operation/address/page-state + faulting-thread id; harness `SET_TEXSTREAMTRACE`, `guard_rejects=` on the STREAM line; `g_bTextureStreamingEnabled` back to **true**) | **ROOT CAUSE of the crash that forced streaming OFF the same day it shipped.** `Trapped` and `RPG Template` died ~15 s into level load with an AV inside memcpy. It was NOT the streaming thread and NOT the decrypt/re-encrypt cycle — both were the leading suspects and both were wrong. The new symbolized crash stack put the fault in the **initial** upload (`LoadResourceDirectly` → `CreateTexture`), and the per-load trace named the texture: **DOOR1_surface.dds, 500×500 DXT1**. The mip reduction halves width/height with no regard for block-compression alignment; 500 is a multiple of 4 but **250 is not**, and a BC resource's TOP mip must be block-aligned (sub-mips are exempt, which is why the unreduced load was always fine). `GetCopyableFootprints` refuses such a desc and — instead of failing — writes `0xFFFF..` sentinels into every output. Upstream's only guard against that is `rowSizesInBytes[i] > (SIZE_T)-1`, which on a 64-bit build compares UINT64 against UINT64_MAX and **can never be true**, so `MemcpySubresource` looped `numRows = 4,294,967,295` straight off the end of the file buffer. Hence content-dependent: only levels holding a BC texture whose size is 4×odd trip it. Fix = stop the reduction before it would produce a misaligned top mip (such textures simply keep a larger base mip and save nothing), on both halving sites; the no-progress `continue` matters because without it a 500×500 texture rebuilds a byte-identical replacement every streaming pass (**observed `replaced=260468` on Trapped**). The sentinel fix is the backstop so no future bad desc is ever a memory fault again. **Verified: all 19 hub demos load clean with streaming ON, `guard_rejects=0`, zero `stream_guard.txt`.** |
| **1.74 Merged grass — one hair system per CHUNK instead of one per grass TYPE** | applied 2026-08-01, **DEFAULT OFF** (engine `a6cb310e`: ShaderInterop_HairParticle.h `GGHairGrassType` table + `GG_HAIR_GRASS_MERGED`; hairparticle_simulateCS adopts the per-cell type instead of killing the strand, per-type length/width/stiffness/drag/viewdist, type carried in `vb_nor.w` as `(type+1)/127.0` — SNORM8 round-trips integer steps exactly; four hair PS read it back. Game `8fd09cb7`: `gg_grass_merge` + `ProcessGrassChunkMerged`, harness `SET_GRASSMERGE` + `GRASS_CHUNKS` per-chunk type histogram) | Premise was MEASURED first, via the new histogram: 10.4× merge headroom available, not the 4× assumed. Result on the pink-flower benchmark: 52→5 hair systems, 4.216M→418K strands, **−1567 MB, FPS 60→75**. **But it FAILS the density gate** (`GRASS_BENCHMARK.md`): coverage 10.96 % vs 9.40 % (limit ±0.15). clumpCV is clean, so this is uniform OVER-density, not the 2026-08-01 clumping failure mode. Eliminated: texture aspect, absent-type rendering. Fixed but insufficient: strand-LOD viewDistance coupling. **Next step is an instrument, not another theory — read back the hair indirect-draw args for the real drawn index count per system in each mode.** Task #52. |
| **1.75 transparent-shadow RGBA8 rejection** | applied ~2026-07 (ledger row added retroactively 2026-08-05) | Row was missing from this table (the 1.74→1.76 gap) — flagged by the 2026-08-05 drift audit. The change itself has been in the clone since July; see engine commit `94b*` for the diff. Recorded here so an upstream re-apply does not drop it. |
| **1.76 SVT atlas default 16384 → 12288** | applied 2026-08-02 (engine `90375285`: wiTerrain.cpp `gg_svt_atlas_height` default; game `0daf5692` DOCDOC) | Soak-backed: peak tile demand across four levels is 1864–2001, and 12288 supplies 2852 tiles (30–35 % headroom) versus 16384's 3844. Saves ~150–300 MB on EVERY level. **8192 IS DEAD** — 1922 tiles is below Aztec's 2001 peak, free hits 0 and terrain blurs. Judge from `VT: free=`, never from screenshots (cross-run diffs are animation-dominated). USER-CONFIRMED 2026-08-02 at high camera speed on Canyon, Jungle Fever, Aztec. |
| **1.77 Object-PSO permutation trim + the instrument that found it — −624 MB of driver VRAM on EVERY level** | applied 2026-08-02 (engine: wiRenderer.cpp `gg_pso_trim` gates the mesh_shader axis on `MESH_SHADER_ALLOWED` as well as the GPU capability, and caps the cullMode axis at 2; wiGraphicsDevice_DX12.cpp census header gains `pso_creates/pso_compiles/descheap_bytes/cmdalloc` + `GG_VRAMStage()` driver-usage marks. Game: harness `VRAM_STAGE <label>`) | Driver-reported usage ran **1.16–1.61 GB above the resource census on every demo**, content-independent, and nothing in the census could explain it. Rather than theorise again, four counters went into the census header — and named it in one run: **13,976 pipeline state objects**, each created with a `renderpass_info` (so a real `ID3D12PipelineState`, driver memory the census structurally cannot see) at ≈96 KB apiece. Descriptor heaps were 30.5 MB and command allocators 90 — both ruled out by the same reading. Two permutation axes were UNREACHABLE by `GetObjectPSO`: mesh_shader (gated on GPU capability, but selection uses `IsMeshShaderAllowed()`/`MESH_SHADER_ALLOWED`, which MAX never sets) and cullMode 3 (the variant field is only ever filled from CullMode NONE/FRONT/BACK; the 4th value duplicates BACK). **13,976→7,496 PSOs; Switch Escape driver 4452→3828 MB, hub 4270→3774 MB, resource census unmoved (2599→2606) — which is the proof it was pipelines.** Verified no dropped draws: POLYS bit-identical to the pre-trim sweep. Follow-ups costed in `VRAM_FLOOR.md`: the tessellation and voxelize axes are also dead in MAX (~100 MB each) but `SetTessellationEnabled(false)` runs AFTER `LoadShaders`, so the flag must move earlier first. |
| **1.78 Transparent shadow atlas DROPPED (batch 1 of the 4 GB preset work)** | applied 2026-08-02 (engine `8acce73f`: `gg_transparent_shadows` defaults false, atlas never created, shadow render pass loses its colour target, object RENDERPASS_SHADOW PSOs built with `rt_count = 0`; game `81224917`: corrected the stale UI-audit comment) | Product decision — the `SET_TRANSPARENTSHADOWS` A/B built to answer "does this atlas earn its memory" answered no. It was pure floor cost: **160 MB on every level, 520 MB on Bounty / Foggy Forest (16384×4096 packers) and on Amazon / Disruption**. Safe by construction, not by luck: with the atlas invalid `UpdateRenderData` binds `texturehelper::getWhite()` (1,1,1,1) and every shader site does `pcf *= transparent_shadow.rgb` — a multiply by white — whether or not `TRANSPARENT_SHADOWMAP_SECONDARY_DEPTH_CHECK` takes the branch. The object shadow PSO is the ONLY shadow-pass pipeline that bakes a `renderpass_info`; hair, emitters, impostors and `PSO_shadowClear_GG` all use the deferred two-argument `CreatePipelineState` and follow the depth-only pass automatically. **Verified 4 demos: POLYS bit-identical (Island 4115636, Cellar 28048), `transparent_shadow_batches` 0, no `shadowMapAtlas_Transparent` census record, depth atlas intact, screenshots unchanged.** Island isolated −160.0 MB; cumulative with 1.77 — Bounty 5383→4167, Foggy Forest 6469→5253, Cellar 4671→3808. **Runtime `SET_TRANSPARENTSHADOWS 1` now only re-enables the DRAWS** — a real re-enable needs the engine default flipped plus a restart, because the PSO latches at LoadShaders. |
| **1.79 Lazy object PSOs (batch 3 of the 4 GB preset work) — another −633 MB, 6337 driver pipelines → 1** | applied 2026-08-02, **DEFAULT OFF** (engine `3666bbf7`: `gg_pso_lazy_object` + `wiRenderer.h` extern + `gg_dbg_pso_driver_eager` counter; game: `GGSetLowVRAM()` sets it, and `GetSetupIniEarly()` reads the key early enough) | Passing nullptr for `renderpass_info` puts object PSOs on the backend's existing deferred path — `pso_validate` builds the real `ID3D12PipelineState` at first BIND with the live pass's formats/sample count and caches it in `pipelines_global` keyed by `{pso, renderpass_hash}` (topology is set there too). A supported mode: `PSO_object_wire`, hair, emitters and impostors already use the two-argument form. **Island Showdown, one binary: driver 4312.7→3679.8 MB (−632.9), eager pipelines 6337→1, lazy compiles 22→57, POLYS 4115636 bit-identical, FPS 63.6→63.2. Z Island with the full preset: 8998.8→8302.1 MB, POLYS identical, FPS 70.4→80.4.** 57 pipelines is ~1 % of what was built eagerly. **TWO TRAPS, both caught by measurement:** (a) `gg_dbg_pso_creates` counts CreatePipelineState CALLS, which stay at 7496 in lazy mode by design — the first A/B read it, saw no change, and would have concluded the feature did nothing; hence the new `pso_driver_eager` counter. (b) the flag must be set before `LoadShaders`, and `FPSC_LoadSETUPINI` runs LATER — wiring the key only there was measurably inert, so `GetSetupIniEarly()` (called from `main()`) reads it too. Consequence: **`SET_LOWVRAM` cannot enable this half**, only setup.ini can. Default off because the cost is a first-use compile hitch per pipeline. |
| **1.80 Tier A3 + A5 — suballocator granularity and two dead PSO axes** | applied 2026-08-02 (engine `e0287386`; game `e1b437a4` moves `SetTessellationEnabled(false)` into `main()`) | **A3**: `GPUSubAllocator::blocksize` 256→128 MB. The pool only grows in whole blocks, so granularity is pure rounding loss on the last one — the 19-demo audit found small levels holding 512 MB with ~310 used. Switch Escape now takes 3×128 = 384 (−128); levels needing ~470 take four blocks and are unchanged. Note the `>blocksize/2` guard: allocations over 64 MB now bypass suballocation and become standalone buffers — same total memory, unpooled. **A5**: skip the tessellation and `RENDERPASS_VOXELIZE` object-PSO axes, **gated on the LIVE flags** (`tessellationEnabled`, `VXGI_ENABLED`) not a hardcoded assumption, and `LoadShaders` re-runs on shader reload so enabling either feature restores the pipelines. MAX disables tessellation outright ("has never worked") and never enables VXGI. **Eager driver pipelines 6337→4033 (−36 %).** Needed the tessellation flag moved into `main()` — master_part1 set it long after LoadShaders had built the pipelines, the **third** time that ordering trap has bitten. Measured with game A2: TESTPRO1 5710→5390, Island Showdown 4145→3841, Foggy Forest 5037→4701, Switch Escape 3780→3116; **POLYS bit-identical everywhere**, TESTPRO1 passes the density gate (coverage 9.486, clumpCV 1.103 vs ref 1.104). |
| **1.81 Scene::Update caller tracer (instrument)** | applied 2026-08-02 (engine `ad8fa350`; game `991e8329` harness `DUMP_SCENEUPDATE`) | The perf panel shows every `Scene::Update`-internal range at `(3x)` and reading the code could not settle which callers those were — two of the three game-side `scene.Update(0)` sites turned out to be one-off setup. Each call now records `_ReturnAddress()` (symbolized game-side with dbghelp, already present for the crash logger), the **Scene instance** (which separates "one scene updated three times" from "three scenes updated once"), the frame and dt. **Verdict on Canyon Offensive, editor AND test game: 96 records over 96 frames, 1 call/frame. `Scene::Update` runs exactly once per frame — there is no duplicated scene work.** Four independent static agents agreed: the only per-frame path is `wiApplication.cpp:444 activePath->Update` → `RenderPath3D::Update:384` → `scene->Update`. **What `(Nx)` actually means:** `BeginRangeCPU` hash-probes for a free slot ("If one range name is hit multiple times, differentiate between them!") and `EndRange` does **not** clear `in_use` — only the profiler frame update does. So `(Nx)` is entries between two resets and the ms is their SUM, i.e. **an N-frame total that must not be compared to the single-frame `CPU Frame` row**. |
| **1.82 LAZY OBJECT PSOs BECOME THE DEFAULT + the hitch instrument that justified it** | applied 2026-08-03 (engine; game harness `HITCH_RESET` + `HITCH:` line) | `gg_pso_lazy_object` flips to **true for everyone**; revert with setup.ini **`lazypso=0`**. It shipped in 1.79 behind `lowvram=1`, which meant the 8 GB majority kept paying ~390 MB for pipelines they never bind, so the only real question was the first-use compile hitch — and that could not be answered by anything already in the build. The 100 ms wall-gap tracer never fires on a 1-30 ms stall, and an FPS mean smears 26 compiles across 600 frames into nothing. So: **`gg_dbg_pso_compile_max_us`** (the WORST SINGLE compile — the sum is not what a user feels) plus a **per-frame histogram** on the gap tracer's always-on path, buckets >16.7/25/33/50/100 ms with a reset, read via `HITCH:` in `GET_PERF_DATA`. **Verdict on the new TESTPRO1, 5 cold runs, three windows each (since-launch / first 25 s of the level / 8-waypoint travel): the hitch does not exist.** Lazy costs **26 extra compiles totalling ~10 ms across an entire session, worst single compile 1.1 ms** — under a tenth of one 60 FPS frame — and every one of them lands during a level load already dominated by a 13-SECOND stall. The first 25 s of play recorded **`psoC=0` in every run, both configs**; travel added 5 compiles / 1.6 ms. **`pso_driver_eager` 4033 → 1.** Driver VRAM **−392 MB**, reproducible to 0.2 MB on the lazy side across runs, and 4032 × 96 KB ≈ 387 MB confirms the per-pipeline arithmetic. **POLYS bit-identical in every window of every run** (3165848 settled, 2669257 travelled) — the acceptance test, since a missing pipeline makes `RenderMeshes` skip the draw. Screenshots identical. **Measurement trap caught by repeating**: the first A/B showed lazy with 98 frames over 25 ms against the control's 1, which reads as a smoking gun — but the causal counter said `psoC=0`, and on repeat the *control* swung 1 → 87 while lazy stayed 98 → 101 → 2. Pure run variance on a scene whose frame time sits near the bucket edge; the same ±8 FPS launch-to-launch swing already on record. **One counter reading zero was worth more than a plausible 98× difference.** Startup is unchanged (launch→hub 11 s, launch→editor 38-40 s in both) — eagerly building 4033 pipelines was never measurably slow, it was only expensive in memory. |
| **1.83 D3D12MA block size 64→16 MB, and the padding attribution that found it** | applied 2026-08-03 (engine; game harness `DUMP_MAPAD`) | The audit's `pad` column (`d3d12ma_blocks - census_bytes`) averaged 363 MB across 19 demos and had never been broken down. `DUMP_MAPAD` now dumps per-heap-type `CalculateStatistics` **plus D3D12MA's own detailed JSON block map**, and it corrected two things at once. **First, the column was measuring the wrong memory.** `d3d12ma_blocks` sums BOTH segments — L1 video and L0 system, where UPLOAD/READBACK staging lives — while `driver_usage` is video-only. So `pad` charged system-RAM waste to the video budget and `nonres` (`driver_usage - blocks`) subtracted system blocks from a video figure, understating it by the same amount. On TESTPRO1, `pad` read 257 MB of which only **152 MB was ever video memory**; true non-resource was **483 MB, not the 259 the old arithmetic gave**. The two errors cancel in the SUM (the `blocks` term drops out), so the audit's ~3.3 GB bottom line stands — but neither column alone meant anything. The census header now carries `d3d12ma_allocated_video` / `d3d12ma_blocks_video` so no consumer can mix segments again, and `tools/vram_audit.sh` uses them (flagging pre-1.83 censuses as mixed). **Second, the padding was mostly avoidable.** The JSON showed 24 pooled 64 MB blocks and 61 committed allocations — committed ones pad **exactly zero** (3688 MB of them, including the 576 MB SVT pool), and essentially all the waste sat in **4 sparse blocks**, one holding just *two* allocations in 64 MB. D3D12MA's `PreferredBlockSize` had been left commented out, so the library default of 64 MB applied. Measured, four sizes, repeated: **64 → 104/152/152 MB padding · 32 → 130 · 16 → 11.5/11.6/11.6 · 8 → 1.7**. **16 MB is the pick: −92 to −140 MB of video memory on every level, with non-resource driver memory flat (483.4 → 483.7) — the 41 extra heaps cost nothing.** FPS flat (65.6-66.1 across every config), **POLYS bit-identical (3,165,848) in all eleven runs**, screenshots clean. 16 is also far *more stable* than what it replaces: three runs gave 11.5/11.6/11.6 while 64 MB swung 104-168. 8 MB buys only 4.5 MB more while non-resource starts climbing — the heap-count cost appearing. **32 being worse than 64 is not a typo**: D3D12MA sends any allocation over half a block to its own committed heap, so the threshold reshuffles which resources pool. Revert with setup.ini **`mablockmb=64`**. **Ordering trap, fourth occurrence:** the knob was first wired through the game's `GetSetupIniEarly()` and measured **inert** — D3D12MA kept reporting 64 MB, because the allocator is built with the device, before `main()` gets there. The engine now reads the key itself at `CreateAllocator`, so there is no ordering dependency left. Caught only because the JSON echoes `PreferredBlockSize` back; the padding numbers had *moved* between those runs (104 vs 152) and would have read as a small win. **Evidence is TESTPRO1-only so far.** |
| **1.85–1.95b merged-grass flicker probe suite** (instruments — BUT NOTE: **1.86 inside this family is a real CONTENT change, the grass over-density fix, not an instrument** — drift audit 2026-08-05) | applied 2026-08-03/04 | The bitmask probe family behind harness `SET_GRASSTYPEFREEZE`: `1` freeze whole type path · `2` present · `4` textureIndex · `8` length · `16` grasstype visualisation PS · `32` stable `DTid.x%8` hash instead of the resolved type · `64` ALWAYSWRITE (suppress cull early-return) · `128` uniform-index CB resolve loop (needs `xHairGrassTypeCount`, new CB field in the old `xHair_padding_alt1` slot) · bits 8-15 `(k+1)<<8` force EVERY pixel's texture lookup to type k. Plus 1.85 hair-kill tracer (`DUMP_HAIRKILL`) and the game-side `DUMP_GRASSTYPES` (captured per-type table vs live descriptor re-resolve). All diagnostic-only, all default-off, all instant (CB flags, no reload). These are the instruments that killed fourteen wrong theories and cornered the real one — see the 1.96 row and `VRAM_CENSUS.md` for the full ledger. |
| **1.96 THE MERGED-GRASS FLICKER FIX — dxc dropped the NonUniformResourceIndex annotation at a function boundary** | applied 2026-08-04 | **Root cause:** `GGGetGrassTexture` returned a `Texture2D` through an `out` parameter with `NonUniformResourceIndex` applied inside the helper (1.89). `dxc -dumpbin` on the shipping `hairparticlePS.cso` showed **every `createHandle` carrying `nonUniformIndex=false`** — the annotation does not survive a resource handle crossing a function boundary through a local/out-param. So the divergent bindless `Sample` ran as undefined behaviour: the driver readfirstlaned the descriptor and whole pixel-waves sampled ONE strand's blade texture, re-rolled every frame by rasterizer wave packing = scene-wide square-patch flicker (merged 12.0-12.6 meanAbsDiff vs per-type 0.22-0.58). It also explains why 1.89 cost no FPS: the predicted waterfall was never generated. **Fix:** helper now returns the descriptor INDEX (`GGGetGrassTextureIndex`) and all four PS variants (lit, prepass, prepass_depthonly, shadow) build the sample as ONE expression — `bindless_textures_half4[NonUniformResourceIndex(descriptor_index(gidx))].Sample(...)` — the pattern dxc guarantees. **Verified three ways:** DXIL now shows exactly one `i1 true` handle per variant; plain-control churn 12.0-12.6 → **0.45-0.52** first burst, **0.11-0.15** repeat (below the per-type floor); FPS 82.0 vs 82.5 baseline. **Density gate PASS:** coverage 4.72→4.97% (prepass silhouettes now cut from the CORRECT sprite), clumpCV 1.02→1.08, near bands unchanged. **Rule this hardened: any NonUniformResourceIndex change MUST be verified with `dxc -dumpbin` — the 1.89 'fix' was measured as a null and wrongly ELIMINATED the true mechanism because the annotation silently vanished.** Also corrected: the 1.92 SNORM→UNORM16 move stays (latent precision fix) but its 'THE FLICKER FIX' comments were false — measured unchanged. |
| **1.97 VRAM misc levers: visibility-payload skip + lazy debugUAV + cloud-noise free path + CopyAllocator trim** | applied 2026-08-04 | Four independent reductions from the misc-bucket autopsy, verified together on Z Island (driver 3859→3804, POLYS bit-identical, census shows zero `res.texture_payload_*`, zero `debugUAV`, zero retained `CopyAllocator::uploadBuffer` rows after settle). (a) `CreateVisibilityResources` takes `include_payload` (= `visibility_shading_in_compute`, the only consumer of payload_0/1 via Visibility_Surface/Shade); SSR-only levels save ~39 MB, recreate-on-flip guard in PreRender. (b) `debugUAV` (viewport RGBA8, ~5 MB) created on demand in PreRender when light-culling debug / VRS-classification debug / SurfelGI is on (SurfelGI coverage CS binds it unconditionally, hence included), freed when all off; the two formerly unconditional barriers now check IsValid(). (c) cloud noise statics (shapeNoise/detailNoise/curlNoise/weatherMap, ~5.4 MB) freed when `!IsVolumetricClouds()`, `volumetric_clouds_precomputed` reset so re-enable regenerates; all bind sites verified clouds-gated. (d) CopyAllocator freelist aged per frame (≥32 MB idle >64 frames, anything idle >600), stamps in submit(); plus pow2 rounding softened to 16 MB multiples above 64 MB (the 134.5 MB sky upload used to mint a 256 MB buffer). ~250-283 MB SYSTEM RAM returned per level; entries are GPU-idle by construction (submit blocks on the copy fence before repooling). |
| **1.98 A4: terrain SVT emissive map dropped (pool 576→480 MB every terrain level)** | applied 2026-08-04 | MAX assigns only basecolor/normal/surface on terrain materials (GGTerrainWicked.cpp) but the engine allocated a 4th sparse EMISSIVE map + its pool share unconditionally (BC1 = 1/6 of pool = 96 MB at the shipping 12288 atlas). All four map loops now skip it; the material bind loop binds an EXPLICIT `wi::texturehelper::getBlack()` 1x1 with residency/feedback descriptors -1 — the 2026-08-02 attempt left the slot UNBOUND and the whole scene washed to white (undefined sparse sample → tonemapper). Verified on A Grand Canyon Adventure: pool 480.0 exact, atlas sparse textures 8→6, POLYS bit-identical, screenshot NORMAL (terrain/sky/exposure correct). Tile-coordinate allocation & aging untouched (the 2026-08-02 soak already proved residency identical with these same guarded loops — the failure was shading, now fixed). Revert: setup.ini `svtemissive=1` (level reload). |
| **1.99 CPU vertex-skinning memoization in Scene::Intersects ray path** | applied 2026-08-04 | A skinned mesh has no BVH, so a ray entering its AABB brute-force tests every triangle and shared vertices were re-skinned once per referencing triangle corner (~6x per vertex, 4-8 matrix ops each). One pickup-prompt crosshair ray through a corpse measured ~18 ms — the "look down at a dead character" FPS plummet (96→21 FPS with the unthrottled double-ray LUA bug on top). Generation-stamped thread_local scratch skins each vertex once per candidate: measured 17.9→8.7 ms/ray on the same corpse, no semantic change, POLYS untouched; benefits every LOS/gunfire/pickup ray against characters. Paired game-side fix: module_misclib.pinpoint now casts ONE shared crosshair ray cached 150 ms (was per-collectible per-frame, PLUS a second ray per miss frame via an operator-precedence slip). Verified at the repro: FPS 21.1→92.2, rays 42/s→6.4/s, RAYS2 counters. |
| **2.00 GameGuru WPE emitter re-port (wiEmittedParticle + emit/simulate shaders + renderer cull gate)** | applied 2026-08-04 | The DX11 fork (`D:\max\WickedRepo`) carried ~35 GameGuru-private emitter members inside a commented `//#ifdef GGREDUCED` block; the DX12 API migration (`d3ae5996`) dropped every one of them, and with them the entire WPE particle system. Re-added to `wi::EmittedParticleSystem`: `fadein_time`, `endcolor_*`, `normal_factor_x/y/z`, `burst_factor_x/y/z`, `burst_factor_speed`, the four independent randomisers (`normal_random`/`rotation_random`/`size_random`/`scaling_random`) that upstream conflates into the single `random_factor`, `start_rotation`, `random_position(_scale)`, `startpos`, `burst_amount/split/delay`, `spawn_random`+`randemit`/`randpause` gust model, `total_emit_count`, `distance_sort_bias`, `bVisible`/`bActive`/`bFindFloor`/`bFollowCamera`, and the auto-deactivate that lets fire-and-forget decal effects stop costing anything. **`FLAG_EMIT_PAUSE` moved to bit 10** — the fork used bit 7, which upstream now uses for `FLAG_COLLIDERS_DISABLED`; the .PE loader remaps it. 16 CB fields added to `ShaderInterop_EmittedParticle.h`; `emittedparticle_emitCS.hlsl` gained the spawn offsets and per-axis velocity terms; `emittedparticle_simulateCS.hlsl` gained the DX11 alpha formula (`saturate(linearFade * baseColor.a)` THEN the fade-in ramp — nesting is load-bearing, shipped content sets baseColor.a up to 20) and colour-over-life. `wiRenderer.cpp` emitter cull restored to honour `IsVisible()`/`IsActive()`. Non-legacy emitters are unaffected: `fadein_time` defaults to -1, which keeps the stock opacity-curve path. Verified live on TESTPRO1: SCENE_EMITTERS 0→95, 'Skipped non-Wicked archive' log events 47→0, no FPS regression (83→87). |
| **2.00b WPE: Component_Attach local-space + Burst(0) semantics + verbatim UpdateCPU emission** | applied 2026-08-04 (engine `8b5b236a`) | Three DX11 divergences fixed together after the "preview shows nothing" hunt. (a) The legacy .PE loader now calls `Component_Attach(child, parent, child_already_in_local_space=TRUE)` — archive transforms are already parent-local; the default multiplied them by `inverse(parent.world)`, which cancels at load but bakes a huge offset the moment the root is repositioned (shipped .PE roots carry leftover authoring positions like z=-18265, so previews rendered ~18k units away). (b) `Burst(0)` restored to mean "fire burst_amount" (`if (num <= 0) num = burst_amount`), which every editor-preview/LUA/weapon/decal call site relies on — on stock upstream every burst-only effect fired nothing. (c) `UpdateCPU` emission is the fork’s verbatim model: spawn_random gust (randemit/randpause) + fractional burst release (burst_split, burst_delay in MILLISECONDS); reduces to upstream behaviour when both are 0. |
| **2.00c WPE: material alpha applied ONCE, not squared** | applied 2026-08-04 (engine `d5220b88`) | `emittedparticle_simulateCS.hlsl`: the emit shader already bakes `material.baseColor.a` into `particle.color`, and the simulate shader’s `opacity` term contained it AGAIN, finishing with `particleColor.a *= opacity` — alpha-squared. Shipped `baseColor.a` 0.33 rendered at 0.11 (a 3.03× under-render, user-visible as "particles look ~50% too weak"). DX11’s VS ASSIGNS the alpha byte (`emittedparticleVS.hlsl:103`), so the fix is assignment on the legacy path: `particleColor.a = opacity;`. Exact for every effect regardless of its authored alpha; a blanket 2× gain would only have been right for one of them. |
| **2.01 EmittedParticleSystem::Serialize round-trips the GameGuru emitter fields (emitters component version 2 → 4)** | applied 2026-08-05 | **The 2.00 re-port added ~35 GameGuru members to the emitter but never taught the serializer about them, and `Scene::Entity_Duplicate()` duplicates an entity by serializing it.** `preload_wicked_particle_effect()` (`M-Entity_part5.cpp:27-136`) builds a 5-slot `ready_decals[decal_id][]` clone cache per decal exactly that way — load the `.PE` once, `Entity_Duplicate` the root four times — so **clones 1-4 came back with every GameGuru field at its struct default, `burst_amount` = 0 above all. `Burst(0)` resolves `num = burst_amount`, so those four cache slots emitted NOTHING while still consuming their turn in the round-robin.** User-visible as "shoot a material and you cannot get a second effect until the first one finishes": only one slot in five actually fired, and DX11-style overlapping repeat hits were impossible. The fix serializes the same set the DX11 fork did (`D:\max\WickedRepo\WickedEngine\wiEmittedParticle.cpp:953-1003`, its 5072-5077 version bands), minus the three `wpe_filler_*` placeholders and `spawn_pause`/`spawn_pause_random` which have no member in this struct. **Guarded on the emitters component-library version, bumped 2 → 4 in `wiScene.h`** — **3 is deliberately skipped, upstream Wicked already took emitter version 3 for its `burst_on_create` field, which this clone does not carry; reusing 3 would make a future upstream pull silently misread whichever side wrote the file** — that version is stored per component in the file, so scenes written before this change read exactly as before and keep the defaults; the shared archive ceiling is untouched. **A/B verified with the new harness command `WPE_CLONETEST`**, which loads a `.PE` and duplicates it four times exactly as the preload does, then diffs every field: guard disabled → master `burst_amount` 2.93 vs clones 0.000, FAIL 4/4 on impact, blood and explosion; guard enabled → 0/4 differ, PASS on all three. **Rule: any field added to a component struct must be added to that component’s Serialize as well — Entity_Duplicate is a serialization round-trip, so an unserialized field silently becomes a default on every clone.** |
| **2.02 hardening batch from the 2026-08-05 overnight audit: GGMAX flag bits moved to a reserved range + dead null-deref removed** | applied 2026-08-05 | (a) **`EmittedParticleSystem::FLAG_EMIT_PAUSE` moved 1<<10 → 1<<28 and `ObjectComponent::OCCLUSION_QUERY_DISABLED` moved 1<<11 → 1<<28.** Both sat on their enum’s next sequential append slot, and upstream grows BOTH enums by sequential append (ObjectComponent gained bits 8/9/10 in 2023-24, verified on upstream/master, whose enum still ends at bit 10 — our bit 11 was exactly its next slot). A future pull would have silently collided. **CONVENTION ESTABLISHED: GGMAX-added bits in upstream-owned FLAGS enums live in the reserved range 24-31, never the next sequential slot.** Safe because MAX never persists _flags to disk (no .wiscene save path; the .PE loader assigns FLAG_EMIT_PAUSE symbolically when remapping the fork’s bit 7). (b) `wiRenderer.cpp` emitter-simulate loop: removed a dead `*transforms.GetComponent(entity)` dereference — the variable was unused and an emitter entity without a TransformComponent made it a latent per-frame null deref. Game-side siblings in the same batch (game repo): .PE loader rejects self-parenting/cyclic hierarchy rows (Release strips `Component_Attach`’s assert and the stock `RunHierarchyUpdateSystem` ancestor walk has no cycle cap — a corrupt row = permanent render-thread hang) and clamps `maxParticles` to 262,144 (raw uint64 off disk sizing eleven GPU buffers; largest shipped effect asks for 25,000). Empirical gate: 7-case .PE fuzz barrage (truncations, hostile 0xFF/random bodies, tail chop) — all rejected or degraded gracefully, editor alive and functional after each. |
| **2.02b ApplyTransform garbage tripwire capped at 64 events/session** | applied 2026-08-05 (engine `0f6165a7`) | The parrot-hunt tripwire in `TransformComponent::ApplyTransform` had NO cap: forensics showed 13,086 logged events since 2026-07-18 (~30-800 per level load, ALL from `WickedCall_LoadNode` composing sheared model-node transforms, all healed by the decompose guard, zero runtime events) = a synchronous fopen/append storm on every load + 12 MB of repeating text. First 64 events/session keep full symbolized diagnostics, then one SUPPRESSED line; the heal itself is untouched. NOTE: the file lands in the game’s `Files/` CWD, not next to the exe — check there. Verified: one TESTPRO1 load = 34 entries / 31 KB, grass+hair state identical. |
| **2.02c per-renderpass user-stencil draw counters (outline diagnostics)** | applied 2026-08-05 (engine `84b1e787`) | `gg_dbg_userstencil_draws[]`/`gg_dbg_total_draws[]` in `RenderMeshes` + global bridges for harness `DUMP_OUTLINE`. Two relaxed atomics per instanced batch. Built during the selection-outline restoration (game `a0847fc1` — the DX12 port had the whole outline pipeline as dead code because DX11’s two ENGINE hook sites, fork `RenderPath3D.cpp:1630` + `RenderPath2D.cpp:337`, were dropped in the migration; the DX12 fix is game-side only, using `customDraw_Compose` + a `MasterRenderer::Render` tail call). These counters proved the stencil pipeline was healthy while the test bed was selecting off-screen entities — see WETEST.md’s Selection-Outline section for the select-by-index trap. |
| **2.03 device-removal forensics + fail-clean guards (PLAY GAME crash cascade 2026-08-05)** | applied 2026-08-05 | Built for the Aztec Teaser PLAY GAME cascade (DEVICE_HUNG during LOADING LEVEL at 20:13, then two null-deref AVs on relaunch while the driver was still recovering). (a) **DRED pass names**: when `gg_dred_armed`, `EventBegin`/`EventEnd`/`SetMarker` emit raw embedded ANSI markers (`ID3D12GraphicsCommandList::BeginEvent(1,...)`) instead of WinPixEventRuntime PIX3 blobs - DRED's breadcrumb-context decoder only reads the legacy embedded form, so dred_report.txt now names the pass around every op (the 20:13 report had bare op types). Also fixed the dump's context-gathering loop to scan ALL context array slots (it started at `firstOp`, but array slots are not op indices). (b) **Constructor fail-clean**: all 24 `wilog_messagebox + wi::platform::Exit()` failure sites in the GraphicsDevice_DX12 constructor now call `gg_fatal_device_init()` (backlog + `ExitProcess(1)`) - `wi::platform::Exit()` is just PostQuitMessage and RETURNS, so a failed `CreateCommandQueue` fell through to `queue->SetName` on a null ComPtr = the user's silent crash at 20:14:23 (wiGraphicsDevice_DX12.cpp:2963, called from SetWindow). (c) **DescriptorAllocator::block_allocate guard**: failed `CreateDescriptorHeap` (device removed mid-load) routed to `OnDeviceRemoved()` (writes the DRED report, mutex-serialized with the removal-callback thread) + `ExitProcess(1)` instead of dereferencing the null heap (the 20:14:39 AV inside CreateTexture during MasterRenderer::Load). |
| **2.04 SVT tile-render material_index bounds clamp (DEVICE_HUNG root cause)** | applied 2026-08-05 | The Aztec PLAY GAME hang (6/6 soak repro, page fault always in the same heap, freed-match mat18 textures, COMPUTE list hung ~op 100 of ~550 = UpdateVirtualTexturesGPU Render Tile Regions). Root cause: the per-layer material_index buffer is built at record time from scene->materials.GetIndex(materialEntities[i]) with NO validation - (1) an entity without a live MaterialComponent yields SIZE_MAX -> 0xFFFFFFFF -> the CS reads a ShaderMaterial miles out of bounds -> garbage texture descriptor -> deterministic GPU page fault; (2) baseMaterialCount comes from the CHUNK blendmap array size, so a chunk carrying more layers than materialEntities.size() made materialEntities[i] an OOB vector read before the entity check even ran. Both holes clamped to material 0 with a one-shot backlog line naming the slot/entity. Game-side structural fix in the same session (GGTerrainWicked SetupWickedTerrainMaterials): WaitForGPU before the set swap + truncate materialEntities to the new set and Entity_Remove the stale tail (a previous set with more painted materials left dead-entity tail slots that regenerated chunk blendmaps still spanned - the mid-play trigger as VT residency reached those layers). See game repo PLAYGAME_CRASH_2026-08-05.md for the full forensics. |
| **2.05 device-hang hunt diagnostics: keep-alive bisection knobs (flag-gated, default OFF)** | applied 2026-08-05 | Three flag-file knobs used to NAME the Aztec PLAY GAME DEVICE_HUNG's faulted resource by elimination: (a) with DRED armed, `RenderPath3D::DeleteGPUResources` retains every generation of the depth-history chain (Round A: hang persisted - depth exonerated, and proved the DRED freed-match names are heap corpses that rotate with allocation layout); (b) `leakall.txt` makes `AllocationHandler::Update` retain EVERY deferred destroy (Round B: hang GONE - faulter reads a legitimately freed object); (c) `leakterraintex.txt` makes wiResourceManager pin only `terraintextures/*` resources (Round C: hang GONE with everything else freeing - **faulted resource NAMED: the terrain material DDS set, freed at the level-load swap, sampled by the SVT Render Tile Regions CS**). ~85%%-per-cycle hang -> 0/9 clean cycles across B+C; the 2.04 clamp never fired (indices valid - staleness is a GPU-side ShaderMaterial texture descriptor) and WaitForGPU at the swap did not help (persistent data, not in-flight work). Ship fix is game-side (GGTerrainWicked `SetupWickedTerrainMaterials`): double-buffered retention of the outgoing texture set until the next swap. Knobs left in place default-OFF for future hunts. Full forensics: game repo `PLAYGAME_CRASH_2026-08-05.md`. |
| **2.06 shadow-pack diagnostics (DUMP_SHADOWRECTS bridge)** | applied 2026-08-05 | `GG_GetShadowRects` exposes the packer's final iterative_scaling + every packed light's rect (entity/type/w/h) for the harness. Built for the Snowy Mountain Stroll spot-flicker hunt; it EXONERATED the packer (scale 1.0, rects stable at every pose) and pointed the hunt at the Forward+ tile culling, where the real bug was GAME-side: the DX11->DX12 port renamed old LightComponent::fov (FULL cone angle) to outerConeAngle (HALF angle) without halving - spots ran at double their authored cone and anything authored >=90 deg went degenerate (negative cone cos -> broken tile-culling sphere r=range*0.5/cos^2 + invalid shadow fov=outer*2). Fix in game wickedcalls_part3 WickedCall_UpdateLight (halve + clamp 1..85 deg). Side finding recorded: POINT light cube shadow rects run TINY (33-90 px/face at room distances, amount=range/dist * 256 base) - a quality lever for later. |
| **2.07 local shadow budget SPLIT PER TYPE + spot resolution knob live (`SetShadowPropsSpot`)** | applied 2026-08-06 | User-proven on a TESTPRO2 1-spot/1-point scene: the editor's Shadow Quantity SPOT dropdown did nothing and the POINT dropdown turned spot shadows on/off. Cause: the GG Phase 1 budget was ONE pool fed from `iShadowPointMax` alone, yet its candidate list mixes POINT + SPOT + RECTANGLE casters; the game computed the spot cap and discarded it (its old consumer `SetShadowPropsSpot2D` no longer exists). Now `SetLocalShadowBudget(maxSpot, maxPoint)` runs two candidate pools (SPOT+RECT vs POINT), each with its own cap, nth_element scoring and Phase 1.5 hysteresis; all `localShadowBudget >= 0` gates became `GG_LocalShadowBudgetActive()`. Same port gap hid the spot RESOLUTION dropdown — SPOT/RECT atlas rects sized from `max_shadow_resolution_2D`, which the game feeds the SUN CASCADE resolution; new `max_shadow_resolution_spot` + `SetShadowPropsSpot(res)` make it live. Game side (`M-GridEditB_part3.cpp Wicked_Update_Shadows`): pushes both per-type budgets + the spot resolution every visuals apply; Phase 2 caching now enables when EITHER type is on (was point-only). Harness: `SET_SHADOW_MAX` forces both caps, new `SET_SHADOW_MAX_SPOT`/`SET_SHADOW_MAX_POINT` per type. NOTE: this is also the prime suspect coupling for the OPEN mouselook shadow-flicker — the budget candidate set is built from frustum-culled `vis.visibleLights`, so rotation changes who competes for grants; with spots forced into a pool sized by the POINT knob, grant flapping under rotation was structurally guaranteed on mixed-light levels. |
| **2.07b Sun=Off no longer kills spot shadow RENDERING (DrawShadowmaps guard)** | applied 2026-08-06 | Second user-proven coupling, found minutes after 2.07 landed: turning the SUN shadow resolution to Off made the spot shadow vanish too. DrawShadowmaps' SPOT/RECT case guarded `if (max_shadow_resolution_2D == 0) break;` — correct upstream (spot rects size from _2D there), wrong here where the game feeds _2D from the SUN dropdown. Post-2.07 the spot rect still PACKED (sized from max_shadow_resolution_spot) but never RENDERED — sampled an empty/cleared atlas region = fully lit. One-line fix: guard on `max_shadow_resolution_spot` instead. Isolated with the new harness `SET_SHADOW_RES sun\|spot\|point <res>` (mirrors the UI dropdowns: visuals field + engine setter): rect dump proved the rect survived sun-off, `SET_SHADOW_CACHE 0` exonerated the Phase 2 cache, which left only the render path. Verified: sun off + spot shadow now renders. DIRECTIONAL's _2D guard and POINT's _cube guard are each correct for their own knob. |
| **2.07c Phase 2 shadow-cache key gains direction + cone angle (stale spot shadows on Spotlight Radius edits)** | applied 2026-08-06 | Third user-proven spot bug of the day: raising Spotlight Radius 29→100 left shadows "where they should not be" until the light was clicked/moved. The Phase 2 static-cache decision keyed each local slot on (entity, atlas rect, position, range) ONLY — a cone-angle change (or rotating a spot in place) changes the shadow projection without changing any key field, so the cache kept serving the map rendered with the OLD cone, sampled through the NEW projection = shadows on the wrong side / bright streaks. Clicking the light healed it because the position key changed. Fix: `LocalShadowSlot` carries `dir` + `cone` (L.direction / L.outerConeAngle) and the comparison includes them. No-churn verified live: lighting_loop re-pushes identical values every frame and steady-state SHADOW_LOCAL_RENDERED stays 0. Game pair: harness `SET_LIGHT_RADIUS <idx> <deg>` (writes the light-list spotlightradius; NOTE entity-profile lights may re-sync from the profile per frame — drive the UI slider for those). |
| **2.07d dynamic CASTERS invalidate the Phase 2 shadow cache (moving object under a spot left its shadow behind)** | applied 2026-08-06 | Fourth user-proven spot bug of the day: moving a box under the spot left the box's shadow at the OLD position until the light itself was clicked. The 2.07c cache key covers the LIGHTS' state only — nothing about the casters in their volume. New check in the Phase 2 decision: any renderable shadow-casting object whose world matrix differs from last frame's (`scene.matrix_objects` vs `matrix_objects_prev`, straight memcmp) AND whose AABB intersects a granted light's sphere (radius expanded by the translation delta, so leaving the range still invalidates) forces the full re-render frame. Verified live: static scene stays at SHADOW_LOCAL_RENDERED 0 across repeated samples (no false-positive churn — the torch-bank win is intact), FPS unchanged (119 at the test scene). KNOWN LIMITATION (pre-existing, now documented): bone-only animation does not change the object matrix, so an idling character's local shadow can freeze mid-pose while cached; masked in practice by rect churn under camera motion. |
| **2.07e spot/rect shadows use the DX11 feathered receiver compare — THE spot-shadow acne/"mouselook flicker" fix (shader-only)** | applied 2026-08-06 | USER-DIAGNOSED: artifacts only on the directly-lit surface, pose-dependent IN/OUT flipping, and a point light on the same surface is clean. Root cause: **1.58 ported the DX11 graded-tolerance gather for DIRECTIONAL only** — spots kept upstream's hard `SampleCmpLevelZero` with ZERO receiver-side tolerance, and on D32 float maps the rasterizer bias is exponent-scaled (negligible), so the spot self-shadow compare sat on a knife edge; any camera transform jitter flipped pixels IN/OUT of shadow per frame = the bistable mouselook flicker patterns. POINT lights never suffered because `shadow_cube` carries its own analytic 0.989 distance cushion (the spot/point asymmetry the user spotted). DX11's `shadowCascadeSpot` (WickedRepo lightingHF.hlsli:186-270) ran the SAME feather as its sun path — scaleFactor 65536/(cascade+1), spot cascade 0 → 65536 exactly. Fix: `light_spot` + `light_rect` in lightingHF.hlsli call `shadow_2D_feathered(..., 0, camera_distance)` instead of `shadow_2D`. Verified: the acne mottling on the TESTPRO2 box's lit top face is gone, cast/contact shadows intact. Shader-only — recompiles at launch, no C++ rebuild. Perf: same 8→1 distance-stepped tap kernel the sun already pays on every lit pixel. |
| **2.07g ★ THE SCREEN-TILE LIGHT DIVIDE: fp16 range² overflow deleted the attenuation window (lightingHF float promotion)** | applied 2026-08-06 (engine `a229fffe`) | USER-PROVEN artifact (they measured 20×20 display-px tile alignment in PSP and insisted through two wrong verdicts — they were right): staircase-shaped brightness divides on floors, screen-tile aligned, camera-following, shadow-independent, shadow-res-independent. Root cause: attenuation math ran in HALF precision; **fp16 overflows at 65504, so any light with `range > 255.9` had `range² = +INF`** → the falloff window `saturate(1-(d²/r²)²)` collapsed to 1 → raw un-windowed 1/d² that NEVER reaches zero → the tiled light culling truncated a still-visible `intensity/d²` at its 16px-tile boundary = the staircase (TILE_DEBUG heatmap showed the per-tile count divide exactly along it). Spot range 503 showed it; point lights < 256 stayed windowed = the misleading "point light is clean" asymmetry. SECOND bug from the same overflow: beyond ~256u `dist²` itself went INF (rsqrt→0, L=0) → **large-range lights were hard-killed at a fixed ~256-unit circle regardless of authored range** — big lights now genuinely reach their range (levels with big spots/points will look better AND correctly windowed = slightly dimmer at the rim than the broken over-lit look). Fix: dist²/range² promoted to float in light_point/light_spot/light_rect + attenuation_pointlight/spotlight; half outputs kept. A/B at the saved repro pose: 20-luminance one-pixel tile cliff → continuous gradient. This supersedes the earlier "catwalk mesh / not-a-bug" verdict for the DIVIDE component (the mesh-lattice shadow analysis remains true for the lattice pattern itself). |
| **2.07f spot/rect feather: constant 8-tap kernel (DX11 spot parity; camera_distance −1 sentinel)** | applied 2026-08-06 (engine `a229fffe`) | The 2.07e feather reused the DX11 DIRECTIONAL distance-stepped tap-count formula (8→1 taps by camera distance); DX11's `shadowCascadeSpot` used a FIXED 9-tap kernel. Stepping put camera-anchored brightness bands (tap-count iso-distance contours) into dappled spot shadow. Spots/rects now pass camera_distance −1 = constant 8 taps; the sun keeps its DX11-matched stepping. |
| **2.08 ★ CHARACTER HAIR: double-sided transparent geometry must not write depth (DX11 `BLENDMODE_ALPHANOZ` parity)** | applied 2026-08-07 | USER-REPORTED with the right diagnosis first time ("the alpha channel … is stopping the hair beneath it from being rendered"). DX12 hair rendered as pale card-shaped bites with the scalp showing through; DX11 renders the same head as a solid dark mass. **Root cause = a dropped GGREDUCED rule, not a new bug.** The DX11 fork (`WickedRepo/WickedEngine/wiRenderer.cpp:3736`) carries a comment that names it outright — *"if mesh is double sided, probably hair or leaves, so ensure NO DEPTH WRITE happens to mess up coverage!"* — and does two things upstream does not: (1) forces `BLENDMODE_ALPHA` → `BLENDMODE_ALPHANOZ`, whose depth-stencil state is `DSSTYPE_DEPTHREAD` (test on, **write off**); (2) draws back faces then **front faces only** (`DOUBLESIDED_DISABLED`), where upstream draws back faces then BOTH faces, blending every back face twice. Neither survived the port: upstream puts every transparent in `DSSTYPE_TRANSPARENT`, which writes depth (that is upstream commit `c3e8d2df` "transparent render pass allows equal depth test" — a design choice, not a fork regression). Why it eats hair: transparents sort back-to-front **per object, never per triangle**, so inside one hair mesh the cards draw in index order; a near card drawn early writes depth over its whole footprint and every card behind it fails the test. **The instrument settled it rather than the reasoning**: new harness `DUMP_TRANSPARENTS` reported the actual material — `adult male hair 10_color.dds`, `blend=1 dsided=1(mesh=1,mat=0) alphaRef=1.000 **alphatest=0**` — alpha test is OFF, so even the alpha≈0 texels of the card quad rasterise and occlude. **Fix:** new `DSSTYPE_TRANSPARENT_NODEPTHWRITE` (identical to `DSSTYPE_TRANSPARENT` but `depth_write_mask=ZERO`; test/stencil deliberately untouched so outline + SSS stencil bits and sort order are unchanged), a new `nodepthwrite` bit on `ObjectRenderingVariant`, and the DX11 face pairing (BACK then FRONT). The permutation is built only for MAIN + transparent + cull BACK/FRONT, and the selection path guards on `blendmode != OPAQUE` because `GetObjectPSO` uses `operator[]` — asking for an unbuilt variant would insert into the map from a render thread. Alpha-tested OPAQUE materials (trees, GGTrees foliage) return `FILTER_OPAQUE` and are untouched; GG glass/water take the `customShaderID` branch and never reach this code. **Verified live** at the user's own saved camera in `spotshadowtest.fpm`: carved pale hair → solid dark mass matching the DX11 reference, POLYS **bit-identical** 178839 across four A/B arms, FPS flat (136.7/130.2/135.5/137.3 — no trend), wide-view diff 0.386% of pixels confined to invisible sub-pixel conifer-edge changes. Live A/B `SET_HAIRDEPTH 0\|1`; revert `setup.ini hairnodepthwrite=0`. **CANDIDATE FOR UPSTREAM PR** — upstream has the same artifact on any alpha-blended double-sided mesh with unsorted internal geometry. |
| **2.09 ★★ FIRST-PERSON WEAPON: restore the FORCEDEPTH depth-carve, as a Z-prepass stamp + a main-pass fill (DX11 parity) — USER-CONFIRMED** | applied 2026-08-07, **user-tested and confirmed the same day** ("I have tested manually, it works great") | USER-REPORTED and user-staged: walking into a crate cut the front half off the pistol (screenshot). Same port-gap family as 2.08. DX11's `WickedCall_SetMeshDisableDepth` set `userBlendMode = BLENDMODE_FORCEDEPTH`, one field carrying TWO meanings — transparent-pass membership, and a pipeline whose depth state is `DSSTYPE_WRITEONLY` (write ALL, compare **ALWAYS**). RenderMeshes then ran a SECOND FULL LOOP over the batch list for those materials (`iDoubleRender`, WickedRepo wiRenderer.cpp:3529/3563/3744): back faces stamp the weapon's own volume over the world's depth, then front faces draw normally — so world geometry can never clip the weapon while it still self-occludes. The DX12 port had no FORCEDEPTH to map to, left the material `BLENDMODE_OPAQUE` and kept only `SetDoubleSided(true)`. **Measured before touching anything** (harness `DUMP_TRANSPARENTS`): `blend=0 userblend=0 filt=0x1(OPAQUE) alphaRef=0.010` on `gamecore/guns/enhanced/r870/gun_color.dds`. **Final shape — THREE draws across TWO passes, which is deliberately NOT how DX11 did it.** (a) Z-PREPASS: back faces, `DSSTYPE_WRITEONLY`, admitted by a second `DrawScene(RENDERPASS_PREPASS, DRAWSCENE_TRANSPARENT)` gated on `GG_FORCEDEPTH` — DX11 has this pass too and its comment names the case (*"write depth for transparent objects that are solid (opaque=100%) like guns and solid doors with windows in"*, WickedRepo RenderPath3D.cpp:1056). Without it the weapon leaves the prepass entirely, and the prepass feeds GPU occlusion queries, the light-shaft sun cutout, velocity reconstruction for TAA/FSR2 and SSAO — worst case the occlusion query culls the weapon as hidden behind the very crate it should carve through. (b) MAIN: back-face colour fill (`DSSTYPE_FORCEDEPTH_FILL` — compare ALWAYS, depth write **OFF**), then front faces with the ordinary transparent state. **Why not DX11's shape:** the first attempt put the stamp and the front draw back-to-back per subset on the existing `(pso_backside, pso)` pair. DX11's two halves were two FULL LOOPS, so every FORCEDEPTH mesh stamped before ANY drew; adjacent-per-subset breaks mutual occlusion between the several FORCEDEPTH meshes and **the pistol grip rendered over the glove holding it** (caught in an A/B, not reasoned about). Moving the stamp to the prepass restores "all stamps before all draws" structurally, because a pass boundary enforces it. The main-pass fill then exists because the opaque pass runs `DSSTYPE_DEPTHREADEQUAL`: any world pixel the stamp overwrote fails EQUAL and is never shaded, so wherever back-face coverage exceeds front-face coverage (barrel bores, open cuffs, single-sided sights) it would keep the clear value. DX11 got that fill free — its stamp pass wrote colour. Depth write OFF on the fill is what stops it re-introducing the grip-over-glove wipe. **Scope is not weapon-only and never was**: the flag follows the DBP `DisableObjectZDepth` command — 287 materials in this level, 276 of them editor logic-highlight markers (overlay geometry meant to draw on top) and 11 weapon/arm meshes. Zero world geometry. **`WickedCall_SetMeshTransparent` CLEARS the flag, deliberately**: DX11's single field made this last-writer-wins, which is exactly why the gun carves (`DisableObjectZDepth` last, G-Gun_part3.cpp:735) and the LUA 3D prompt planes do not (`SetObjectTransparency` last, M-LUA-General.cpp:390). An earlier "fix" that pinned FORCEDEPTH materials to ALPHA would have made those planes — single quads whose front faces point away from the camera — stamp depth and draw nothing. `setup.ini weaponforcedepth=0` is a true revert (read at material setup AND in the renderer); harness `SET_WEAPONDEPTH 0|1` flips only the renderer half, so it is an A/B lever, NOT a pre-2.09 baseline. Verified: POLYS **bit-identical** 1356132 across four A/B arms, FPS flat; and at a pose where nothing occludes the gun the OFF-vs-ON diff (0.48-0.52%) is BELOW the same-knob animation floor (0.91-0.93%) — that control is mandatory here, see the measurement rule in WETEST. **USER-CONFIRMED live against the staged crate repro.** **An adversarial multi-agent review (41 agents) caught five real defects before they shipped**, including the two above, a dangling `PipelineState*` (`wi::unordered_map` is `ska::flat_hash_map` — open addressing, `operator[]` rehash invalidates element pointers), and a revert knob that dropped the weapon into 2.08's hair rule. `GetObjectPSO` is now FIND-ONLY with a separate `SetObjectPSO` for the LoadShaders safe point, which removes that whole class plus the concurrent render-thread insert. **NOT ported**: `SHADERTYPE_WEAPON` / `objectPS_weapon.hlsl` (`WEAPON_SHADOW`) — needs a new object-shader permutation and a `SHADERTYPE_BIN_COUNT` bump recompiling ~375 shaders. |
| **2.10 LIGHT POWER PARITY: punctual lights use the DX11 product falloff (energy 30 x (1-d2/r2)^2, no inverse-square)** | applied 2026-08-07, awaiting user eye | USER-REPORTED vs the DX11 spotshadowtest baseline: point/spot light power reads ~half of DX11 and collapses into a hot pool at the source instead of the DX11 broad even flood. This is task #109's prediction landing: the range2*PI/4 intensity heuristic in `WickedCall_UpdateLight` was tuned while the 2.07g fp16 overflow had DELETED the attenuation window, and its premise was wrong twice over — DX11's `BRDF_GetDiffuse` DID divide by PI (WickedRepo brdf.hlsli:449, a fork early-return of constant 1/PI), and no single intensity scalar can turn upstream's windowed 1/d2 into DX11's `(1-d2/r2)^2` SHAPE (WickedRepo lightingHF.hlsli:621/699). DX11 ran EVERY GG map light at constant energy 30 (set once in `WickedCall_AddLight` -> `Entity_CreateLight(...,30,500)`, DX11 wickedcalls.cpp:6655; `UpdateLight` never touched energy) — range alone shaped the curve; energy packed raw fp16 with no CPU scaling (WickedRepo wiRenderer.cpp:5092). ENGINE HALF: `OPTION_BIT_GG_DX11_LIGHT_FALLOFF` (bit 30, reserved GG range) set from `wi::renderer::gg_dx11_light_falloff` (default ON) at frameCB assembly; `attenuation_pointlight` branches to `pow2(saturate(1-dist2/range2))` (float math — 2.07g rule) and `attenuation_spotlight` skips the modern SQUARING of the angular term: GG never sets innerConeAngle (default 0 -> cos=1), so the packed angle_scale/angle_offset already reduce to DX11's linear-to-center cone ramp `saturate(1-(1-SpotFactor)/(1-coneCos))` (WickedRepo lightingHF.hlsli:700) — only the squaring differed. NO 1/PI correction anywhere: both engines carry exactly one Lambert 1/PI on diffuse (DX11 inside BRDF_GetDiffuse, DX12 at ApplyLighting's `direct.diffuse/PI`) and both specular D terms their own, so intensity is in DX11 energy units 1:1 — diffuse AND specular parity from one constant. Placing the branch inside the two attenuation functions makes every consumer follow automatically: surface lighting, volumetricLight_Point/Spot, raytraceCS, ddgi, surfels, renderlightmap, shadingHF. The DX11 curve reaches exactly ZERO at range, so the Forward+ tile-cull boundary never truncates a visible contribution — the 2.07g artifact class cannot return under this curve. GAME HALF (same commit family): `UpdateLight` sets `intensity = 30.0f` under the knob (heuristic kept verbatim as the revert arm), `WickedCall_AddLight` creation intensity 30 (was `BackCompatSetEnergy(60)` = point 1200/spot 12000, instantly overwritten anyway). Editor camera/thumb lights (created at intensity 8) now mean DX11's energy-8 — the same fixture DX11 lit thumbnails with. Levers: harness `SET_LIGHTFALLOFF 0|1` is FULLY live in one knob (shader bit is per-frame CB; lighting_loop re-pushes every light's intensity through UpdateLight each frame, both read the same bool); `setup.ini lightfalloff=0` persistent revert. Files: ShaderInterop_Renderer.h, lightingHF.hlsli, wiRenderer.h/.cpp + game wickedcalls_part3.cpp, master_part1.cpp (GGSetDX11LightFalloff), Common_part3.cpp, AutomationHarness.cpp. |
| **2.11 pso_validate null-guard + failure forensics (gpup restore fallout)** | applied 2026-08-07 | Upstream's lazy at-draw PSO compile (pso_validate) fell straight through to `SetPipelineState(nullptr)` when the driver rejected the stream — Release assert is a no-op, so any rejected pipeline was an instant d3d12.dll AV at address 0x18c with zero diagnostics (wi::backlog buffers and loses its tail in a crash). Now: a failed compile writes hr + shader pointers + full input-layout element dump + render-pass facts to `gg_pso_fail.txt` (fopen/fflush per line, survives anything), skips the bind (previous pipeline stays bound — a wrong draw beats a dead process) and clears dirty_pso. Built to name the gpup particle-restore crash; kept because it converts EVERY future driver-rejected PSO from an unexplained AV into a named one-line report. THE TWO BUGS IT/ITS TRACE NAMED (both fixed GAME-side, GPUParticles_part0.cpp): (1) gpup_init built its PipelineStateDesc from STACK LOCALS — PipelineStateDesc stores POINTERS and the input layout's SemanticName is kept as a c_str() into the caller's InputLayout (wiGraphicsDevice_DX12.cpp:4865); with the driver compile deferred to first draw, the strings were dangling and d3d12 read garbage semantics. Descs are now static (same latent landmine fixed in GGGrass.cpp's dormant legacy path). DX11-era Wicked compiled PSOs eagerly, so stack locals were correct back then — audit any OTHER game-side CreatePipelineState for this class when porting. (2) gpup_draw called UpdateBuffer INSIDE the transparent render pass — UpdateBuffer records a CopyBuffer, copies inside a D3D12 render pass are an INVALID CALL, and the device was REMOVED at submit (DXGI_ERROR_INVALID_CALL on Present, the two error dialogs). Replaced with BindDynamicConstantBuffer (transient upload memory, no copy command) — THE in-pass idiom for anything drawn from customDraw_* callbacks. |
| **2.12 descriptor-ring + bindless-pool forensics (`GG_DumpBinderStats` / free bridge `GG_GetDescriptorRingStats`)** | applied 2026-08-08 (diagnostic only) | White-out hunt instrument: per-heap allocationOffset / fence-completed / gap / ring size / laps, `gg_desc_ring_waits` + `gg_desc_frames_signaled` atomics, bindless pool occupancy (used/capacity + pending-destroy under destroylocker). Printed by the game's `GPUP_DUMP`. Exonerated the descriptor rings and bindless pools with data (gaps flat through a live white-out, zero waits, sampler pool 41/256 flat). Zero hot-path cost — counters only. |
| **2.13 STATE-SAFE customDraw_* HOOK BOUNDARIES (camera-CB restoration + `GG_InvalidateCommandListState`) — fixes the steam-scene whole-screen white-out** | applied 2026-08-08, 3-round deterministic-repro verified | THE "whole screen washed out" defect on spotshadowtest (user-reported twice, historically took "~50 min") — closed after a 6-instrument elimination campaign. ROOT CAUSE: **the gpup hook's draws bind their constant buffers on shared-binder slots b0/b1 — and b1 is `CBSLOT_RENDERER_CAMERA`. `DrawLensFlares`, later in the SAME transparent pass, reads `GetCamera()` without rebinding the camera CB, so the flare VS executed against GPUP'S PARTICLE CONSTANTS MISREAD AS THE CAMERA**: `canvas_size_rcp` became a particle float, scaling the sun-flare quad FULLSCREEN from its legitimately off-screen anchor, sampling the flare texture's bright center at high alpha = a uniform ~88%-opacity veil (`out = 197 + 0.12*scene`). THE "TRIGGER"/"AGING" WAS NEITHER: the per-frame back-to-front emitter sort decides WHOSE constants sit in b1 when the hook returns; camera pose changes the sort (incl. the 6553.5-unit decihash wrap), flipping the misread between harmless-garbage and fullscreen-garbage. 250 min of pure idle never reproduced; every historical "~50 min to white" was time-until-the-camera-changed-the-sort. PROOF CHAIN (each step eliminated a layer with data): gpup pixels out (GPUP_CANARY all-red changed nothing) -> sim pools/textures/buffers/samplers/clocks/rings out (byte-level instruments, GPUP_DUMP) -> lightshafts contribution out (green-tint stayed white, rtSun[1] zero) -> lensFlarePS red-tint turned the veil RED (flare pixels convicted) -> skip-only-the-flare-Draw removed it (its own draws) -> CPU-recomputed flare push constants were CORRECT+off-screen (DUMP_SUN) -> state-tracker invalidation alone did NOT fix, push->CB conversion alone did NOT fix -> camera-CB restoration after the hook FIXED 3/3. ⚠ SET_LIGHTSHAFTS/SET_LENSFLARE toggles both "cured" it — frame-structure confounds (they moved the residue), only pixel-level tints discriminated. FIX: after ALL customDraw_* hook returns in wiRenderPath3D.cpp: `BindCameraCB(pass-appropriate cameras) + BindCommonResources(cmd)` restore the pass's CB contract (in-pass hooks), plus `GraphicsDevice::GG_InvalidateCommandListState(cmd)` (virtual no-op; DX12 clears prev_pt/prev_pipeline_hash/active_pso/cs/rt/active_rootsig_graphics+compute/dirty_pso) as defense-in-depth for the tracker class. ★ RULE: a game hook that records draws inside an engine pass must be followed by restoration of EVERY implicit binding contract the rest of the pass relies on — the camera CB is the big one, and this class is invisible to every resource-content instrument (the corruption is in WHICH buffer a slot points at, not in any buffer's content). |
| **2.28 Directional shadow-caster cull extrusion (`gg_shadow_caster_extrude`) + caster counter** | applied 2026-08-12 (engine `bab373ea`: `wiRenderer.cpp` `CreateDirLightShadowCams` extrusion floor, `gg_dbg_shadow_casters`; externs in `wiRenderer.h`. Game: harness `SET_SHADOWEXTRUDE`, setup.ini `shadowextrude`) | ⚠ **This row was missing until 2026-08-13 — the delta had shipped and would have been lost on the next upstream pull.** Stock Wicked's caster-cull extrusion floor is `min(2000, farPlane) * 0.5` = **1000 units**, a metres-scale constant in GameGuru's INCH-scale world: only 25 m of caster allowance, so an object that left the camera frustum stopped casting even though its shadow was still on screen. User-reported ("the shadow pops away"). Floor raised to `gg_shadow_caster_extrude`, default 4000 u (~101 m); **0 restores stock exactly**. Fully live, so it is a true A/B lever. **USER-CONFIRMED FIXED.** ⚠ Judge the cost on the Shadowmap pass, never FPS, and bucket samples on `SHADOW_CASTERS` first — the count is BIMODAL (43 vs 856 on Island Showdown) because of the staggered cascade refresh, and a naive 5-sample mean measures which phase you caught (a 2026-08-12 run read stock 1.478 / new 1.124 / stock-again 0.856, i.e. "stock is slower than stock"). |
| **2.31 Per-mesh draw tracer inside `batch_flush` (instrument)** | applied 2026-08-13 (engine `90604faf`: `gg_dbg_watch_mesh` + per-RENDERPASS batches/instances/subsets/draws/nofilter/nopso/nobuffer; harness `WHYNOTDRAWN` arms and reads it) | Built because a per-OBJECT visibility instrument was insufficient: on the invisible collectable pistol it reported "passes every gate, in the visible set" while nothing reached the screen, so the loss had to be per-SUBSET or per-PSO-variant — both invisible from outside `batch_flush`. Keyed on mesh index because `batch_flush` IS the instancing unit, so `instances` also answers "who is this batched with". `nopso` catches the SILENT missing/invalid-variant skip, which is per-renderpass and therefore able to drop the camera draw while the shadow draw survives. Zero cost when unarmed (-1 default). |
| **2.32 ★★★ CLAMP THE RenderBatch DISTANCE BEFORE THE HALF — objects vanished from the camera while still casting shadows** | applied 2026-08-13 (engine `9751e2cf`: `RenderBatch::Create` clamps to 65504; plus the `gg_dbg_watch_q[pass][stage]` queue-build attribution that found it) | **`RenderBatch` stores its sort/fade distance as a HALF** (`wiRenderer.cpp:474`, `:483` `XMConvertFloatToHalf`). A half's largest finite value is **65504**. The collectable pistol on spotshadowtest sits 1 m from the camera but its bounding-box CENTRE is **86,730 units** away (its AABB is corrupted to span from the table to the parked library master at 100000,100000,100000), so the conversion returned **+INF**. `RenderMeshes` then computes `dither = max(transparency, max(0, GetDistance() - fadeDistance) / radius)` — `INF - FLT_MAX` is INF — so `dither > 0.99f` fired, the instance was skipped, `instancedBatch.instanceCount` stayed 0 and `batch_flush` returned at its first line. **No error, no warning, no draw**, while the SHADOW pass (distance 0) drew it perfectly. Located by the contradiction between the two tracer halves: `MAIN QUEUE ADDED=1143` but `MAIN batches=0`. **Measured: MAIN batches/draws 0 → 1088, and the pistol plus both ammo pickups render.** Clamping rather than widening the field keeps `RenderBatch` at 16 bytes and its `static_assert` intact; an object sorting as "65504 away" is harmless. ★★ **SECOND fp16-range bug on this project** (see 2.07g, the light-attenuation range² overflow). GameGuru is INCH-scale, so 65504 units is only ~1.66 km and trivially reachable; upstream Wicked is metres, where the same limit is 65 km. **When porting a metres-scale engine to an inch-scale game, every half-precision distance is a range bug waiting to happen — audit them as a class.** ⚠ The corrupt AABB itself is a separate, still-open GAME-side defect. |
| 2.1 hairparticlePS white | reverted 2026-06-18 | none — shader back to upstream state |
| 2.2 hairparticle_simulateCS overrides | reverted 2026-06-18 | none — shader back to upstream state |
| 2.3 hairparticlePS_prepass alpha=1 | reverted 2026-06-18 | none — shader back to upstream state |
| 2.46 emitted-particle velocity-aligned rotation (`emittedparticle_simulateCS.hlsl` + `emittedparticle_emitCS.hlsl`) | applied 2026-08-14 | **re-apply on upstream pull.** Restores the DX11 fork's `start_rotation` semantics: the fork used it as a SCALE on a per-particle rotation derived from direction of travel (`emittedparticleVS.hlsl:52-58`), new Wicked reinterpreted the same field as a constant initial angle. Gated on `xEmitterFadeinTime >= 0`, the established legacy-.PE discriminator (`wiEmittedParticle.h:156`), so upstream/editor emitters keep upstream behaviour. Paired with the emitCS change that skips seeding the initial angle for those emitters so the value is not applied twice. Two deliberate infidelities-to-clean-code are documented at the site: the float3→float truncation of `tan(normalize(velocity))`, and the column-vector `mul(M, v)` that is the transpose of Wicked's usual convention — both reproduce the fork exactly |

| 2.47 positive-only distortion clamp (`emittedparticlePS_soft.hlsl`) | applied 2026-08-14 | **re-apply on upstream pull.** Restores the fork's `return max(color, 0)`, which clamped the signed distortion vector so a particle could only push the scene one way in UV. Deliberate on the fork's part, not implied by its buffer — the fork's `rtParticleDistortion` was `R16G16B16A16_FLOAT` (`RenderPath3D.cpp:329`), so negatives would have survived there too. Scoped to the DISTORTION permutation (a no-op elsewhere, so soft/soft_lighting stay provably untouched) AND to legacy .PE emitters via `xEmitterFadeinTime >= 0`, because rain draws through a SOFT_DISTORTION override (`wiRenderer.cpp:7289`) and must keep upstream's signed behaviour. Does not disable the effect: 64.5 % of the distortion map's visible texels survive the clamp |

| 2.53 generation-center override (`wiTerrain.h` + `wiTerrain.cpp` Generation_Update) | applied 2026-08-15 | **re-apply on upstream pull.** Three namespace globals (`gg_generation_center_override_x/_z/_enabled`, deliberately NOT Terrain members so nothing rides Serialize/Entity_Duplicate) let the game pin the chunk ring to a world-space XZ instead of `camera.Eye` — the override substitutes only at the ONE `center_chunk` computation inside the `IsCenterToCamEnabled()` block, and everything downstream (creation ring, removal threshold, priority sorts, freesort) keys off `center_chunk`, so the whole generation system follows. Used by the Terrain Generator to keep chunks around the editable-area marker while the preview camera roams; the GGTerrainWicked bridge force-clears it whenever `bProceduralLevel` is false, so no other mode can inherit a pinned ring. Verify after re-apply: `TERRAIN_RING ... ovr=1` in the generator, `ovr=0` in the editor |

| 2.58 chunk-gen profiler + generator BVH skip (`wiTerrain.h/.cpp`) | applied 2026-08-15 | **re-apply on upstream pull.** gg_genprof_* per-phase accumulators inside Generation_Update's chunk bake (dumped by the game's TERRAIN_GENPROF) + `gg_generation_skip_bvh` (default false; the Terrain Generator sets it — SetBVHEnabled's synchronous 8712-triangle CPU BVH build measured at 8.2 of 10.96 ms/chunk = 75% of generation, and generator drag-picks work on the brute-force fallback; generator chunks are wiped on exit so the editor always regenerates WITH BVHs). Verify after re-apply: `TERRAIN_GENPROF` shows bvh≈0 for generator-born chunks |

| 2.60 merge profiler + generator grass skip (`wiScene.h/.cpp`, `wiTerrain.h/.cpp`) | applied 2026-08-15 | **re-apply on upstream pull.** gg_mergeprof_* (per-manager MergeFastInternal timing, MERGE_PROF harness dump) + `gg_generation_skip_grass` (default false; Terrain Generator sets it — skips grass perlin seeding + system creation per chunk; generator chunks are wiped on exit so editor/test game keep grass). Verify after re-apply: generator TERRAIN_GENPROF grass≈0.1, editor grass unchanged |
| 2.61 VT/copy-wait/trace instrumentation + BVH-heal generator gate (`wiTerrain.h/.cpp`, `wiScene.cpp`, `wiGraphicsDevice_DX12.cpp`, `wiProfiler.cpp`) | applied 2026-08-15 | **re-apply on upstream pull.** THE FIX: Generation_Update's per-frame chunk-loop BVH heal (`if (!bvh.IsValid()) SetBVHEnabled(true)`) now honors `gg_generation_skip_bvh` — without the gate it rebuilt every 2.58-skipped chunk's BVH synchronously ON THE MAIN THREAD (~6 s of 400-550 ms frames per generator fill; fill 24 s -> 13 s with it). Plus instruments: gg_vtprof_* (VT_PROF), gg_dbg_copywait_* (CopyAllocator::submit's null-event SetEventOnCompletion is a CPU-blocking copy-queue wait; gap tracer prints copyWaits/copyWaitMs), gg_trace_mark decomposition in Scene::Update (scn-*) and Generation_Update (tg-*). Verify after re-apply: generator fill shows no 400+ ms tg-chunkloop gap frames; editor picks still work (heal active when flag false) |
| 2.71 diagnostic trace-file gate (`wiAllocator.h`, `wiProfiler.h/.cpp`, `wiScene.cpp`, `wiScene_Components.cpp`) | applied 2026-08-16 | **re-apply on upstream pull.** Standalone exports run `producelogfiles=0` and must not drop trace files in players' folders: `wi::allocator::gg_alloc_tripwire_file` (inline bool, gates ONLY the alloc_tripwire.txt per-op ledger — tracking + graceful reject stay live), `wi::profiler::gg_trace_file_enable(bool)` (gates ONLY the gap_trace.txt fopen — gap counters + hitch histogram keep feeding GET_PERF_DATA), `gg_anim_garbage_file` (wiScene.cpp, 2 ANIMROT sites) and `gg_applytransform_garbage_file` (wiScene_Components.cpp, 2 sites) gate the parrot tripwire FILES while the sanitizers keep healing. All default TRUE (editor behavior unchanged); the game clears them via GGSetDiagTraceFiles from the producelogfiles setup.ini key (early pass + normal parse). Crash writers (dred_report.txt) untouched. Verify after re-apply: standalone folder stays clean of gap_trace/alloc_tripwire/anim_garbage txt; editor with producelogfiles=1 still writes them |
| 2.75 debug env-probe preview scale (`wiRenderer.h/.cpp`) | applied 2026-08-16 | **re-apply on upstream pull.** `wi::renderer::SetDebugEnvProbeSphereScale(float)` + `gg_debugprobe_sphere_scale` (default 1.0 = stock unit sphere): the debug env-probe mirror spheres (DrawDebugWorld, `debugEnvProbes` block) are drawn with `XMMatrixScaling(s,s,s) * translation`. The game sets s = picked %probe marker ball world-radius x 1.15 (G-Lighting.cpp) so probe pick shows a LARGE ACCURATE preview of the captured cube enclosing the (now matte, game-side 2.75) marker ball — the legacy ball's own PBR shading read as per-face portholes and was mistaken for cube-map corruption (#155, notes SS2.74b/SS2.75). Verify after re-apply: pick a probe marker, the big mirror sphere shows a seamless environment |

| 2.76 debug env-probe focus (`wiRenderer.h/.cpp`) | applied 2026-08-17 | **re-apply on upstream pull.** `wi::renderer::SetDebugEnvProbeFocus(x,y,z,radius)` + `gg_debugprobe_focus_*`: the `debugEnvProbes` sphere loop now draws ONE sphere — the probe NEAREST the focus point, and only if it is inside `radius` (radius <= 0 = stock all-probes, used by the SET_DEBUGPROBES harness override). Stock drew a mirror sphere for EVERY probe in the scene, which was invisible at the stock unit scale but made every probe in the level swell the moment 2.75 sized the spheres to the marker ball; nearest-wins rather than a plain radius test because two markers can sit closer together than any sane tolerance (Lee's spotshadowtest pair is 63 units apart). Pairs with the game-side 2.76 sizing fix (half-extent, not AABB getRadius). Verify after re-apply: two probe markers side by side, pick one — only that one shows the mirror preview and neither changes size (#158, notes SS2.76) |

| 2.77 env-probe capture trace (`wiRenderer.h/.cpp`) | applied 2026-08-17 | **re-apply on upstream pull.** DIAGNOSTIC, default OFF (zero cost): `wi::renderer::SetProbeCaptureTrace(enable,radius)` + `gg_probecapture_trace*`. When on, RefreshEnvProbes appends one block per CAPTURE to Files/probecapture.txt: the capture's znear/zfar (note: inherited from the MAIN camera via `vis.camera->zNearP`, so the volume hidden around every probe is a cube of half-width znear) and every object within radius with its keep/skip reason (layer / range sphere / renderable / NOT_VISIBLE_IN_REFLECTIONS). This is the instrument that finally named the #157 circles (a second, unexcluded marker object being captured from the inside). ⚠ The externs are declared at FILE scope above RefreshEnvProbes on purpose — a block-scope extern inside the `render_probe` LAMBDA mangles into the global namespace and fails to link. Driven by the SET_PROBECAPTURETRACE harness command (notes SS2.77) |

| 2.78 debug probe force-global (`wiRenderer.h/.cpp`) | applied 2026-08-17 | **re-apply on upstream pull.** DIAGNOSTIC, default OFF: `wi::renderer::SetDebugEnvProbeForceGlobal(int)` + `gg_debugprobe_force_global`. When on, the debug env-probe spheres bind the GLOBAL probe's cube (identified by largest range) instead of their own, while keeping the focused probe's position/scale — so the base env map can be studied continuously instead of glimpsed while a Probe Range slider is dragged. ⚠ The extern is declared above `DrawDebugWorld` because the debug block precedes the definition in file order. Driven by SET_DEBUGPROBEGLOBAL (notes SS2.78) |

Update this table any time we add, revert, or commit a change to the
`WickedEngineDX12` clone.
