# Resume block — written 2026-09-24, for the session after the compact

Everything below is committed and pushed. Game `main`, engine `master`, both trees clean.

## Where the pre-alpha stands

**The gate is effectively CLEAN and I would ship it.** Full 19-demo run on game `66b3dd7c` /
engine `38c13913`, written up in `DEMO_FPS_SWEEP.md` under **0923b**:

| | |
|---|---|
| C1 LOAD | PASS 19/19 |
| C2 GEOMETRY | 18 of 19 exact or in-range. The 19th (Z Island, −320) is that demo's own noise, **re-measured over 12 samples and confirmed to reach the reference** |
| C3 VRAM | PASS — worst **3799.3 MB**, Aztec Game Kit in Test Game, **296.7 MB headroom** under the 4 GB floor |
| C4 GAME | PASS 19/19 |

## The three things Lee still has to decide

1. **`tools/prealpha_clean.sh --apply`** — the last step before zipping. **1551 MB** of non-product
   files. One arming file left, `dred.txt`; the standing recommendation is to KEEP it for a
   pre-alpha (an armed DRED turns a tester's device removal into a real report), but that call is
   Lee's and the script lists arming files separately for exactly that reason.
2. **§3.86 — the terrain/tree LOD is still not restored after Test Game.** This is the
   35%-geometry-loss finding: after one Test Level round trip, every level loaded afterwards can
   draw up to 35% fewer triangles for the rest of the session. Root cause named to the line;
   **still live.** The highest-value open item.
3. **Re-measure the 3.91 reflection default on the AMD card.** 1024 + blur 2 is now the shipped
   default and costs a full second scene render at 1024x534 with 4x MSAA. The RX 9060 XT could not
   see the cost (1.8 ms GPU busy in a 4.6 ms frame — CPU-bound, ~60% GPU idle), so a green sweep
   here says nothing about the 6-year-old AMD card the whole low-spec campaign exists for.

## What landed 2026-09-23/24 — 3.87 through 3.95

All Lee-confirmed unless noted.

| | |
|---|---|
| 3.87 | Test Game no longer wipes the level's authored post-processing. Fixed at the CALL SITE, because `SetGlobalGraphicsSettings` has a second caller (the shipped game's own graphics menu) with the opposite intent |
| 3.88 | Terrain Bake no longer kills the planar reflection — `GGTerrainBakeVS.hlsl` was the only VS in the family with no `SV_ClipDistance0` |
| 3.89 | **Water Reflection Size** slider (Auto/512/768/1024/1536/2048) |
| 3.90 | **Water Reflection Blur** (0–3 passes of the engine's separable Gaussian) |
| 3.91 | Those defaults set to **1024 + blur 2** — Lee's call. ⚠ the only shipped behaviour change that moves VRAM (~+26 MiB/level) |
| 3.92 | Skinning streamout ping-pong gated on the skip decision |
| 3.93 | One Reduction Scale decision per CHARACTER, keyed on the shared world pivot |
| 3.94/b | The Object Library live preview exempt from all three animation throttles |
| 3.95 | This gate run, plus two instrument corrections |

## Two instruments corrected this round — read before trusting either

- **Z Island's POLYS spread is at least 4120, not the 528 in notes 3.74** (corrected in place). And
  **the gate's `polysrange` escape hatch samples only three times**, which cannot bracket that — so
  it fails on luck. Recorded, not fixed; the remedy is more samples on the three known
  non-deterministic demos and costs sweep wall-clock.
- **Cross-run `driver_usage_mb` cannot resolve a ~27 MB VRAM change.** Seven of nineteen demos went
  DOWN after a change that can only add memory. Use the in-session interleaved A/B (notes 3.90b).

## Standing open items, unchanged

- Standalone holds 950–1520 MB more than Test Game on the same level (09-20 §3.6).
- Export Game — least-tested path in the build, untouched since 08-16.
- `visObj=7` — the preview camera reports seven visible objects where subject + backdrop is two.
- Thumbnail generation to disk / snapshot / particle-thumb modes still disabled in DX12.
- The `bEnableAnimationCulling` panel checkbox **controls nothing** — an engine static shadows the
  game global and nothing mirrors it (found 3.94b, its own ticket).
