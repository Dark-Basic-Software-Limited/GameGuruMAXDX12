# Resume block - updated 2026-09-24 evening, as Lee prepares the tester build

Everything is committed and pushed. Game `main`, engine `master`.

## State: ready for Lee's manual tester build

- **Gate (overnight 0924): effectively CLEAN** - 19/19 load, 296.7 MB VRAM headroom, the only POLYS
  miss is Z Island's known noise. Soak confirmed 3.96 exactly. Notes 3.101.
- **Landed today, Lee-confirmed:** 3.96 (Test Game no longer rewrites level terrain to disk),
  3.97/3.98 (library preview owns its lighting; DX12 lighting stage; float skinned normals - fixed
  black -Z faces on static props in LEVELS too), 3.99/3.99b (full-size preview backdrop; None = DX11
  grey), 3.100 (Switch Escape nav mesh verified working).

## Lee's open decisions

1. Reset the two levels already baked at a downgraded terrain preset? (`operation amazon` MED,
   shipped; `testpro2level` LOW) - notes 3.101.
2. `tools/prealpha_clean.sh --apply` before zipping - **Lee does file cleanup himself**. Arming file
   `dred.txt`: recommendation is keep it for a pre-alpha.
3. One line for tester notes: editing a shipped demo saves to Documents, not over the demo.
4. Re-measure the 1024 + blur 2 reflection default on the AMD card (this rig is too GPU-idle to judge).

## Known untested / open

- PLAY GAME standalone and Export Game - exercised by hand only (overnight phase 2 tested nothing).
- Standalone holds 950-1520 MB more VRAM than Test Game on the same level (09-20 §3.6).
- How many stock props had exactly-(0,0,-1) faces drawing black before 3.98 - not counted.
- Lighting-stage defaults (3.98) provisional; the DX12 thumbnail regeneration is a nice-to-have.
- `bEnableAnimationCulling` panel checkbox controls nothing (3.94b).
