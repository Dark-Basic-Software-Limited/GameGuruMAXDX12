# GameGuruMAX Project Memory

> **Resuming work? Read [Next action immediate](project_next_action_immediate.md) FIRST** — current state, next step, do-not list.
> ⚙ This folder is mirrored into the repo at `GameGuru Core/claude-memory/` so it survives a
> machine change. `tools/sync_claude_memory.sh to-repo` after a session; `to-live` on a new box.
> ⚠ The live path is keyed on the repo's ABSOLUTE path — a wrong key fails SILENTLY.

## Current campaign — performance / ultra-low-spec
- ⚠ **[Alpha packaging](project_alpha_packaging.md)** — 3.38 GB of debris removed 08-29. **`ffmpeg.exe` and `GameGuruMAX.pdb` look like debris and are NOT** — two editor features shell out to ffmpeg, and the pdb SHIPS so tester crash logs symbolise (a rebuild cannot match it). Read before tidying the build folder.
- ★★★ **3.35b–h Lee's five reports, one a DEVICE HANG (08-29)** — live Texture Detail RACED the streaming thread → GPU page fault on a freed texture → DEVICE_HUNG; the guard (`GGReloadGuardBegin/End`, 1.44) already existed and the divide never asked for it. ★★★ Two rules: **dropping a texture means substituting for every channel it supplied** (5 defects now), and **a crash path must not use RTTI** — 3.26's `dynamic_cast` threw and turned a handled device loss into a silent close. Also: tooltips wrap, Super Quick defaults to 3, baked water gets a Schlick ramp, 3.19 GB of my debris removed. Notes §3.35b–h.
- ★★★ **3.34–3.35 Super Quick made real + the Opaque breakdown (08-28)** — Opaque Scene now splits into Objects/Terrain-Trees-Grass/Sky: it is **99% objects**. A shader fetching NOTHING still costs 61%, and 6.25× fewer pixels moves it 13% — the floor is 1320 draws, not fill. Three rungs + a reduced VS/PS layout: stock→FLAT **−52%**. ★★★ Cost three defects to one rule: **dropping a texture means substituting for every channel it supplied — never leave the multiply identity.** Notes §3.34–§3.35.
- ★★★ **[Terrain Bake / Water Bake](project_terrain_bake.md)** — the 3.25 milestone. Terrain → plain meshes + BC1, two-tier 256/8192 with a 1024 MB budget, mipmapped; Water Bake; Reduction Scale; all Brutal switches now PER-LEVEL. ★ Nine defects, none of which announced itself. Read its rules before touching a custom draw, a bake, or anything that removes a component.
- ★★★ **3.25 AMD-card round (08-26)** — **Terrain Bake** (bakes chunks to plain mesh+texture then reuses the Terrain Off teardown; fast card 174→228 FPS, VRAM −820 MB), **Water Bake**, **Reduction Scale** (stock Wicked skins EVERY mesh in the scene every frame, no distance test — that is why the 30fps box never moved Skinning and Morph), and **every Brutal switch is now PER-LEVEL**. Post Effects Off + Simple Sky removed. ★ Three defects that all looked healthy: shaders silently unloaded (CWD moves to Files/), device removed by a compute Dispatch inside a render pass, and invisible terrain with perfect counters. Notes §3.25.
- [Terrain + VT performance](project_terrain_vt_perf.md) — the four brutal off-switches (Aztec 15.60→5.85 ms), the 3.96 ms/frame VT cost that was hiding as "GPU idle", and what a baked terrain is actually worth (+35%, and the cliff is TILE COUNT not resolution). Notes §2.94–§2.94f, §3.02.
- [Far-tree billboards](project_far_tree_billboards.md) — 2.95–3.07: distant trees restored, pool cap, per-level atlas slices, and the three defects they shipped with (black trees, the flicker, harsh shade). Notes §2.95–§3.07.
- ★★ **3.19 panel + controls (08-25)** — profiler rows no longer vanish or reorder (name order, measured: no deadband survives a 10× swing); Object Detail Distance now works UPWARDS (`<` → `!=`); Texture Detail applies LIVE to objects AND terrain. Notes §3.19. ★★★ **3.20**: tree position LATCHED (a thread_local parent moved a row 82 lines on 1 dump in 31 — the THIRD shifting mechanism); "Hide idle rows" tick box ships at **0.05 ms** (Lee's choice), **127→95 rows settled**; its transient is monotone INSERTIONS, never removals. ⚠ I gave him a WRONG number to decide on (32 vs the true 12) from a sparse sample — see [Measuring rules](project_measuring_rules.md). Notes §3.20–§3.20b.
- ★ **Sweep 0825 = the new POLYS reference** (19/19 load + game, worst in-game VRAM 3976 MB). `tools/sweepgate.sh` was REBASELINED to it (3.19d, provenance in the script) and now reports 19/19 on all four criteria with 120.4 MB VRAM headroom. ⚠ Anything touching the tree pool, the billboard handover or a draw_distance moves these numbers again — amend C2 in writing BEFORE the run. `DEMO_FPS_SWEEP.md` top section.
- [Low-spec off-switches](project_lowspec_offswitches.md) — the 3.08–3.12 switch set (8 new, all default-off). ★ Shadows Off **+17% FPS**; Object Detail Distance **+9–10%** but the curve is **U-shaped**; Occlusion Culling Off is a measured **LOSS**. Texture Detail is an ENGINE change (−211 MB). Render scale and "simple water" were already covered. **Read before adding another switch.**

## Recently closed (detail lives in the linked file — do not re-derive here)
- ★★ **2.90 probe properties + 2.90a pre-alpha gate 08-18** (engine `bde205ad`, game `26482fe6`/`2f73633c`) — Brightness = SEVERED chain (restored; ⚠ mip 0 excluded as in DX11); Range = CLOBBERED value, never worked in either renderer, slider removed. Gate 19/19, POLYS identical, 4 GB holds. ⚠⚠ its FPS table reads −13.4% vs 0816 but the same morning was already −11.2% below — drift, **do not re-open**.
- ★★★ **#157 ENV-PROBE CIRCLES SOLVED + 2.89 BASELINE 08-18** (engine `630a245e` / game `017c2b43`) — parallax ray-exit ran in `half` in WORLD units; OBBs over 37,820 overflow to +INF = the circles. Ships `probeparallax=3`. [Env probes](project_env_probes.md)
- ⚠ **2.77 probe-capture defect — STATUS CONFLICTED, needs one eyeball.** `MakeObjectEnvMatte` excludes only the one object id given but a %probe is TWO. 08-18 a local cube rendered CLEAN. [Env probes](project_env_probes.md)
- ★ **Probe pick no longer resizes the balls (2.76, 08-17)** — AABB `getRadius()` is the half-DIAGONAL, and the engine drew a debug sphere for EVERY probe. [Env probes](project_env_probes.md)
- **gpup/.arx particles** RE-ENABLED 08-08 (`82959a2b`) — "ping" = shared-CB copy race; white-out = gpup b1 stomping the camera CB. [gpup particles](project_gpup_particles.md)
- **Light power parity** 2.10 — DX11 energy 30 × `(1-d²/r²)²`. [Light falloff](project_light_falloff_parity.md)
- **FP weapon depth carve** 2.09 + **WEAPON_SHADOW** 2.14 + **character hair** 2.08 [Transparency parity](project_transparency_parity.md) · **screen-tile light divide** + **spot acne** [Shadow system](project_shadow_system.md)
- **PLAY GAME DEVICE_HUNG** — terrain DDS freed at level-load swap, stale descriptor. Repo `PLAYGAME_CRASH_2026-08-05.md`. [PLAY GAME crash](project_playgame_crash.md)
- **WPE particles** re-enabled (2.00–2.02). [WPE](project_wpe_particles.md); repo `AUDIT_2026-08-05.md`.
- **Also closed**: selection outline restored (`a0847fc1`, hooks re-wired via `customDraw_Compose`); sculpt-grass (`1e9fb978`); editor zone-select 50→91 FPS.
- **VRAM campaign goal met 2026-08-04** (all 19 demos fit 4 GB). ★ TESTPRO1 is the standing test bed.
- **Standalone arc 2.69–2.71 Lee-confirmed 08-16**: boot splash, save/load slots (Lua 5.4 float filenames), producelogfiles=0 trace gate.
- **[Convert ALL stock DDS](../../../../../max/GameGuruMAXDX12/GameGuru%20Core/MILESTONE_DDS_CONVERSION.md)** — EXECUTED 08-16: 1641 files, sweep 19/19, VRAM down everywhere. Revert = copy back `D:\max\mipbackup`. Tools `tools/ddsconvert.py`, `tools/surfacescan.py`.

## Durable cross-cutting RULES (hard-won; not derivable from the code)

### Measuring

- ★★★ **[Measuring rules](project_measuring_rules.md) — READ BEFORE ANY A/B OR PERF CLAIM.** Ten
  hard-won rules, each paid for once. The headlines: count the WORK not the CALLERS (§3.17);
  size the window to the rarest thing you could be WRONG about, not the bug you are testing
  (§3.20, a 1-in-31 event hid behind a clean 12-dump result); caching is a RISK CALCULUS judged by
  its failure mode (§3.16 vs §3.18); an ordering derived from a noisy measurement cannot be
  stabilised, remove the dependency (§3.19); a single cell over the noise floor needs an
  INTERLEAVED same-session A/B of the two BUILDS (§2.92b); an invariant-justified cache can be
  tested OFFLINE, do not wait for a human to click (§3.19b–c); a PIX marker is not a profiler
  range and an idle bucket is an accounting hole (§2.94c); a "total" is not a "sum of parts"
  (§2.91); the CPU panel prints a call TREE so parents and children overlap by design (§3.13);
  a specification can be satisfied exactly while the goal is missed — say so with numbers (§3.20).

### Rendering / DX12
- ★★★ **When you throttle work, the throttle's unit must be the unit the DATA is shared over — not whatever collection you happen to be iterating.** A GameGuru character is SEVERAL OBJECTS sharing ONE ARMATURE (head/body/legs are separate meshes). Keying an animation-skip phase and distance off the OBJECT index put the head and body on different frames AND in different periods, and a head posed from an older armature state than its body is literally **a second head** (Lee, 3.25n). Decide ONCE per armature and have every consumer read that one answer — not "compute it identically in two places", which drifts. Notes §3.25n.
- ★★★ **A depth-prepass + depth-test-only colour pass makes the two passes' VERTEX MATHS a correctness contract — textually identical HLSL compiled twice is NOT the same code.** Through `rsqrt`/`sin`/`cos` (approximate instructions) a different schedule moves the vertex in WORLD space, not one ULP; reverse-Z `GREATER_EQUAL` then rejects the colour fragment → unwritten gbuffer → BLACK that flickers with tiny camera moves. **Fix by SHARING the compiled VS** (a PS input signature only has to be a SUBSET of the VS output). ★ Diagnose with two PSO twins: depth-write OFF and depth-test ALWAYS — if either clears it, no shader edit ever will.
- ★★★ **A visibility test added to ONE pass and not its partner writes depth without colour → BLACK.** Any `discard`/`clip`/`SV_ClipDistance` belongs to a FAMILY (prepass, colour, shadow, envprobe, reflection) and must land in all of them in the same edit. Bitten twice. ★ **Diff the pass pair's vertex shaders** before theorising. ★ A risk written in a design doc is not a control — 3.00 shipped the exact failure `DESIGN_FAR_TREES.md` §6.2 predicted.
- ★★ **Resource creation belongs on the MAIN THREAD, never inside a render callback.** Textures + CopyTexture inside `customDraw_*` (a job thread), bound in the same command list, AV'd in `DescriptorBinder::flush` twice. Split into an INIT and a GATE, with the ready flag set at the END of the upload.
- ★★★ **Wicked DX12 compiles PSOs lazily at BIND time and `PipelineStateDesc` stores POINTERS** — a PSO built from stack locals compiles from destroyed memory. Snapshot every state into per-PSO storage.
- ★★★ **fp16 is THE recurring bug class in this inch-scale world** — half tops out at 65504 = 1.66 km. Hit three times (2.07g light range², 2.32 RenderBatch distance, 2.89 probe parallax). `min16float` IS real fp16; `GetRange()`/`GetRadius()` are fp16 too.
- **DX11→DX12 ports must audit EVERY `UpdateBuffer`** — one shared buffer updated between consumers = a silent, validation-clean race. **Custom draw hooks must restore the pass's implicit binding contracts** (camera CB, common resources); slot-POINTER corruption is invisible to content instruments, only shader tints convict it.
- ★★ **A PAIRED accessor where only ONE half got the DX12 bridge is a bug template** — `GetImagePointer` (DX11-only → NULL) vs `GetImagePointerView` disabled EVERY screen-editor image (2.37). **Symptom "loaded but invisible"** → prove LOADED separately from VISIBLE.
- ★★ **DX12 pick tests the CACHED `aabb.layerMask`, not the live LayerComponent** — transient swap-pick-restore is a silent no-op (2.48). For any transient wicked state ask: who reads the cache vs the component? [Layer-cache pick](project_layer_cache_pick.md)
- ★★ **A superset bound is safe AS A BOUND, unsafe AS A POSITION** — enlargement is monotone through frustum/overlap tests; a centre fed to distance/plane/sort/projection is not. Measure the affected content (`DUMP_BIGAABB`), don't reason about it.
- ★★ **Cached local shadows only invalidate on a caster that MOVED; a REMOVED caster is invisible to that test** — call `wi::renderer::InvalidateLocalShadows()`, hooked on the deferred CONSUMER (`destroyme`), not the requester. [Shadow system](project_shadow_system.md)
- **A field added to a Wicked component struct MUST ride its `Serialize`** — `Entity_Duplicate` is a serialization round-trip. **GGMAX bits in upstream FLAGS enums live in reserved range 24-31** ⚠ NOT `options_stencilref` (bits 24-31 = stencil ref).
- ★★ **Texture streaming is ON by default** — gate = per-load opt-in && **editor only** (`gameisexe==0`) && plain `DDS ` magic. Residency is per-MATERIAL mip feedback → no static/dynamic distinction. ⚠ task #37's "DEFAULT OFF" title is WRONG. Repo `STREAMING_STATUS_2026-08-18.md`.
- **SVT atlas default is 12288** (engine `90375285`) — 8192 IS DEAD (starves). Judge from `VT: free=`, not screenshots.

### Debugging habits
- ★★★ **FIVE layers of "this is dead, skip it" hid one feature, each citing the others** (the far-tree chain). When a comment says "this is dead", check whether the thing that killed it is itself a bug.
- ★★ **A count of CALLS is not a count of SUCCESSES — a bool return you discard is a diagnostic you threw away.** `atlasSlices=38` read as success while all 76 uploads failed; counting successes gave 0 and one captured reason string named the cause instantly. ★ Companion: a SOLID-COLOUR bisect (force the PS to one colour, KEEP the real transform) clears geometry, transform, camera CB, depth, blend and root signature in ONE reading. ⚠ size the debug primitive — a fullscreen quad × 98,410 instances killed the app.
- ★★★ **A control that is CORRECT-BUT-DEFERRED is indistinguishable from a broken one.** Texture Detail (3.12) applied at load time by design and said so in its tooltip; Lee reported it as doing nothing. Nobody reads a tooltip that explains why the thing they just clicked did not respond. Either make it act now or make the deferral visible in the UI. Notes §3.19.
- ★★ **When a scan loop deliberately SKIPS the items it already owns, the explicit refresh is the ONLY path that can ever update them — so it must fire on ANY change, not just the direction you had in mind.** 3.19's Object Detail Distance re-tightened on `< s_prevCull`, so the slider worked downwards and was inert upwards. Same family as "a visibility test in one pass and not its partner".
- ★★ **A dead UI knob fails in TWO ways** — *severed chain* (call commented out) vs *clobbered value* (something re-derives the field every frame). Ask **"who writes it LAST, and what derives it?"** [Env probes](project_env_probes.md)
- ★★ **A shader edit is NOT LIVE until a VISIBLE change proves it** — and when it "isn't live", suspect the C++ VALUE PLUMBING first (an INI bool-ization, not the pipeline). Prove a knob's INI and LIVE paths equivalent. ★ ini knobs are INT PASSTHROUGH, never bool-ized.
- ★★ **An edge-triggered switch cannot be driven twice in one command** — a frame must observe the intermediate state. `SET_TERRAINGEN` set a flag on and off in one handler and a whole ladder reported the same value at every rung. **An edge-triggered teardown latch must clear on BOTH edges, and clearing state ≠ re-running the pass** — the 2.94 grass latch was one-way, and even fixed, grass would not regrow because `ProcessGrassChunks` is gated on dirty/sig/settle/nudge/camera-moved and a PARKED camera fires none (`g_grassPassNudge = true`).
- ★★ **"The element's object" ≠ "the entity's geometry"** — GG allocates MORE THAN ONE object per marker (a %probe = inner sphere + a separate 'root' shell). `DUMP_OBJENT` the object RANGE first. [Env probes](project_env_probes.md)
- ★ **"Make X the size of Y" wants the HALF-EXTENT** — `AABB::getRadius()` is the half-diagonal (√3×). Before scaling something the engine draws for ALL of a class, read the loop: harmless at scale 1 ≠ harmless at scale 60.
- **Map record ≠ live entity — validate LIVENESS** before trusting a scene lookup. **A port diff must include the CALLER'S dt clamp regime.** **Never verify a time-dependent system on a short window**; `frac(sin(big·seed))` degrades with seed magnitude.
- ⚠ **Don't reach for the obvious content lever — three of four are LEVEL-DATA MUTATORS**: `GGTrees_HideAll` writes 400K instance words, `GGGrass_RemoveAll` zeroes the 16 MB painted-grass map, `visuals->bWaterEnable` is re-derived every load. `SetTerrainVisible(false)` is the fourth trap: chunks persist and `Generation_Update` keeps making more.

### Environment / tooling
- ★★★ **`taskkill` reporting SUCCESS does NOT mean the process died.** 08-22 a MAX instance wedged at 6.9 GB survived `taskkill //F` by PID and IM, PowerShell `Stop-Process -Force` and `Process.Kill()`, all reporting success, while it held the GPU and stalled later launches. **Only `tasklist //FI "IMAGENAME eq ..."` is reliable** — verify GONE, never trust the kill's exit status.
- ★★★ **`bc` DOES NOT EXIST in this Git Bash — and a test that never reaches the path PASSES VACUOUSLY.** `$(echo "$X+1000" | bc)` yields EMPTY, `SET_CAMERA` silently takes malformed args, and "I flew around, all fine" is worthless. Use `awk 'BEGIN{printf "%.1f", a+b}'`; ALWAYS echo the reply and READ BACK the state you changed.
- ⚠★ **`OPEN_PROJECT` only sees MY GAMES projects** — the 19 hub demos need `SELECT_DEMO` + `CLICK edit_game` + `CLICK_ONLY_LEVEL` (`tools/probe_one.sh`); a My Games project needs `OPEN_PROJECT` + `CLICK_ONLY_LEVEL`. A full ladder once ran on the hub's EMPTY scene and produced a plausible, worthless table. Gate on the LOAD, never on exit 0.
- **Legacy error paths are MODAL** (`RunTimeError` = MessageBox) — headless reads them as hangs. `timestampactivity` BUFFERS (loses tail under `taskkill //F`); hang forensics need open-append-close per line.
- **Runtime `fopen` files land in the game's `Files/` CWD**, not the exe dir. `wi::platform::Exit()` **RETURNS**.
- ★ **Never leave working files in the build OUTPUT dir** — it is packaged and un-versioned. Scratchpad for throwaway, repo `tools/` for keepers. ⚠ its 5 loose `run_*.sh`/`perf_test.sh` are stale, not mine; `dxdiagsystemspecs.bat` is a PRODUCT file.
- ⚠ MSVC **C1061** in harness dispatch — hoist new commands into helper chains, don't extend the ladder. ⚠ Combined heredocs in one Bash call fail with "unexpected EOF" — split them.
- ⚠ `SELECT_ENTITY` is by index, not visibility. ⚠ Verify subagent claims by grep; Windows `tail -f` emits phantom partial lines. ⚠ `tail` in a pipeline withholds a background script's output until it exits.

## CRITICAL BUILD RULES
- **NEVER use Debug builds** (exit 3). ALWAYS Release. Release outputs directly to the EXE dir — no copy step.
- **Build (bash)**: `cd "D:/max/GameGuruMAXDX12/GameGuru Core" && ./build.bat Release` — cwd MUST be GameGuru Core; no `cmd //c`.
- **Launch**: `cd "D:/DEV/BUILD/GameGuru Wicked MAX Build Area/Max/" && "./GameGuruMAX.exe" &`
- **Kill MAX before building**: `taskkill.exe //IM GameGuruMAX.exe //F 2>/dev/null; true; sleep 2` — then VERIFY with tasklist.
- **Game build does NOT rebuild the engine lib** — `cd D:/max/WickedEngineDX12 && ./build_wicked.bat Release` first.
- ★ The ENGINE builds in **~40 seconds** — do not defer an engine change as “expensive”. ⚠ The engine repo's branch is **`master`**, not `main` (`git push origin master`).
- **Shader compile has TWO paths** — engine .cso freshness unreliable; GGTerrain/game shaders need a GAME rebuild. Prove a shader edit landed by the .cso SIZE changing, not its timestamp. [Shader build pipeline](project_shader_build_pipeline.md)
- **NEVER `build.bat | tail && probe`** — gate on real error count. [feedback](feedback_gate_probes_on_build_errors.md)
- **A killed harness run leaves a stale `auto_command.txt` the NEXT launch executes.** [feedback](feedback_stale_auto_command.md)
- ⚠ **Leaked parallel runners corrupt sweeps**; `pkill` dead in Git Bash — `ps -W`+`kill -9`, lockfile every long script. [feedback](feedback_leaked_parallel_runner.md)
- **Building from a worktree**: junction + copied *.lib + cmd /c wrapper. [feedback](feedback_worktree_build.md) · **Worktree session: EVERY file path starts with the worktree prefix** [feedback](feedback_edit_worktree_path.md) · **mirror EVERY file the branch touches** [feedback](feedback_mirror_complete_file_set.md) · **check main repo status first** [feedback](feedback_check_main_repo_status.md)
- **DX11 reference `D:\max\GameGuruMAX` and `D:\max\WickedRepo` are READ ONLY.** [feedback](feedback_dx11_reference_readonly.md)
- **"Commit and push" means** local `main` + origin; never disturb stashes. [feedback](feedback_commit_and_push_means_main.md)
- **Notes + commit + push as each milestone lands.** [feedback](feedback_notes_commit_as_you_go.md)
- **Be decisive; autonomous test loop wanted** — kill/rebuild/launch/screenshot without asking. [feedback](feedback_be_decisive_dont_over_confirm.md) · [feedback](feedback_autonomous_testing.md) · **user idle ~10 min = AWAY, kill MAX** [feedback](feedback_kill_max_after_10min.md)
- **Two failed attempts = wrong approach, not wrong parameters.** [feedback](feedback_two_attempts_change_approach.md) · **Don't thrash on automation** [feedback](feedback_dont_thrash_on_automation.md) · **Build the instrument that NAMES the culprit** [feedback](feedback_instrument_before_theory.md)

## Topic index
- **Lua logic cost**: [Per-entity logic gating](project_lua_logic_cost.md) — ★ the engine ALREADY gates logic at 750 units (2000 for characters); `SetEntityAlwaysActive` opts OUT, and door.lua/door_sliding.lua both do. ⚠ The "Top Ten Expensive Logic" box prints QPC **ticks not µs** (10× smaller than it reads), shows nine not ten, and times the whole per-entity update.
- **Method**: [Measuring rules](project_measuring_rules.md) — 19 rules on instruments, windows, A/B protocol and when a null result is the TEST's fault. Read before any perf claim.
- **VRAM**: [floor / 4 GB min spec](project_vram_floor.md) (repo `VRAM_FLOOR.md` = authority; acceptance test for ANY pipeline change) · [knob plan](project_vram_audit.md) · [census + hub sweep](project_vram_census.md) · [grass benchmark = THE gate](project_grass_benchmark.md)
- **Particles (three systems, don't confuse)**: [WPE](project_wpe_particles.md) · [gpup/.arx](project_gpup_particles.md) · ravey_particles out of scope
- **Performance**: [Switch Escape DX11 vs DX12](project_switchescape_perf.md) · [Performance](project_performance.md) (⚠ its single-queue "do-not-re-chase" verdict is STALE) · [G&P panel audit](project_gfx_perf_panel_audit.md) · [Demo FPS baseline](project_demo_fps_baseline.md) · [Harness: open My Games](project_harness_open_my_games.md)
- **Terrain**: [render path — shipping = Wicked NATIVE SVT](project_terrain_render_path.md) · [VT zoom squares](project_vt_zoom_squares.md) · [sculpt/paint](project_terrain_editing.md) · [blendmap rules](project_terrain_blendmaps.md) · [texture mismatch](project_terrain_texture_mismatch.md) · [chunk scale](project_chunk_scale.md)
- **Trees / transparency / shadows / lights**: [Phase 5 trees](project_trees_phase5.md) (**Wicked SetAlphaRef is INVERTED**) · [DX11 parity baseline](project_dx11_parity_baseline.md) · [Transparency parity](project_transparency_parity.md) · [Point-shadow budget](project_point_shadow_budget_design.md) · [Shadow system](project_shadow_system.md) · [Shadow flicker](project_shadow_flicker.md) · [Light falloff](project_light_falloff_parity.md)
- **Engine deltas**: [Wicked Engine changes](project_wicked_engine_changes.md) — repo `WICKED_ENGINE_CHANGES.md` is the authority; every engine edit needs a delta row.
- **Tech debt**: [UI hookup audit](project_ui_audit_2026_07_28.md) · [Save crash: group thumbnails](project_save_crash_group_thumbnails.md) · [Level file version](project_level_version_debt.md) · [Reload corruption](project_reload_corruption.md) · [Parrot corruption](project_parrot_corruption.md) · [Review 07-17](project_review_2026_07_17.md)
