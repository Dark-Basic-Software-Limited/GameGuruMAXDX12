---
name: project-gfx-perf-panel-audit
description: "Audit of the editor Graphics & Performance panel checkboxes in DX12 — which are wired vs orphaned DX11-era flags, the safe readout fix, and the riskier wiring backlog."
metadata: 
  node_type: memory
  type: project
  originSessionId: 63b96d44-9243-46fb-bc37-9eb4d4be60b9
  modified: 2026-07-25T00:14:34.491Z
---

# Graphics & Performance panel: wiring audit (2026-07-24)

## >>> WALK-THROUGH POSITION (resume here) <<<
Going top→bottom through the panel, deciding wire-it / hide-it / leave-it for each. DONE so far (all game main, pushed):
FSR wired `77537906` (+SET_FSRSHARP `e206f8c0`); Lower Animation & LUA wired `fc959fe5` (engine 1.29 `93b7c189`); Frustum/Apparent Culled readout fixed `77ce8b2f`; Debug Bounding Box revived + Debug removed `d954fe7b`/`ea82c001`; 5 dead cull children `#if 0`'d, parent Occlusion Culling kept `c01c8d2d`; Occluded Point/Spot Shadows readouts wired to real data `77c9a688`; **Apparent Size slider WIRED `a7777ce7` (engine 1.30 `8ad10e54`) — see below**. Delayed Shadows/Laptop were already wired.
**Apparent Size — DONE & USER-TEST-READY 2026-07-24 (engine `8ad10e54` delta 1.30 + game `a7777ce7`).** Was DEAD (`maxApparentSize`/`t.visuals.ApparentSize` set but never consumed — the DX11 cull lived in the closed engine DLL, confirmed zero consumers even in the DX11 ref tree). Implemented as a scale-invariant apparent-SCREEN-SIZE cull in ENGINE `wiRenderer::UpdateVisibility` object loop: object passes frustum but world `aabb.getRadius()/distance < tangent` → dropped from `visibleObjects`. World AABB radius bakes in original size × scale; /distance = on-screen size → invariant to both (exactly the user's spec: "don't render distant objects that are just too small"). Threshold = atomic `gg_apparent_cull_bits` (fixed-point µ-units, 0=off), driven per-frame by game `MasterRenderer::Update`: `tangent = (maxApparentSize − 0.000008)·g_apparentCullK` (`wickedcalls_part0.cpp`); default slider (0.000008) → tangent 0 → NO cull on load (existing levels unchanged); slide right ramps it. **K RECALIBRATED 2026-07-25 `5f56acb3`: K=320 was too aggressive (a level saved at slider 1.00 hit tangent 0.0294, gutting mid props); dropped to K=59 so slider 1.00 = the old 0.25 (tangent ~0.0054 — the value that looked right on TESTPRO1). Levels keep their displayed/stored 1.00; only the mapping constant changed (saved ApparentSize untouched). New range: slider 0.25→0.0010, 1.0→0.0054, 2.0→0.0113.** **Verified on TESTPRO1 via the real slider path (K=320 build): the small emissive Aztec Column G at the saved gate camera vanishes at slider ~0.3, nearer figure/torches persist; at max only huge near geometry survives (visible 3334→701; tangent 0.5 whole gate culled 562→17). Post-recalibration: at the level's stored 1.0 the column is now gently visible (47 yellow px).** Also fixes the "Frustum/Apparent Culled" readout name (it reads `objects − visibleObjects`, now includes apparent-culled). Harness `SET_APPARENTSIZE <tangent>` (direct override) / `SET_APPARENTSIZE slider <fASize>` (full UI path incl. K). Shadow cascades cull separately → a culled speck still casts its sub-pixel shadow (fine). Follow-up if wanted: K/curve is a feel knob (one-line in `g_apparentCullK`); could expose K or make the slider non-linear.
**LOD Multiplier + 5 "Use Fastest LOD" children — HIDDEN 2026-07-25 `0a74e6c2` (`#if 0` w/ reason).** Full-tree trace confirmed ALL dead in DX12 (stored+saved only, DX11 consumers were in the closed engine DLL): `fLODMultiplier` (Wicked picks LOD by projected SCREEN SIZE — `Scene::ComputeObjectLODForView`: `lod=log2(1/maxScreenDim)+object.lod_bias` (wiScene.cpp:9035), NOT a global distance mult, so the slider can't drive it as-is); and `bShadowsLowestLOD`/`bProbesLowestLOD`/`bRaycastLowestLOD`/`bPhysicsLowestLOD`/`bReflectionsLowestLOD` (zero consumers each — **corrects the earlier note that Physics-Use-Fastest-LOD was wired build-time; it is NOT**). **DEEP-TRACED 2026-07-29 (user asked; tooltip added `2aef3325`): it is a debug FORCE-FULL-DETAIL override, NOT a perf lever — unticked(default)=LOD system on; ticked=skip *_lod.dbo substitute AND skip LOD-limb pruning (all lod_1..3 + collision_mesh frames become live Wicked objects = object bloat). Load-time only, never persisted, resets each launch. DX11 benefit of the system it disables: distance-switched baked index sets (400/600/800u × LOD Multiplier) + 5 lowest-LOD pass overrides; DX12 keeps the _lod.dbo subsets alive via Wicked screen-size LOD (subsets_per_lod). Content reality: 100 _lod.dbo all in the SNIFF user pack, stock packs ZERO, LOD frames only in 9 Max Collection animals — no-op on stock islands.** **KEPT the parent "Disable LOD Load"** (`bDisableLODLoad`) — genuinely WIRED: drives GameGuru's OWN LOD path (loads external `*_lod.dbo` at entity load M-Entity_part0.cpp:1095; gates in-model LOD-frame pick M-Entity_part5.cpp:1296), independent of Wicked's screen-size LOD. If LOD Multiplier is ever wanted, the DX12 hook is a global add to `object.lod_bias` — but only bites on meshes with multiple Wicked LOD subsets (`GetLODCount()>1`); most GG content uses the load-time `_lod.dbo` swap instead, so check subset counts before investing.
**Laptop checkbox — NOW WIRED 2026-07-25 (engine 1.31 `de50638b` + game `bd227a5a`).** Was dead (only set the DX11 point-shadow globals). Now a "twice as aggressive" Delayed Shadows: when Delayed Shadows + Laptop are both ticked, the engine holds the far directional cascades (1..N) for 4 frames instead of 2 (`SetDelayedShadowCascadeInterval`, default 2 → 4), driven per-frame by `MasterRenderer::Update` from `g_bDelayedShadowsLaptop`. Cascades 1..N still refresh together → flicker-safe at any interval (see [[project-shadow-flicker]]/[[project-shadow-system]]). Harness `SET_LAPTOP_SHADOWS <0|1>`. Verified: interval-4 shadows statistically identical to interval-2 at a static camera. TESTPRO1 is sun-only/light-shadow so no big FPS delta here — user testing in a heavy-shadow scene.
**NEXT = panel walk-through essentially COMPLETE:** everything top→bottom is wired, hidden-with-reason, or a kept dev tool. Bigger open items are the RISKIER backlog below (anim-cull-checkbox honesty, shadow-cull counters, FSR2) — supervised/engine work, not simple panel wiring.
**Working style: be decisive — implement the obvious call, don't ask each step; user reverts if wrong. See [[feedback-be-decisive-dont-over-confirm]].**

User asked: verify every checkbox in the editor **Graphics and Performance** panel is actually connected to the DX12/Wicked engine (crowd of ~30 animating Aztecs added to TESTPRO1 island to load the animation system). Panel source: `GameGuru/Source/M-GridEditB_part24.cpp` `Graphics_Performance_Settings()`. Method: 5 parallel consumer-trace agents across game+engine repos (authoritative — a flag with zero consumers cannot move the frame) + empirical FPS/MS A/B on the per-frame-wired ones. TESTPRO1 editor is **CPU-bound** (CPU ~15.4ms / GPU ~8.5ms / ~61 FPS).

## The finding: the panel is mostly orphaned DX11-era flags
Of 20 controls:
- **Per-frame WIRED (2):** Delayed Shadows (`g_bDelayedShadows` → wiRenderer SetDelayedShadowCascadesEnabled + SetShadowLODOverrideEnabled; EMP Shadowmap 0.79→0.66ms staggered), Occlusion Culling (`t.visuals.bOcclusionCulling` → wiRenderer::SetOcclusionCullingEnabled; EMP key-9 makes GPU occl passes appear/vanish; ~0 FPS here = CPU-bound + few occluders).
- **Load/build-time WIRED (1):** Disable LOD Load (`bDisableLODLoad` — external `*_lod.dbo` substitute load M-Entity_part0.cpp:1095 + in-model LOD-frame gate M-Entity_part5.cpp:1296). `g_iUseLODObjects` gates whether the (now-hidden) child checkboxes draw. **CORRECTION 2026-07-25: "Physics Use Fastest LOD" (`bPhysicsLowestLOD`) is NOT wired — full-tree trace shows zero consumers (the `lastPhysicsLODLevel` in GGTerrain is a hardcoded −1, unrelated). Now hidden with the other dead LOD children `0a74e6c2`.**
- **Lower Animation & LUA Speed — NOW WIRED 2026-07-24 (engine `93b7c189` + game `fc959fe5`, delta 1.29).** Was PARTIAL (LUA half alive `M-LUA.cpp:828`; animation half dead — GGAnimBridge removed as too slow `89873913`, engine throttle in `RunAnimationUpdateSystem` left gated on a hardcoded static=0). (b) wired the engine throttle to the checkbox via `gg_anim30fps_enabled/frame` set per-frame by `MasterRenderer::Update`; fixed parity to a real per-frame counter (old `iAnimFrames` was a racy per-job static). (c) per-object distance gate `gg_anim30fps_dist2` (game `g_animThrottleFarDist` default 2000, clamped ≤60000) so near-camera armatures stay full-rate. Timer advances on skipped frames (true 30fps, not slow-mo); keeps `!updateonce` + quaternion guards → parrot-safe (no tripwire). **Measured: throttle-all halves anim sampling 0.61→0.20ms; default(2000) ~0.60→0.50ms (crowd all <~4000 units).** Harness `SET_ANIM30FPS <0|1> [farDist]`. **OPEN: absolute distance is scene-scale-dependent — should become a UI slider or an apparent-size gate (scale-invariant). The panel checkbox that toggles it works; no relabel needed now.**
- **FSR — FIXED 2026-07-24 (game `77537906`, FSR1 spatial, game-only):** was a no-op (resolutionScale never lowered). Now each mode lowers 3D internal res (Ultra 1/1.3, Quality 1/1.5, Balanced 1/1.7, Performance 1/2.0, None 1.0) → engine EASU+RCAS `Postprocess_FSR` upscales to display. Wired in BOTH panel (M-GridEditB_part24) + load path (M-GridEditB_part3) lock-step. **Companion blocker fixed:** editor selection-outline RTs (`master_part1.cpp` ResizeBuffers) were physical-res but bound with internal-res depth → D3D12 DEVICE_DRAW_VIEW_DIMENSION_MISMATCH at scale<1; sized to `GetInternalResolution()`. New `SET_FSR <0-4>` harness cmd (reports internal vs physical res). Verified: 0.5x → internal 768x400/physical 1536x801, Postprocess_FSR 0.12ms, no device errors, clean softer upscale + crisp UI. **CPU-bound editor = FPS flat (~64); GPU dropped 9.22→8.74ms — the win is on GPU-bound views.** 3 minor follow-ups: (1) test MSAA-on + object-selected + FSR (untested branch, static analysis says safe); (2) renderpass block in ResizeBuffers derefs `*GetDepthStencil()` unguarded (pre-existing, dormant — add null guard); (3) FXAA forced-on not restored on None (cosmetic, parity-preserved). FSR2 (temporal) is a future upgrade — fully impl engine-side, unused by game, needs the sampler-mip-bias-restore fix. Sharpness slider is inverted (0.0=sharpest).
- **The 5 per-category cull children — COMMENTED OUT 2026-07-24 (game `c01c8d2d`, `#if 0` with reasoning).** Terrain Chunk / Point Shadow / Spot Shadow / Object / Animation Culling: all dead in DX12 (Wicked does object+frustum cull every frame in UpdateVisibility, animation cull always-on, terrain via SVT, point/spot shadows via the local shadow-budget system). Kept the PARENT **Occlusion Culling** — genuinely wired to the engine GPU occlusion-query pass, useful on GPU-bound/occluder-dense levels (user decision: keep for now). Readouts stay live outside the `#if 0`.
- **DEAD checkboxes (11 at audit; 5 of the culling ones now `#if 0`'d per above):** Laptop (`g_bDelayedShadowsLaptop` — only sets dead bEnableDelayPointShadow/pointShadowScaler), Terrain Chunk Culling (only reader GGTerrain_Draw early-returns — Wicked SVT terrain), Point Shadow Culling, Spot Shadow Culling, Object Culling (only commented engine code), Animation Culling (reader GGAnimBridge_PreUpdate commented; engine culls via its own hardcoded static), Apparent Size (`maxApparentSize` — no consumer; Wicked has no CPU apparent-size cull), LOD Multiplier (`fLODMultiplier` — no consumer), Shadows/Probes/Raycast/Reflections Use Fastest LOD (no consumers).
- **Debug checkboxes (dev-tools only):** "Debug" REMOVED + "Debug Bounding Box" REVIVED 2026-07-24 (game `d954fe7b`). Both were dead (occlusion-debug disabled in the port). Debug Bounding Box now draws a yellow world-AABB box per visible object via `wiRenderer::DrawBox(aabb_objects[objects.GetIndex(entity)], ...)` (M-GridEditB_part23.cpp DrawOccludedObjects bBox branch; DrawBox renders as-is — same path as selection boxes, DrawDebugWorld ungated each frame). Removed "Debug" + its dead-only readouts (Occluded Objects, Occluded Terrain chunks [dead under SVT terrain], Culled Animations); the p/s light markers still show via the profiler overlay (tmpdebugfunc). Draws while the Game Settings panel is up; dev-tools-only (g_iDevToolsOpen>=1). NOT self-verified on-screen (can't drive the ImGui panel from the harness) — build+DrawBox-path+logic verified, user does the visual tick.
- **Dead readouts (4):** Frustum/Apparent Culled (**FIXED** `77ce8b2f`); Occluded Point/Spot Shadows (`iCulled*`/`iRendered*` never written → "0 r(0)") **FIXED `77c9a688`** — now real "Point/Spot Shadow Lights: N" (WickedCall_GetCube/SpotShadowLights) + "Local Shadows: G granted, C capped, R rendered" (wiRenderer::GetLocalShadowStats, the [[project-point-shadow-budget-design]] counters); reads 0 on sun-only island, non-zero on torch-lit levels. Culled Animations (never written) — REMOVED with the Debug checkbox.

Note: every live consumer reads the **standalone global**, never the `t.visuals.*`/`t.gamevisuals.*` mirror (those are SAVE/LOAD only, synced back into the globals at level load `M-GridEditB_part3.cpp:1749-1753`).

## SAFE change made — game `77ce8b2f` (pushed, main==origin)
Fixed the user-flagged "Frustum/Apparent Culled: 0". New bridge `WickedCall_GetFrustumVisibleObjects()` (wickedcalls.h/_part3.cpp) returns `master.masterrenderer.visibility_main.visibleObjects.size()`; both readouts (part24 panel + part23 "FC:" overlay) now show `totalObjects − frustumVisible` (clamped ≥0). Verified by construction: the harness already prints that exact expression as `VISIBLE_OBJECTS` (754 vs SCENE_OBJECTS 8265 → readout ≈ 7511). Display-only, game-only build. It's a pure frustum count (no apparent-size cull exists).

## RISKIER wiring backlog (do SUPERVISED; most need engine changes = re-apply deltas on pull)
1. **Lower-Animation half = DONE** (delta 1.29, see above). **Animation Culling checkbox (#11) still dead** — the engine culls off-screen animations unconditionally (`wiScene.cpp` hardcoded `bEnableAnimationCulling=1`); wiring the checkbox to it is now the SAME easy pattern as 1.29 (a `gg_*` global set from the panel). Low value though (culling already always-on); mostly a "make the checkbox honest" fix. Do NOT re-enable the old `GGAnimBridge` hooks (`master_part1.cpp:298/308`) — too slow + slow-motion bug.
2. **FSR — DONE (`77537906`, FSR1)**, see the FSR line above. Follow-up: FSR2 (temporal, higher quality) for in-game; the sharpness slider is inverted (0.0=sharpest).
3. **Point/Spot Shadow Culling + counters** — never ported to DX12. Either implement, or repoint the dead readouts at the existing `localShadowBudget` counters (`SHADOW_LOCAL_GRANTED/CAPPED/RENDERED`). MODERATE.
4. **Apparent Size — DONE** (`a7777ce7`/engine 1.30, see walk-through block above). **LOD Multiplier** still dead — map `fLODMultiplier` to Wicked per-object LOD switch distance. MODERATE, real feature.
5. **Object / Terrain Chunk Culling, Shadows/Probes/Raycast/Reflections Fastest LOD** — redundant with engine internal frustum/occlusion cull + internal LOD. Low value — hide or leave.
6. **Product decision:** grey-out/hide the 11 dead checkboxes so users don't tune inert controls.

Session experiment log: scratchpad `gfx_panel_audit.md`. Related: [[project-performance]] (CPU-bound frame model), [[project-shadow-system]] (delayed cascades), [[project-terrain-render-path]] (why terrain-chunk-cull is dead), [[project-point-shadow-budget-design]] (localShadowBudget counters).
